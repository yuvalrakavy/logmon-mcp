{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "logmon-broker-protocol-v1",
  "description": "logmon broker JSON-RPC protocol, PROTOCOL_VERSION = 1",
  "version": 1,
  "definitions": {
    "LogsRecent": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsRecent",
      "type": "object",
      "properties": {
        "count": {
          "description": "Number of log entries to return (default: 50)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "filter": {
          "description": "Optional DSL filter expression",
          "type": [
            "string",
            "null"
          ]
        },
        "trace_id": {
          "description": "Filter logs by trace ID (32-char hex)",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "LogsRecentResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsRecentResult",
      "type": "object",
      "properties": {
        "logs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogEntry"
          }
        },
        "count": {
          "description": "Number of records that MATCHED the filter and were returned (capped by\nthe requested `count`). This is the \"matched\" count in B2's vocabulary.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "scanned": {
          "description": "Number of buffered records EXAMINED to produce this result (B2). With\n`count == 0`, `scanned > 0` means \"filter's fault, data is flowing\",\nwhile `scanned == 0` means an empty buffer / dead pipeline.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "buffer_total": {
          "description": "Total records currently in the queried buffer.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "buffer_oldest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "buffer_newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "truncated": {
          "description": "B5: true when the query's lower bound (a `b>=` / `c>=` cursor) predates\nthe oldest retained record — some in-window records were evicted, so this\nresult is not the complete window.",
          "type": "boolean",
          "default": false
        },
        "evicted_before_window": {
          "description": "Seqs between the requested window start and the oldest retained record\n(an upper bound on how many records rolled off), when `truncated`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "cursor_advanced_to": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "logs",
        "count"
      ],
      "$defs": {
        "LogEntry": {
          "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "level": {
              "$ref": "#/$defs/Level"
            },
            "message": {
              "type": "string"
            },
            "full_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "host": {
              "type": "string"
            },
            "facility": {
              "type": [
                "string",
                "null"
              ]
            },
            "file": {
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "additional_fields": {
              "type": "object",
              "additionalProperties": true
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "matched_filters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "source": {
              "$ref": "#/$defs/LogSource",
              "default": "Filter"
            }
          },
          "required": [
            "seq",
            "timestamp",
            "level",
            "message",
            "host"
          ]
        },
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        }
      }
    },
    "LogsFields": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsFields",
      "description": "`logs.fields` — the map an agent needs before it can name an axis.",
      "type": "object",
      "properties": {
        "filter": {
          "description": "DSL filter describing the population to describe. Defaults to everything.\n\nCursor qualifiers (`c>=`) are **rejected**: a cursor advances on read, so\na second identical call would describe a different population.",
          "type": [
            "string",
            "null"
          ]
        },
        "top_values": {
          "description": "Most-frequent values reported per field (default 3).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "min_coverage_pct": {
          "description": "Omit fields present on fewer than this share of matched records.\n`0` (the default) reports everything, **including fields at 0%** — an\nabsent built-in is a fact worth seeing, not a row worth hiding.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "LogsFieldsResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsFieldsResult",
      "type": "object",
      "properties": {
        "fields": {
          "description": "Coverage descending, then field name — stable across identical calls.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/FieldStats"
          }
        },
        "matched": {
          "description": "Records that passed the filter.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "scanned": {
          "description": "Records EXAMINED — the whole ring, never a `count`-limited prefix. A\ndescription of the buffer that silently described its newest slice would\nbe the exact sampling bias this family of reads exists to remove.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "buffer_total": {
          "description": "Total records currently in the queried buffer.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "buffer_oldest_seq": {
          "description": "The ring's real bounds.\n\nPresent because `truncated` can only speak about a WINDOW: it needs a\nresolved lower bound, so a call with no `b>=` reports `false` however\nmuch rolled off. These two, with `lost_below`, are how a caller sees a\nwrapped buffer regardless of the filter.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "buffer_newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "lost_below": {
          "description": "The lowest seq this buffer can still speak for; `0` means nothing has\never been evicted.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 0
        },
        "truncated": {
          "description": "True when the query's lower bound predates the oldest retained record,\nso this describes an incomplete window. **Requires a lower bound to be\nmeaningful** — see `buffer_oldest_seq`.",
          "type": "boolean",
          "default": false
        },
        "evicted_before_window": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "names_capped": {
          "description": "True when the field-NAME cap was reached and some names are missing from\n`fields`. A truncated map presented as the whole one would be the same\nsilent-undercount this read exists to prevent.",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "fields",
        "matched",
        "scanned",
        "buffer_total"
      ],
      "$defs": {
        "FieldStats": {
          "description": "One field's row in the map.\n\nIdentified by `(source, field)`, not by `field` alone: two rows may share a\nname and differ in source. Read `selector` to know what to actually type.",
          "type": "object",
          "properties": {
            "field": {
              "description": "The field's name as it appears on the record.",
              "type": "string"
            },
            "source": {
              "$ref": "#/$defs/FieldSource"
            },
            "selector": {
              "description": "**The DSL spelling that matches this row**, ready to paste into a\nfilter — `fi` for the built-in `file`, `file` for the additional field of\nthe same name.\n\n`null` means no log filter selector reaches this field at all. That is\nthe case for `trace_id` and `span_id`: the parser removes them from\n`additional_fields`, so `trace_id=…` in a filter would match nothing and\nreport no error. Use the `trace_id` PARAMETER on `logs.recent` instead.",
              "type": [
                "string",
                "null"
              ]
            },
            "present": {
              "description": "Records carrying it, out of `matched`.",
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "coverage_pct": {
              "description": "Share of `matched` carrying it.",
              "type": "number",
              "format": "double"
            },
            "distinct": {
              "description": "Distinct values — **`null` once the cap was exceeded**, never a partial\ncount presented as a total.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint",
              "minimum": 0
            },
            "top_values": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TopValue"
              }
            },
            "kind": {
              "$ref": "#/$defs/ValueKind"
            }
          },
          "required": [
            "field",
            "source",
            "present",
            "coverage_pct",
            "top_values",
            "kind"
          ]
        },
        "FieldSource": {
          "description": "Where a field's value lives on a record — and therefore which filter\nselector reaches it.\n\n**A name alone does not identify a field.** GELF strips the `_` prefix, so a\npayload sending both `file` and `_file` produces a top-level `LogEntry.file`\nAND an `additional_fields[\"file\"]`. Those are two different values reached by\ntwo different selectors (`fi` versus `file`), and merging them into one row\ndouble-counts coverage and hides the built-in's true (often zero) presence —\nwhich is the one fact this whole read exists to surface.",
          "oneOf": [
            {
              "description": "A dedicated slot on the record: level, message, host, facility, file,\nline. Reached by a short DSL selector (`fi`, `ln`, …).",
              "type": "string",
              "const": "builtin"
            },
            {
              "description": "An underscore-prefixed GELF extra, stripped of its prefix. Reached by its\nown name.",
              "type": "string",
              "const": "additional"
            },
            {
              "description": "Lifted out of `additional_fields` by the parser (`trace_id`, `span_id`).\nReal, but **no log filter selector reaches it** — see `selector`.",
              "type": "string",
              "const": "promoted"
            }
          ]
        },
        "TopValue": {
          "type": "object",
          "properties": {
            "value": {
              "description": "Rendered exactly as the filter matcher renders it, so this string can be\npasted into a filter and will match.",
              "type": "string"
            },
            "count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "value",
            "count"
          ]
        },
        "ValueKind": {
          "description": "What the observed values of a field look like.\n\nLoad-bearing rather than decorative: it is what tells a caller which fields\na numeric aggregation could ever sum, without sampling records to find out.",
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "string",
                "integer",
                "float",
                "bool"
              ]
            },
            {
              "description": "Integers and floats seen under one name. Still summable — an emitter\nwriting `100` and `100.5` for one quantity is ordinary, and calling that\n`mixed` would warn against the one case where the warning is wrong.",
              "type": "string",
              "const": "number"
            },
            {
              "description": "Genuinely disagreeing kinds under one name (a string and a number, say).\nNot an error — emitters do this — but a caller must not assume it can be\nsummed.",
              "type": "string",
              "const": "mixed"
            },
            {
              "description": "Null, array or object: present, but neither a dimension nor a number.",
              "type": "string",
              "const": "other"
            }
          ]
        }
      }
    },
    "FieldStats": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FieldStats",
      "description": "One field's row in the map.\n\nIdentified by `(source, field)`, not by `field` alone: two rows may share a\nname and differ in source. Read `selector` to know what to actually type.",
      "type": "object",
      "properties": {
        "field": {
          "description": "The field's name as it appears on the record.",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/FieldSource"
        },
        "selector": {
          "description": "**The DSL spelling that matches this row**, ready to paste into a\nfilter — `fi` for the built-in `file`, `file` for the additional field of\nthe same name.\n\n`null` means no log filter selector reaches this field at all. That is\nthe case for `trace_id` and `span_id`: the parser removes them from\n`additional_fields`, so `trace_id=…` in a filter would match nothing and\nreport no error. Use the `trace_id` PARAMETER on `logs.recent` instead.",
          "type": [
            "string",
            "null"
          ]
        },
        "present": {
          "description": "Records carrying it, out of `matched`.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "coverage_pct": {
          "description": "Share of `matched` carrying it.",
          "type": "number",
          "format": "double"
        },
        "distinct": {
          "description": "Distinct values — **`null` once the cap was exceeded**, never a partial\ncount presented as a total.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "top_values": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TopValue"
          }
        },
        "kind": {
          "$ref": "#/$defs/ValueKind"
        }
      },
      "required": [
        "field",
        "source",
        "present",
        "coverage_pct",
        "top_values",
        "kind"
      ],
      "$defs": {
        "FieldSource": {
          "description": "Where a field's value lives on a record — and therefore which filter\nselector reaches it.\n\n**A name alone does not identify a field.** GELF strips the `_` prefix, so a\npayload sending both `file` and `_file` produces a top-level `LogEntry.file`\nAND an `additional_fields[\"file\"]`. Those are two different values reached by\ntwo different selectors (`fi` versus `file`), and merging them into one row\ndouble-counts coverage and hides the built-in's true (often zero) presence —\nwhich is the one fact this whole read exists to surface.",
          "oneOf": [
            {
              "description": "A dedicated slot on the record: level, message, host, facility, file,\nline. Reached by a short DSL selector (`fi`, `ln`, …).",
              "type": "string",
              "const": "builtin"
            },
            {
              "description": "An underscore-prefixed GELF extra, stripped of its prefix. Reached by its\nown name.",
              "type": "string",
              "const": "additional"
            },
            {
              "description": "Lifted out of `additional_fields` by the parser (`trace_id`, `span_id`).\nReal, but **no log filter selector reaches it** — see `selector`.",
              "type": "string",
              "const": "promoted"
            }
          ]
        },
        "TopValue": {
          "type": "object",
          "properties": {
            "value": {
              "description": "Rendered exactly as the filter matcher renders it, so this string can be\npasted into a filter and will match.",
              "type": "string"
            },
            "count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "value",
            "count"
          ]
        },
        "ValueKind": {
          "description": "What the observed values of a field look like.\n\nLoad-bearing rather than decorative: it is what tells a caller which fields\na numeric aggregation could ever sum, without sampling records to find out.",
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "string",
                "integer",
                "float",
                "bool"
              ]
            },
            {
              "description": "Integers and floats seen under one name. Still summable — an emitter\nwriting `100` and `100.5` for one quantity is ordinary, and calling that\n`mixed` would warn against the one case where the warning is wrong.",
              "type": "string",
              "const": "number"
            },
            {
              "description": "Genuinely disagreeing kinds under one name (a string and a number, say).\nNot an error — emitters do this — but a caller must not assume it can be\nsummed.",
              "type": "string",
              "const": "mixed"
            },
            {
              "description": "Null, array or object: present, but neither a dimension nor a number.",
              "type": "string",
              "const": "other"
            }
          ]
        }
      }
    },
    "TopValue": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TopValue",
      "type": "object",
      "properties": {
        "value": {
          "description": "Rendered exactly as the filter matcher renders it, so this string can be\npasted into a filter and will match.",
          "type": "string"
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "value",
        "count"
      ]
    },
    "ValueKind": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ValueKind",
      "description": "What the observed values of a field look like.\n\nLoad-bearing rather than decorative: it is what tells a caller which fields\na numeric aggregation could ever sum, without sampling records to find out.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "string",
            "integer",
            "float",
            "bool"
          ]
        },
        {
          "description": "Integers and floats seen under one name. Still summable — an emitter\nwriting `100` and `100.5` for one quantity is ordinary, and calling that\n`mixed` would warn against the one case where the warning is wrong.",
          "type": "string",
          "const": "number"
        },
        {
          "description": "Genuinely disagreeing kinds under one name (a string and a number, say).\nNot an error — emitters do this — but a caller must not assume it can be\nsummed.",
          "type": "string",
          "const": "mixed"
        },
        {
          "description": "Null, array or object: present, but neither a dimension nor a number.",
          "type": "string",
          "const": "other"
        }
      ]
    },
    "FieldSource": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FieldSource",
      "description": "Where a field's value lives on a record — and therefore which filter\nselector reaches it.\n\n**A name alone does not identify a field.** GELF strips the `_` prefix, so a\npayload sending both `file` and `_file` produces a top-level `LogEntry.file`\nAND an `additional_fields[\"file\"]`. Those are two different values reached by\ntwo different selectors (`fi` versus `file`), and merging them into one row\ndouble-counts coverage and hides the built-in's true (often zero) presence —\nwhich is the one fact this whole read exists to surface.",
      "oneOf": [
        {
          "description": "A dedicated slot on the record: level, message, host, facility, file,\nline. Reached by a short DSL selector (`fi`, `ln`, …).",
          "type": "string",
          "const": "builtin"
        },
        {
          "description": "An underscore-prefixed GELF extra, stripped of its prefix. Reached by its\nown name.",
          "type": "string",
          "const": "additional"
        },
        {
          "description": "Lifted out of `additional_fields` by the parser (`trace_id`, `span_id`).\nReal, but **no log filter selector reaches it** — see `selector`.",
          "type": "string",
          "const": "promoted"
        }
      ]
    },
    "LogsProfile": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsProfile",
      "description": "`logs.profile` — how records distribute along one named axis.\n\nThe sibling of `logs.fields`: that one says which dimensions exist, this one\nsays how the population spreads along the one you pick. The axis vocabulary\nmirrors `traces.profile` — a closed enum names the KIND of axis, an open\n`group_keys` list names emitter fields — because that is the only spelling\nthat can reach every row `logs.fields` reports. `trace_id` and `span_id`\ncarry `selector: null`, so a filter-selector vocabulary could not name them.",
      "type": "object",
      "properties": {
        "filter": {
          "description": "DSL filter describing the population to profile. Defaults to everything.\n\nCursor qualifiers (`c>=`) are **rejected**: a cursor advances on read, so\na second identical call would profile a different population.",
          "type": [
            "string",
            "null"
          ]
        },
        "group_by": {
          "description": "The axis. `field` takes its names from `group_keys`; everything else is\na built-in or promoted field. `none`, `\"\"` or omitted returns the\nungrouped figures.",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "none",
            "",
            "level",
            "message",
            "host",
            "facility",
            "file",
            "line",
            "trace_id",
            "span_id",
            "field",
            null
          ]
        },
        "group_keys": {
          "description": "Emitter field names, at most 8. **Required non-empty when `group_by` is\n`field`, and an error, non-empty, on any other axis** — a caller who\npasses field names believes they are grouping by fields, so answering a\ndifferent question silently is worse than one refused round trip.\n\nAn explicit `[]` is NOT \"present\": it means the same as absent here.\nElsewhere the two differ (`collectors.edit` treats `[]` as a structural\nclear), but this method persists nothing for an empty list to clear, and\na generated client that always serialises `[]` would otherwise have its\nmost ordinary call refused.\n\nMore than one name forms a tuple, joined with ` / `.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "top_n": {
          "description": "**Real** rows returned (default 20). The reserved buckets are additional\n— see `LogsProfileResult::groups`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "LogsProfileResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsProfileResult",
      "type": "object",
      "properties": {
        "groups": {
          "description": "At most `top_n` **real** rows, plus up to two reserved rows\n(`__absent__`, `__overflow__`), which sort last and do not consume the\nbudget — so asking for 20 rows buys 20 real values. `groups.len()` may\ntherefore be as large as `top_n + 2`.\n\n**The rows sum to `matched` only when the real-key count is at most\n`top_n`.** `groups_total` is what makes the shortfall visible.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogGroup"
          }
        },
        "grouped_by": {
          "description": "The axis actually used. `null` when none was asked for.",
          "type": [
            "string",
            "null"
          ]
        },
        "group_keys": {
          "description": "Echoed so a reply is self-describing. `null` when none were given, or\nwhen an empty array normalised to none.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "groups_total": {
          "description": "Distinct keys **before** truncation, `__absent__` and `__overflow__`\nincluded — the same convention as `ProfileResult::groups_total`, not\n`CollectorsDiffResult`'s.\n\n`null`, never `0`, when no grouping was performed: `0` would read as\n\"this query touched nothing\" rather than \"we did not look\".",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "cardinality_capped": {
          "description": "True when a cardinality cap folded keys into `__overflow__`, so at least\none row aggregates an arbitrary, arrival-ordered member set.",
          "type": "boolean",
          "default": false
        },
        "levels": {
          "description": "Across the whole matched set, not just the returned rows.",
          "$ref": "#/$defs/LevelCounts"
        },
        "matched": {
          "description": "Records that passed the filter.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "scanned": {
          "description": "Records EXAMINED — the whole ring, never a `count`-limited prefix.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "buffer_total": {
          "description": "Total records currently in the queried buffer.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "first_seq": {
          "description": "Bounds of the matched set. `null` when nothing matched.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "last_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "first_time": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "last_time": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "buffer_oldest_seq": {
          "description": "The ring's real bounds — the only way a caller sees a wrapped buffer\nwhen the filter carries no lower bound, in which case `truncated` is\nstructurally false however much rolled off.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "buffer_newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "lost_below": {
          "description": "The lowest seq this buffer can still speak for; `0` means nothing has\never been evicted.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 0
        },
        "truncated": {
          "type": "boolean",
          "default": false
        },
        "evicted_before_window": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "suppressed": {
          "description": "Where an axis absent from every matched record is announced, and where a\nstructured value that cannot be a dimension is accounted for.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Suppressed"
          }
        }
      },
      "required": [
        "levels",
        "matched",
        "scanned",
        "buffer_total"
      ],
      "$defs": {
        "LogGroup": {
          "description": "One row of a `logs.profile` breakdown.\n\n**Each end of the row is one whole record** — a seq, its timestamp, and the\nmessage that was there. That is why `first_time` is the timestamp OF the\nrecord at `first_seq` rather than `min(timestamp)`: `timestamp` is\nemitter-supplied while `seq` is assigned at receipt, so reordering, several\nsenders or clock skew make those different records, and a row that mixed\nthem would describe two records at once.",
          "type": "object",
          "properties": {
            "key": {
              "description": "The value; `__absent__` or `__overflow__` for the reserved buckets;\ntuple members joined with ` / `.\n\n**Not an identity.** Two rows can share a key: an emitter field valued\n`__absent__` renders like the reserved bucket, and a tuple member\ncontaining ` / ` can collide with a different split of the same joined\ntext. Use `reserved` to identify a bucket, and `first_seq` — unique per\nrow, since each record lands in exactly one group — to tell two rows\napart.",
              "type": "string"
            },
            "reserved": {
              "description": "Set only on a reserved bucket. Absent on ordinary rows, including a\npartially-absent tuple like `store::net / __absent__`, which describes\nreal records that carry one member and lack the other.",
              "anyOf": [
                {
                  "$ref": "#/$defs/ReservedBucket"
                },
                {
                  "type": "null"
                }
              ]
            },
            "count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "levels": {
              "$ref": "#/$defs/LevelCounts"
            },
            "first_seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "first_time": {
              "type": "string",
              "format": "date-time"
            },
            "first_exemplar": {
              "description": "The message at `first_seq`, **verbatim** — never synthesised. For a\nrecurring failure the first occurrence usually carries the root cause\nand the later ones are cascade or retry.",
              "type": "string"
            },
            "last_seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "last_time": {
              "type": "string",
              "format": "date-time"
            },
            "last_exemplar": {
              "description": "The message at `last_seq`, verbatim. Read beside `first_exemplar` it\nanswers a question neither alone can: did this group change shape over\nthe window?",
              "type": "string"
            }
          },
          "required": [
            "key",
            "count",
            "levels",
            "first_seq",
            "first_time",
            "first_exemplar",
            "last_seq",
            "last_time",
            "last_exemplar"
          ]
        },
        "ReservedBucket": {
          "description": "Which reserved bucket a `LogGroup` is, if any.\n\n**Structural, never derived from `key`.** A key is emitter text, and GELF\nvalidates nothing after the `_`, so a field valued literally `__absent__`\nrenders character-for-character like the reserved bucket. The projection\nkeeps them apart with a typed key; a consumer that matches on the rendered\nlabel throws that distinction away — and the renderer did exactly that,\npicking the wrong row and inverting its own header's absent share.",
          "oneOf": [
            {
              "description": "The records here lacked the axis entirely.",
              "type": "string",
              "const": "absent"
            },
            {
              "description": "A cardinality cap folded these keys together. A different fact from\n`Absent`, and merging the two makes the denominator lie.",
              "type": "string",
              "const": "overflow"
            }
          ]
        },
        "LevelCounts": {
          "description": "Records per level. A fixed struct, never a map.\n\n`Level` has five variants and no more (`gelf/message.rs`), so every field is\nalways emitted: a reader never has to tell \"key absent\" from \"count zero\",\nwhich carries no information here and is a reliable source of downstream\n`unwrap_or(0)`. It also lets the renderer lay out fixed columns in severity\norder, and lets the schema state the whole domain instead of\n`additionalProperties`.",
          "type": "object",
          "properties": {
            "trace": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "debug": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "info": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "warn": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "error": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "trace",
            "debug",
            "info",
            "warn",
            "error"
          ]
        },
        "Suppressed": {
          "description": "Why a field that normally carries a number is `null`.\n\nA profile has several ways to be legitimately unable to answer — the\nretention level is too low, a warm-up cut makes an unwindowed tier\nmeaningless, nothing nested so self time carries no information. Returning\n`null` alone would leave the reader to guess which, and the guesses differ\nin what they imply about the run. Every suppression names its field and its\nreason so a consumer can distinguish \"not measured\" from \"measured as zero\".",
          "type": "object",
          "properties": {
            "field": {
              "description": "Dotted path of the suppressed field, e.g. `sampled.self_ms`.",
              "type": "string"
            },
            "reason": {
              "type": "string"
            },
            "remedy": {
              "description": "What the caller could change to get a number instead. Absent when\nnothing would help.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "field",
            "reason"
          ]
        }
      }
    },
    "LogGroup": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogGroup",
      "description": "One row of a `logs.profile` breakdown.\n\n**Each end of the row is one whole record** — a seq, its timestamp, and the\nmessage that was there. That is why `first_time` is the timestamp OF the\nrecord at `first_seq` rather than `min(timestamp)`: `timestamp` is\nemitter-supplied while `seq` is assigned at receipt, so reordering, several\nsenders or clock skew make those different records, and a row that mixed\nthem would describe two records at once.",
      "type": "object",
      "properties": {
        "key": {
          "description": "The value; `__absent__` or `__overflow__` for the reserved buckets;\ntuple members joined with ` / `.\n\n**Not an identity.** Two rows can share a key: an emitter field valued\n`__absent__` renders like the reserved bucket, and a tuple member\ncontaining ` / ` can collide with a different split of the same joined\ntext. Use `reserved` to identify a bucket, and `first_seq` — unique per\nrow, since each record lands in exactly one group — to tell two rows\napart.",
          "type": "string"
        },
        "reserved": {
          "description": "Set only on a reserved bucket. Absent on ordinary rows, including a\npartially-absent tuple like `store::net / __absent__`, which describes\nreal records that carry one member and lack the other.",
          "anyOf": [
            {
              "$ref": "#/$defs/ReservedBucket"
            },
            {
              "type": "null"
            }
          ]
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "levels": {
          "$ref": "#/$defs/LevelCounts"
        },
        "first_seq": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "first_time": {
          "type": "string",
          "format": "date-time"
        },
        "first_exemplar": {
          "description": "The message at `first_seq`, **verbatim** — never synthesised. For a\nrecurring failure the first occurrence usually carries the root cause\nand the later ones are cascade or retry.",
          "type": "string"
        },
        "last_seq": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "last_time": {
          "type": "string",
          "format": "date-time"
        },
        "last_exemplar": {
          "description": "The message at `last_seq`, verbatim. Read beside `first_exemplar` it\nanswers a question neither alone can: did this group change shape over\nthe window?",
          "type": "string"
        }
      },
      "required": [
        "key",
        "count",
        "levels",
        "first_seq",
        "first_time",
        "first_exemplar",
        "last_seq",
        "last_time",
        "last_exemplar"
      ],
      "$defs": {
        "ReservedBucket": {
          "description": "Which reserved bucket a `LogGroup` is, if any.\n\n**Structural, never derived from `key`.** A key is emitter text, and GELF\nvalidates nothing after the `_`, so a field valued literally `__absent__`\nrenders character-for-character like the reserved bucket. The projection\nkeeps them apart with a typed key; a consumer that matches on the rendered\nlabel throws that distinction away — and the renderer did exactly that,\npicking the wrong row and inverting its own header's absent share.",
          "oneOf": [
            {
              "description": "The records here lacked the axis entirely.",
              "type": "string",
              "const": "absent"
            },
            {
              "description": "A cardinality cap folded these keys together. A different fact from\n`Absent`, and merging the two makes the denominator lie.",
              "type": "string",
              "const": "overflow"
            }
          ]
        },
        "LevelCounts": {
          "description": "Records per level. A fixed struct, never a map.\n\n`Level` has five variants and no more (`gelf/message.rs`), so every field is\nalways emitted: a reader never has to tell \"key absent\" from \"count zero\",\nwhich carries no information here and is a reliable source of downstream\n`unwrap_or(0)`. It also lets the renderer lay out fixed columns in severity\norder, and lets the schema state the whole domain instead of\n`additionalProperties`.",
          "type": "object",
          "properties": {
            "trace": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "debug": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "info": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "warn": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "error": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "trace",
            "debug",
            "info",
            "warn",
            "error"
          ]
        }
      }
    },
    "LevelCounts": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LevelCounts",
      "description": "Records per level. A fixed struct, never a map.\n\n`Level` has five variants and no more (`gelf/message.rs`), so every field is\nalways emitted: a reader never has to tell \"key absent\" from \"count zero\",\nwhich carries no information here and is a reliable source of downstream\n`unwrap_or(0)`. It also lets the renderer lay out fixed columns in severity\norder, and lets the schema state the whole domain instead of\n`additionalProperties`.",
      "type": "object",
      "properties": {
        "trace": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "debug": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "info": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "warn": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "error": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "trace",
        "debug",
        "info",
        "warn",
        "error"
      ]
    },
    "ReservedBucket": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ReservedBucket",
      "description": "Which reserved bucket a `LogGroup` is, if any.\n\n**Structural, never derived from `key`.** A key is emitter text, and GELF\nvalidates nothing after the `_`, so a field valued literally `__absent__`\nrenders character-for-character like the reserved bucket. The projection\nkeeps them apart with a typed key; a consumer that matches on the rendered\nlabel throws that distinction away — and the renderer did exactly that,\npicking the wrong row and inverting its own header's absent share.",
      "oneOf": [
        {
          "description": "The records here lacked the axis entirely.",
          "type": "string",
          "const": "absent"
        },
        {
          "description": "A cardinality cap folded these keys together. A different fact from\n`Absent`, and merging the two makes the denominator lie.",
          "type": "string",
          "const": "overflow"
        }
      ]
    },
    "LogsContext": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsContext",
      "type": "object",
      "properties": {
        "seq": {
          "description": "Sequence number of the anchor entry",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "before": {
          "description": "Number of entries before the anchor (default: 10)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "after": {
          "description": "Number of entries after the anchor (default: 10)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "seq"
      ]
    },
    "LogsContextResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsContextResult",
      "type": "object",
      "properties": {
        "logs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogEntry"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "logs",
        "count"
      ],
      "$defs": {
        "LogEntry": {
          "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "level": {
              "$ref": "#/$defs/Level"
            },
            "message": {
              "type": "string"
            },
            "full_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "host": {
              "type": "string"
            },
            "facility": {
              "type": [
                "string",
                "null"
              ]
            },
            "file": {
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "additional_fields": {
              "type": "object",
              "additionalProperties": true
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "matched_filters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "source": {
              "$ref": "#/$defs/LogSource",
              "default": "Filter"
            }
          },
          "required": [
            "seq",
            "timestamp",
            "level",
            "message",
            "host"
          ]
        },
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        }
      }
    },
    "LogsExport": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsExport",
      "type": "object",
      "properties": {
        "count": {
          "description": "Maximum number of entries to export",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "filter": {
          "description": "Optional DSL filter expression",
          "type": [
            "string",
            "null"
          ]
        },
        "from_seq": {
          "description": "Lowest seq to export, **inclusive**. Composes with `filter`: a range and\na bookmark bound together resolve to the tighter of the two.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "to_seq": {
          "description": "Highest seq to export, **inclusive**.\n\nInclusive on both ends because the alternative has an off-by-one at the\ncentre of every window: a range built as \"the anchor, plus 0 before and\n0 after\" must contain the anchor, and a strict bound would exclude the\none entry the caller was pointing at.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "LogsExportResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsExportResult",
      "type": "object",
      "properties": {
        "logs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogEntry"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "format": {
          "type": "string"
        },
        "scanned": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "buffer_total": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "buffer_oldest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "buffer_newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "truncated": {
          "type": "boolean",
          "default": false
        },
        "evicted_before_window": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "cursor_advanced_to": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "capped": {
          "description": "`count` stopped this short of everything that matched the range.\n\nStated rather than left to be inferred, because it **cannot** be\ninferred: comparing what was asked for against what came back cannot\ntell a capped range apart from one where exactly that many existed. A\nreader deciding whether a window is complete needs that difference, and\n`truncated` will not supply it — that reports *eviction*, which is a\ndifferent kind of missing.",
          "type": "boolean",
          "default": false
        },
        "verdict": {
          "description": "How much of this window the daemon can vouch for.\n\nDefaults to `cannot_verify`, which is the whole point of the default: a\nreply from a broker too old to send the field must not be read as a\nclean bill of health.",
          "$ref": "#/$defs/EvidenceVerdict",
          "default": "cannot_verify"
        },
        "narrowed_by": {
          "description": "The stretches of the window over which a session filter was narrowing\nwhat the daemon stored, and the filters doing it. Empty unless `verdict`\nis `filtered`.\n\nAlways emitted, including empty — a reader that sees `filtered` comes\nhere next, and \"the field is absent\" must not be one more state it has\nto tell apart from \"nothing narrowed anything\".",
          "type": "array",
          "items": {
            "$ref": "#/$defs/NarrowedRange"
          },
          "default": []
        }
      },
      "required": [
        "logs",
        "count",
        "format"
      ],
      "$defs": {
        "LogEntry": {
          "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "level": {
              "$ref": "#/$defs/Level"
            },
            "message": {
              "type": "string"
            },
            "full_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "host": {
              "type": "string"
            },
            "facility": {
              "type": [
                "string",
                "null"
              ]
            },
            "file": {
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "additional_fields": {
              "type": "object",
              "additionalProperties": true
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "matched_filters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "source": {
              "$ref": "#/$defs/LogSource",
              "default": "Filter"
            }
          },
          "required": [
            "seq",
            "timestamp",
            "level",
            "message",
            "host"
          ]
        },
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        },
        "EvidenceVerdict": {
          "description": "How much of an exported window the daemon can vouch for.\n\nThe reason this exists: storage is conditional, so \"nothing appeared before\nthe error\" is ambiguous between *absence of cause* and *absence of\nrecording*. Only a record of the storage policy over time can separate them,\nand without one a window shaped by another session's filter reads as\ncomplete.",
          "oneOf": [
            {
              "description": "The window lies wholly inside one unfiltered epoch, nothing was evicted,\nand nothing was capped. Everything that reached the daemon over these\nseqs **was stored**.\n\nIt does *not* say everything stored is in this reply: your own `filter`\nstill selects, and `count` still limits. The verdict is about the\ndaemon's storage policy over the range — which is the only thing a later\nread cannot recover — not about what this particular call returned.",
              "type": "string",
              "const": "complete"
            },
            {
              "description": "The ring dropped part of the window.\n\nOn an export, `evicted_before_window` bounds how much — the window's\nlower end and the store's floor are independent numbers there, so their\ndifference means something. On a **capture** there is no such bound and\nnone is reported: the window opens at the first record that SURVIVED, so\nthe two coincide, and the document states the floor and the caller's\nshortfall as the separate facts they are rather than passing one off as\na count of the other.",
              "type": "string",
              "const": "evicted"
            },
            {
              "description": "A session filter was narrowing the store over part of the window. See\n`narrowed_by` for which filters, and over which seqs.",
              "type": "string",
              "const": "filtered"
            },
            {
              "description": "No claim can be made: the store is empty, or the window predates what\nthe daemon still records about its own storage policy (including a\nwindow carried over from a previous daemon run), or the read was capped\nshort of what matched.\n\nThe default, deliberately — see [`LogsExportResult::verdict`].",
              "type": "string",
              "const": "cannot_verify"
            }
          ]
        },
        "NarrowedRange": {
          "description": "One stretch of an exported window over which a session filter was narrowing\nwhat the daemon stored.",
          "type": "object",
          "properties": {
            "from_seq": {
              "description": "Clamped to the window that was asked for, so these are the caller's own\nseqs rather than wherever the epoch happened to begin.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "to_seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "filters": {
              "description": "The filter strings in force over this stretch — the union across every\nsession bound to the domain, since any one of them narrowing the store\nnarrows it for everybody.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "from_seq",
            "to_seq",
            "filters"
          ]
        }
      }
    },
    "EvidenceVerdict": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "EvidenceVerdict",
      "description": "How much of an exported window the daemon can vouch for.\n\nThe reason this exists: storage is conditional, so \"nothing appeared before\nthe error\" is ambiguous between *absence of cause* and *absence of\nrecording*. Only a record of the storage policy over time can separate them,\nand without one a window shaped by another session's filter reads as\ncomplete.",
      "oneOf": [
        {
          "description": "The window lies wholly inside one unfiltered epoch, nothing was evicted,\nand nothing was capped. Everything that reached the daemon over these\nseqs **was stored**.\n\nIt does *not* say everything stored is in this reply: your own `filter`\nstill selects, and `count` still limits. The verdict is about the\ndaemon's storage policy over the range — which is the only thing a later\nread cannot recover — not about what this particular call returned.",
          "type": "string",
          "const": "complete"
        },
        {
          "description": "The ring dropped part of the window.\n\nOn an export, `evicted_before_window` bounds how much — the window's\nlower end and the store's floor are independent numbers there, so their\ndifference means something. On a **capture** there is no such bound and\nnone is reported: the window opens at the first record that SURVIVED, so\nthe two coincide, and the document states the floor and the caller's\nshortfall as the separate facts they are rather than passing one off as\na count of the other.",
          "type": "string",
          "const": "evicted"
        },
        {
          "description": "A session filter was narrowing the store over part of the window. See\n`narrowed_by` for which filters, and over which seqs.",
          "type": "string",
          "const": "filtered"
        },
        {
          "description": "No claim can be made: the store is empty, or the window predates what\nthe daemon still records about its own storage policy (including a\nwindow carried over from a previous daemon run), or the read was capped\nshort of what matched.\n\nThe default, deliberately — see [`LogsExportResult::verdict`].",
          "type": "string",
          "const": "cannot_verify"
        }
      ]
    },
    "NarrowedRange": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "NarrowedRange",
      "description": "One stretch of an exported window over which a session filter was narrowing\nwhat the daemon stored.",
      "type": "object",
      "properties": {
        "from_seq": {
          "description": "Clamped to the window that was asked for, so these are the caller's own\nseqs rather than wherever the epoch happened to begin.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "to_seq": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "filters": {
          "description": "The filter strings in force over this stretch — the union across every\nsession bound to the domain, since any one of them narrowing the store\nnarrows it for everybody.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "from_seq",
        "to_seq",
        "filters"
      ]
    },
    "LogsClear": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsClear",
      "type": "object",
      "additionalProperties": false
    },
    "LogsClearResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogsClearResult",
      "type": "object",
      "properties": {
        "cleared": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "cleared"
      ]
    },
    "FiltersList": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersList",
      "type": "object",
      "additionalProperties": false
    },
    "FiltersListResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersListResult",
      "type": "object",
      "properties": {
        "filters": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FilterInfo"
          }
        }
      },
      "required": [
        "filters"
      ],
      "$defs": {
        "FilterInfo": {
          "description": "One entry in a `filters.list` response.",
          "type": "object",
          "properties": {
            "id": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "filter": {
              "type": "string"
            },
            "description": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "id",
            "filter"
          ]
        }
      }
    },
    "FiltersAdd": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersAdd",
      "type": "object",
      "properties": {
        "filter": {
          "description": "DSL filter expression",
          "type": "string"
        },
        "description": {
          "description": "Human-readable description",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "filter"
      ]
    },
    "FiltersAddResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersAddResult",
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "id"
      ]
    },
    "FiltersEdit": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersEdit",
      "type": "object",
      "properties": {
        "id": {
          "description": "Filter ID to edit",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "filter": {
          "description": "New DSL filter expression",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "New description",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "id"
      ]
    },
    "FiltersEditResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersEditResult",
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "filter": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "filter"
      ]
    },
    "FiltersRemove": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersRemove",
      "type": "object",
      "properties": {
        "id": {
          "description": "Filter ID to remove",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "id"
      ]
    },
    "FiltersRemoveResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FiltersRemoveResult",
      "type": "object",
      "properties": {
        "removed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "removed"
      ]
    },
    "TriggersList": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersList",
      "type": "object",
      "additionalProperties": false
    },
    "TriggersListResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersListResult",
      "type": "object",
      "properties": {
        "triggers": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TriggerInfo"
          }
        }
      },
      "required": [
        "triggers"
      ],
      "$defs": {
        "TriggerInfo": {
          "description": "One entry in a `triggers.list` response.",
          "type": "object",
          "properties": {
            "id": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "filter": {
              "type": "string"
            },
            "pre_window": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "post_window": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "notify_context": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "match_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "oneshot": {
              "description": "When `true`, the trigger auto-removes after its first match. Always\nserialized (no `skip_serializing_if`) so clients see the explicit value.",
              "type": "boolean",
              "default": false
            },
            "post_remaining": {
              "description": "Entries still to pass before this trigger can fire again — its own\ndebounce window; `0` means armed and live. A trigger that looks stuck\nwith a non-zero value here is debounced, not broken.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0,
              "default": 0
            }
          },
          "required": [
            "id",
            "filter",
            "pre_window",
            "post_window",
            "notify_context",
            "match_count"
          ]
        }
      }
    },
    "TriggersAdd": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersAdd",
      "type": "object",
      "properties": {
        "filter": {
          "description": "DSL filter expression that activates the trigger",
          "type": "string"
        },
        "pre_window": {
          "description": "Number of messages to capture before the triggering event (default: 500)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "post_window": {
          "description": "Number of messages to capture after the triggering event (default: 200)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "notify_context": {
          "description": "Number of context entries to include in the notification (default: 5)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "description": {
          "description": "Human-readable description",
          "type": [
            "string",
            "null"
          ]
        },
        "oneshot": {
          "description": "When `true`, the trigger auto-removes after its first match. Defaults\nto `false`; old clients without this field continue to deserialize.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "filter"
      ]
    },
    "TriggersAddResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersAddResult",
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "id"
      ]
    },
    "TriggersEdit": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersEdit",
      "type": "object",
      "properties": {
        "id": {
          "description": "Trigger ID to edit",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "filter": {
          "description": "New DSL filter expression",
          "type": [
            "string",
            "null"
          ]
        },
        "pre_window": {
          "description": "New pre-window size",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "post_window": {
          "description": "New post-window size",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "notify_context": {
          "description": "New notify-context size",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "description": {
          "description": "New description",
          "type": [
            "string",
            "null"
          ]
        },
        "oneshot": {
          "description": "Set or clear fire-once. Absent leaves it as it is.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "id"
      ]
    },
    "TriggersEditResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersEditResult",
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "filter": {
          "type": "string"
        },
        "pre_window": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "post_window": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "notify_context": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "match_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "oneshot": {
          "type": "boolean",
          "default": false
        },
        "post_remaining": {
          "description": "Mirrors `TriggerInfo::post_remaining`. This struct is deliberately\nseparate from `TriggerInfo`, so a field added there does NOT appear\nhere — the daemon emitted `post_remaining` on the edit response while\nthis type lacked it, and serde silently dropped it for every typed SDK\ncaller. `verify-schema` cannot catch that: it checks schema-vs-Rust,\nnot daemon-JSON-vs-Rust.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 0
        }
      },
      "required": [
        "id",
        "filter",
        "pre_window",
        "post_window",
        "notify_context",
        "match_count"
      ]
    },
    "TriggersRemove": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersRemove",
      "type": "object",
      "properties": {
        "id": {
          "description": "Trigger ID to remove",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "id"
      ]
    },
    "TriggersRemoveResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggersRemoveResult",
      "type": "object",
      "properties": {
        "removed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "removed"
      ]
    },
    "TracesRecent": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesRecent",
      "type": "object",
      "properties": {
        "count": {
          "description": "Max traces to return (default: 20)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "filter": {
          "description": "Span filter DSL expression",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "TracesRecentResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesRecentResult",
      "type": "object",
      "properties": {
        "traces": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TraceSummary"
          }
        },
        "count": {
          "description": "Number of traces MATCHED and returned (capped by `count`).",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "scanned": {
          "description": "Spans examined to build these traces. `recent_traces` walks the whole\nspan buffer, so for traces this equals `buffer_total` (B2): `count==0,\nscanned>0` means \"filter's fault\", `scanned==0` means empty span buffer.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "buffer_total": {
          "description": "Total spans currently in the span buffer.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "buffer_oldest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "buffer_newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "traces",
        "count"
      ],
      "$defs": {
        "TraceSummary": {
          "description": "A high-level summary of a single trace. Mirrors `core::span::types::TraceSummary`.",
          "type": "object",
          "properties": {
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": "string"
            },
            "root_span_name": {
              "type": "string"
            },
            "service_name": {
              "type": "string"
            },
            "start_time": {
              "type": "string",
              "format": "date-time"
            },
            "total_duration_ms": {
              "type": "number",
              "format": "double"
            },
            "span_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "has_errors": {
              "type": "boolean"
            },
            "linked_log_count": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          },
          "required": [
            "trace_id",
            "root_span_name",
            "service_name",
            "start_time",
            "total_duration_ms",
            "span_count",
            "has_errors",
            "linked_log_count"
          ]
        }
      }
    },
    "TracesGet": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesGet",
      "type": "object",
      "properties": {
        "trace_id": {
          "description": "32-character hex trace ID",
          "type": "string"
        },
        "include_logs": {
          "description": "Include linked logs (default: true)",
          "type": [
            "boolean",
            "null"
          ]
        },
        "filter": {
          "description": "Filter spans within the trace",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "trace_id"
      ]
    },
    "TracesGetResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesGetResult",
      "type": "object",
      "properties": {
        "trace_id": {
          "type": "string"
        },
        "spans": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SpanEntry"
          }
        },
        "logs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogEntry"
          }
        },
        "span_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "log_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "trace_id",
        "spans",
        "logs",
        "span_count",
        "log_count"
      ],
      "$defs": {
        "SpanEntry": {
          "description": "A single span. Mirrors `core::span::types::SpanEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": "string"
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": "string"
            },
            "parent_span_id": {
              "description": "Parent span id rendered as 16-char lowercase hex (none for root spans).",
              "type": [
                "string",
                "null"
              ]
            },
            "start_time": {
              "type": "string",
              "format": "date-time"
            },
            "end_time": {
              "type": "string",
              "format": "date-time"
            },
            "duration_ms": {
              "type": "number",
              "format": "double"
            },
            "name": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/SpanKind"
            },
            "service_name": {
              "type": "string"
            },
            "status": {
              "$ref": "#/$defs/SpanStatus"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            },
            "events": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpanEvent"
              },
              "default": []
            }
          },
          "required": [
            "seq",
            "trace_id",
            "span_id",
            "start_time",
            "end_time",
            "duration_ms",
            "name",
            "kind",
            "service_name",
            "status"
          ]
        },
        "SpanKind": {
          "description": "Span kind. Wire format is snake_case, matching `core::span::types::SpanKind`.",
          "type": "string",
          "enum": [
            "unspecified",
            "internal",
            "server",
            "client",
            "producer",
            "consumer"
          ]
        },
        "SpanStatus": {
          "description": "Span status. Wire format mirrors `core::span::types::SpanStatus`:\n`{\"type\":\"unset\"}`, `{\"type\":\"ok\"}`, `{\"type\":\"error\",\"message\":\"...\"}`.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "unset"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "ok"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "error"
                },
                "message": {
                  "type": "string"
                }
              },
              "required": [
                "type",
                "message"
              ]
            }
          ]
        },
        "SpanEvent": {
          "description": "A timestamped event attached to a span. Mirrors `core::span::types::SpanEvent`.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            }
          },
          "required": [
            "name",
            "timestamp"
          ]
        },
        "LogEntry": {
          "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "level": {
              "$ref": "#/$defs/Level"
            },
            "message": {
              "type": "string"
            },
            "full_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "host": {
              "type": "string"
            },
            "facility": {
              "type": [
                "string",
                "null"
              ]
            },
            "file": {
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "additional_fields": {
              "type": "object",
              "additionalProperties": true
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "matched_filters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "source": {
              "$ref": "#/$defs/LogSource",
              "default": "Filter"
            }
          },
          "required": [
            "seq",
            "timestamp",
            "level",
            "message",
            "host"
          ]
        },
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        }
      }
    },
    "TracesSummary": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesSummary",
      "type": "object",
      "properties": {
        "trace_id": {
          "description": "32-character hex trace ID",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "trace_id"
      ]
    },
    "TracesSummaryResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesSummaryResult",
      "type": "object",
      "properties": {
        "root_span": {
          "type": "string"
        },
        "total_duration_ms": {
          "type": "number",
          "format": "double"
        },
        "breakdown": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/TraceSummaryBreakdownEntry"
          }
        },
        "other_ms": {
          "type": "number",
          "format": "double"
        },
        "span_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "root_span",
        "total_duration_ms",
        "breakdown",
        "other_ms",
        "span_count"
      ],
      "$defs": {
        "TraceSummaryBreakdownEntry": {
          "description": "One row in `TracesSummaryResult.breakdown`: the contribution of one direct\nchild of the root span to the trace's wall-clock time.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "self_time_ms": {
              "type": "number",
              "format": "double"
            },
            "total_time_ms": {
              "type": "number",
              "format": "double"
            },
            "percentage": {
              "type": "number",
              "format": "double"
            },
            "is_error": {
              "type": "boolean"
            }
          },
          "required": [
            "name",
            "self_time_ms",
            "total_time_ms",
            "percentage",
            "is_error"
          ]
        }
      }
    },
    "TracesSlow": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesSlow",
      "type": "object",
      "properties": {
        "min_duration_ms": {
          "description": "Duration threshold in milliseconds (default: 100)",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "count": {
          "description": "Max results (default: 20)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "filter": {
          "description": "Additional span filter",
          "type": [
            "string",
            "null"
          ]
        },
        "group_by": {
          "description": "`\"name\"` aggregates results into `groups`; `\"none\"` (or `\"\"`, or\nomitting this) returns individual spans in `spans`.\n\n**This was the one `group_by` that accepted any string**, with anything\nother than `\"name\"` quietly selecting the ungrouped arm — so a typo\nreturned a different answer than the one asked for, silently. The\nargument for that was that a closed set would reject every valid call\nmeaning \"do not group\", which held only while no spelling for it\nexisted. `none` is that spelling, so the set closes (#7).",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "name",
            "none",
            "",
            null
          ]
        }
      },
      "additionalProperties": false
    },
    "TracesSlowResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesSlowResult",
      "description": "`traces.slow` returns either an ungrouped list of spans or a grouped\naggregation, depending on the `group_by` parameter. Both shapes are\nexpressed as optional fields on the same result struct.",
      "type": "object",
      "properties": {
        "spans": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/SpanEntry"
          }
        },
        "count": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "grouped_by": {
          "type": [
            "string",
            "null"
          ]
        },
        "groups": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/TracesSlowGroup"
          }
        },
        "population": {
          "description": "Grouped arm only: how many spans the aggregates were computed over.\nStated because it is not `count` and not the number of rows — it is the\nwhole matching population, which is the change that makes the grouped\nnumbers trustworthy.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "display_floor_ms": {
          "description": "Grouped arm only: the `min_duration_ms` that decided which names appear.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      },
      "$defs": {
        "SpanEntry": {
          "description": "A single span. Mirrors `core::span::types::SpanEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": "string"
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": "string"
            },
            "parent_span_id": {
              "description": "Parent span id rendered as 16-char lowercase hex (none for root spans).",
              "type": [
                "string",
                "null"
              ]
            },
            "start_time": {
              "type": "string",
              "format": "date-time"
            },
            "end_time": {
              "type": "string",
              "format": "date-time"
            },
            "duration_ms": {
              "type": "number",
              "format": "double"
            },
            "name": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/SpanKind"
            },
            "service_name": {
              "type": "string"
            },
            "status": {
              "$ref": "#/$defs/SpanStatus"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            },
            "events": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpanEvent"
              },
              "default": []
            }
          },
          "required": [
            "seq",
            "trace_id",
            "span_id",
            "start_time",
            "end_time",
            "duration_ms",
            "name",
            "kind",
            "service_name",
            "status"
          ]
        },
        "SpanKind": {
          "description": "Span kind. Wire format is snake_case, matching `core::span::types::SpanKind`.",
          "type": "string",
          "enum": [
            "unspecified",
            "internal",
            "server",
            "client",
            "producer",
            "consumer"
          ]
        },
        "SpanStatus": {
          "description": "Span status. Wire format mirrors `core::span::types::SpanStatus`:\n`{\"type\":\"unset\"}`, `{\"type\":\"ok\"}`, `{\"type\":\"error\",\"message\":\"...\"}`.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "unset"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "ok"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "error"
                },
                "message": {
                  "type": "string"
                }
              },
              "required": [
                "type",
                "message"
              ]
            }
          ]
        },
        "SpanEvent": {
          "description": "A timestamped event attached to a span. Mirrors `core::span::types::SpanEvent`.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            }
          },
          "required": [
            "name",
            "timestamp"
          ]
        },
        "TracesSlowGroup": {
          "description": "One row in a grouped `TracesSlowResult` (when `group_by == \"name\"`).\n\nThese statistics describe **every** stored span of this name that matched\nthe filter, not only the ones above `min_duration_ms`. The floor decides\nwhich names appear, not which spans are counted — so a name can qualify on\nits `max_ms` while its `avg_ms` sits far below the floor, and that gap is\nthe useful signal.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "avg_ms": {
              "type": "number",
              "format": "double"
            },
            "p50_ms": {
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "p95_ms": {
              "type": "number",
              "format": "double"
            },
            "max_ms": {
              "description": "The slowest span of this name. This is what the display floor tests.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "name",
            "avg_ms",
            "p95_ms",
            "count"
          ]
        }
      }
    },
    "TracesLogs": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesLogs",
      "type": "object",
      "properties": {
        "trace_id": {
          "description": "32-character hex trace ID",
          "type": "string"
        },
        "filter": {
          "description": "Additional log filter DSL",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "trace_id"
      ]
    },
    "TracesLogsResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesLogsResult",
      "type": "object",
      "properties": {
        "logs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogEntry"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "cursor_advanced_to": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "logs",
        "count"
      ],
      "$defs": {
        "LogEntry": {
          "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "level": {
              "$ref": "#/$defs/Level"
            },
            "message": {
              "type": "string"
            },
            "full_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "host": {
              "type": "string"
            },
            "facility": {
              "type": [
                "string",
                "null"
              ]
            },
            "file": {
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "additional_fields": {
              "type": "object",
              "additionalProperties": true
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "matched_filters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "source": {
              "$ref": "#/$defs/LogSource",
              "default": "Filter"
            }
          },
          "required": [
            "seq",
            "timestamp",
            "level",
            "message",
            "host"
          ]
        },
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        }
      }
    },
    "SpansContext": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpansContext",
      "type": "object",
      "properties": {
        "seq": {
          "description": "Span sequence number",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "before": {
          "description": "Spans before (default: 5)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "after": {
          "description": "Spans after (default: 5)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "seq"
      ]
    },
    "SpansContextResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpansContextResult",
      "type": "object",
      "properties": {
        "spans": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SpanEntry"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "spans",
        "count"
      ],
      "$defs": {
        "SpanEntry": {
          "description": "A single span. Mirrors `core::span::types::SpanEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": "string"
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": "string"
            },
            "parent_span_id": {
              "description": "Parent span id rendered as 16-char lowercase hex (none for root spans).",
              "type": [
                "string",
                "null"
              ]
            },
            "start_time": {
              "type": "string",
              "format": "date-time"
            },
            "end_time": {
              "type": "string",
              "format": "date-time"
            },
            "duration_ms": {
              "type": "number",
              "format": "double"
            },
            "name": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/SpanKind"
            },
            "service_name": {
              "type": "string"
            },
            "status": {
              "$ref": "#/$defs/SpanStatus"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            },
            "events": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpanEvent"
              },
              "default": []
            }
          },
          "required": [
            "seq",
            "trace_id",
            "span_id",
            "start_time",
            "end_time",
            "duration_ms",
            "name",
            "kind",
            "service_name",
            "status"
          ]
        },
        "SpanKind": {
          "description": "Span kind. Wire format is snake_case, matching `core::span::types::SpanKind`.",
          "type": "string",
          "enum": [
            "unspecified",
            "internal",
            "server",
            "client",
            "producer",
            "consumer"
          ]
        },
        "SpanStatus": {
          "description": "Span status. Wire format mirrors `core::span::types::SpanStatus`:\n`{\"type\":\"unset\"}`, `{\"type\":\"ok\"}`, `{\"type\":\"error\",\"message\":\"...\"}`.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "unset"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "ok"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "error"
                },
                "message": {
                  "type": "string"
                }
              },
              "required": [
                "type",
                "message"
              ]
            }
          ]
        },
        "SpanEvent": {
          "description": "A timestamped event attached to a span. Mirrors `core::span::types::SpanEvent`.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            }
          },
          "required": [
            "name",
            "timestamp"
          ]
        }
      }
    },
    "SpansExport": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpansExport",
      "description": "Every span whose seq falls in an inclusive range.\n\n**The span store had no seq-ranged read at all.** Its whole surface was\n`get_trace`, `slow_spans`, `for_each_matching`, `duration_by_name`,\n`recent_traces` and `spans.context` — and `spans.context` counts *positions\nin the ring*, not seqs, so it cannot answer \"what spans belong to this\nwindow\". A capture that pairs logs with the spans covering the same interval\nneeds one, which is what this is.",
      "type": "object",
      "properties": {
        "from_seq": {
          "description": "Lowest seq to export, **inclusive**.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "to_seq": {
          "description": "Highest seq to export, **inclusive**. Same convention as `logs.export`:\na window of \"this span, none before and none after\" must contain it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "count": {
          "description": "Maximum spans to return. Everything matching is still counted, so\n`capped` and `matched` stay truthful when this bites.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "filter": {
          "description": "Additional span filter, ANDed with the range.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "SpansExportResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpansExportResult",
      "type": "object",
      "properties": {
        "spans": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SpanEntry"
          }
        },
        "count": {
          "description": "How many are in `spans`.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "matched": {
          "description": "How many matched the range in total. Differs from `count` only when\n`capped`, and it is the figure that says by how much.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "capped": {
          "description": "`count` stopped this short of everything that matched.",
          "type": "boolean",
          "default": false
        },
        "buffer_oldest_seq": {
          "description": "**The span ring's own retention, not the log pipeline's.** The two\nstores share a seq axis but evict independently, so a log window's\nverdict says nothing about whether the spans over that same range\nsurvived. One verdict cannot honestly cover two stores.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "buffer_newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "truncated": {
          "description": "The requested range began before the span ring's oldest retained span.",
          "type": "boolean",
          "default": false
        },
        "evicted_before_window": {
          "description": "How many seqs lie between the requested start and the oldest span still\nheld — an upper bound on what was lost to eviction.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "spans",
        "count"
      ],
      "$defs": {
        "SpanEntry": {
          "description": "A single span. Mirrors `core::span::types::SpanEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": "string"
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": "string"
            },
            "parent_span_id": {
              "description": "Parent span id rendered as 16-char lowercase hex (none for root spans).",
              "type": [
                "string",
                "null"
              ]
            },
            "start_time": {
              "type": "string",
              "format": "date-time"
            },
            "end_time": {
              "type": "string",
              "format": "date-time"
            },
            "duration_ms": {
              "type": "number",
              "format": "double"
            },
            "name": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/$defs/SpanKind"
            },
            "service_name": {
              "type": "string"
            },
            "status": {
              "$ref": "#/$defs/SpanStatus"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            },
            "events": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpanEvent"
              },
              "default": []
            }
          },
          "required": [
            "seq",
            "trace_id",
            "span_id",
            "start_time",
            "end_time",
            "duration_ms",
            "name",
            "kind",
            "service_name",
            "status"
          ]
        },
        "SpanKind": {
          "description": "Span kind. Wire format is snake_case, matching `core::span::types::SpanKind`.",
          "type": "string",
          "enum": [
            "unspecified",
            "internal",
            "server",
            "client",
            "producer",
            "consumer"
          ]
        },
        "SpanStatus": {
          "description": "Span status. Wire format mirrors `core::span::types::SpanStatus`:\n`{\"type\":\"unset\"}`, `{\"type\":\"ok\"}`, `{\"type\":\"error\",\"message\":\"...\"}`.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "unset"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "ok"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "error"
                },
                "message": {
                  "type": "string"
                }
              },
              "required": [
                "type",
                "message"
              ]
            }
          ]
        },
        "SpanEvent": {
          "description": "A timestamped event attached to a span. Mirrors `core::span::types::SpanEvent`.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            }
          },
          "required": [
            "name",
            "timestamp"
          ]
        }
      }
    },
    "CaseAnchor": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CaseAnchor",
      "description": "Where a capture is centred — **tagged**, not one string the daemon sniffs.\n\nSniffing misfires on a bookmark named `12345` or one spelled as 32 hex\ncharacters, and the failure is silent: you get a document anchored somewhere\nelse. The anchor entry's message is the document's headline, so a wrong\nanchor is a wrong document rather than a wrong parameter.\n\nExactly one field must be set.",
      "type": "object",
      "properties": {
        "seq": {
          "description": "A stored entry's sequence number.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "bookmark": {
          "description": "A bookmark name, resolved against the **calling session** exactly as\n`b>=name` resolves it — use `<session>/<name>` for another session's.\nAnchors on the first stored entry **strictly after** the mark, since a\nbookmark marks a boundary rather than a record.",
          "type": [
            "string",
            "null"
          ]
        },
        "trace_id": {
          "description": "A trace id in hex. Anchors on the **earliest by seq** of its entries,\nand the document says how many there were.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "CasesCreate": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CasesCreate",
      "type": "object",
      "properties": {
        "reason": {
          "description": "Why this capture exists. Required: a manual case that cannot say why it\nwas taken has no provenance, and unlike a watch there is no filter\nstanding in for one.",
          "type": "string"
        },
        "anchor": {
          "$ref": "#/$defs/CaseAnchor"
        },
        "dir": {
          "description": "**Absolute** directory the three files are written to. A relative path\nis rejected rather than resolved: the broker runs as a service, so it\nwould resolve against the broker's working directory rather than the\ncaller's.\n\nThe daemon writes, rather than returning the archive over RPC, because a\ncase carries several hundred kilobytes of JSONL and returning it would\nput the whole thing in the model's context — the outcome the\ndocument/logdata split exists to prevent.",
          "type": "string"
        },
        "prefix": {
          "description": "Filename prefix. Falls back to `/case-name` in the registry, then to the\ndomain name.",
          "type": [
            "string",
            "null"
          ]
        },
        "data": {
          "description": "Registry entries, exactly as `domain_data.update` takes them. A leading\n`@` scopes a key to **this document** instead of the domain — how a\ncapturer records something true of this incident rather than of the\ndomain.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/DomainDataEntry"
          }
        },
        "before": {
          "description": "Stored records to capture before the anchor. Counts of **records**, not\nseq distances: one counter feeds both the log and span stores, so a\n200-seq range holds an unpredictable number of logs. Default 350, capped\nat 5000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "after": {
          "description": "Stored records to capture after the anchor. Default 350, capped at 5000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "reason",
        "anchor",
        "dir"
      ],
      "$defs": {
        "CaseAnchor": {
          "description": "Where a capture is centred — **tagged**, not one string the daemon sniffs.\n\nSniffing misfires on a bookmark named `12345` or one spelled as 32 hex\ncharacters, and the failure is silent: you get a document anchored somewhere\nelse. The anchor entry's message is the document's headline, so a wrong\nanchor is a wrong document rather than a wrong parameter.\n\nExactly one field must be set.",
          "type": "object",
          "properties": {
            "seq": {
              "description": "A stored entry's sequence number.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "bookmark": {
              "description": "A bookmark name, resolved against the **calling session** exactly as\n`b>=name` resolves it — use `<session>/<name>` for another session's.\nAnchors on the first stored entry **strictly after** the mark, since a\nbookmark marks a boundary rather than a record.",
              "type": [
                "string",
                "null"
              ]
            },
            "trace_id": {
              "description": "A trace id in hex. Anchors on the **earliest by seq** of its entries,\nand the document says how many there were.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        "DomainDataEntry": {
          "description": "One requested change to the registry.\n\n**Absent ≡ `null` ≡ unchanged, for both `value` and `ttl`**, and the two\nshare one rule deliberately: two optional fields on one object with opposite\nabsent semantics is the footgun this surface exists to avoid, and a client\nserialising a missing field as `null` would otherwise mean the opposite of\nwhat it sent. Erasure is `domain_data.remove`, never `value: null`.",
          "type": "object",
          "properties": {
            "path": {
              "description": "A registry path — `/Build/commit`. A leading `@` scopes the fact to one\ncase document instead of the domain, and is accepted only where a\ndocument exists to receive it.",
              "type": "string"
            },
            "value": {
              "description": "Present → set. Absent → validate only, which never creates.",
              "type": [
                "string",
                "null"
              ]
            },
            "ttl": {
              "description": "A duration literal (`30s`, `5m`, `2h`, `7d`, `4w`), or `false` to clear\none in place. Absent leaves any existing lifetime untouched.\n\n`false` rather than `null` carries the clear because `null` is already\nspoken for above. This is the opposite convention to\n`CollectorsEdit.threshold`, where `null` removes — noted because the two\nlive in one daemon and the difference is deliberate rather than drift."
            }
          },
          "additionalProperties": false,
          "required": [
            "path"
          ]
        }
      }
    },
    "CasesCreateResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CasesCreateResult",
      "type": "object",
      "properties": {
        "stem": {
          "description": "`checkout-hang-260731-021530` — the stem all three files share.",
          "type": "string"
        },
        "paths": {
          "description": "What was actually written, document first. Not what was intended.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "verdict": {
          "description": "The §4.2 verdict for the captured window, on the wire as well as in the\ndocument — the document's most important correctness property must not\nbe reachable only by parsing markdown.",
          "$ref": "#/$defs/EvidenceVerdict"
        },
        "document_bytes": {
          "description": "The document is the artifact whose size can run away, so it is the one\nthat gets measured alongside the two that cannot.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "logdata": {
          "$ref": "#/$defs/CaseFile"
        },
        "spandata": {
          "$ref": "#/$defs/CaseFile"
        },
        "notes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CaseNote"
          }
        },
        "data_outcomes": {
          "description": "Per-entry outcomes for `data`, in the order sent — the same call as\n`domain_data.update`, with **one value it never emits**: `scoped`, for a\nkey carrying the `@` sigil, which went to this document instead of the\nregistry. A closed-set reader written against `domain_data.update` needs\nthat arm added.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainDataOutcome"
          },
          "default": []
        },
        "data_persist_error": {
          "description": "Set when the registry could not be made durable. The capture still\nhappened and the files are still on disk.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "stem",
        "paths",
        "verdict",
        "document_bytes",
        "logdata",
        "spandata",
        "notes"
      ],
      "$defs": {
        "EvidenceVerdict": {
          "description": "How much of an exported window the daemon can vouch for.\n\nThe reason this exists: storage is conditional, so \"nothing appeared before\nthe error\" is ambiguous between *absence of cause* and *absence of\nrecording*. Only a record of the storage policy over time can separate them,\nand without one a window shaped by another session's filter reads as\ncomplete.",
          "oneOf": [
            {
              "description": "The window lies wholly inside one unfiltered epoch, nothing was evicted,\nand nothing was capped. Everything that reached the daemon over these\nseqs **was stored**.\n\nIt does *not* say everything stored is in this reply: your own `filter`\nstill selects, and `count` still limits. The verdict is about the\ndaemon's storage policy over the range — which is the only thing a later\nread cannot recover — not about what this particular call returned.",
              "type": "string",
              "const": "complete"
            },
            {
              "description": "The ring dropped part of the window.\n\nOn an export, `evicted_before_window` bounds how much — the window's\nlower end and the store's floor are independent numbers there, so their\ndifference means something. On a **capture** there is no such bound and\nnone is reported: the window opens at the first record that SURVIVED, so\nthe two coincide, and the document states the floor and the caller's\nshortfall as the separate facts they are rather than passing one off as\na count of the other.",
              "type": "string",
              "const": "evicted"
            },
            {
              "description": "A session filter was narrowing the store over part of the window. See\n`narrowed_by` for which filters, and over which seqs.",
              "type": "string",
              "const": "filtered"
            },
            {
              "description": "No claim can be made: the store is empty, or the window predates what\nthe daemon still records about its own storage policy (including a\nwindow carried over from a previous daemon run), or the read was capped\nshort of what matched.\n\nThe default, deliberately — see [`LogsExportResult::verdict`].",
              "type": "string",
              "const": "cannot_verify"
            }
          ]
        },
        "CaseFile": {
          "description": "One written evidence file.",
          "type": "object",
          "properties": {
            "records": {
              "description": "Data records, excluding the format header line.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "bytes": {
              "description": "File size including the header.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "records",
            "bytes"
          ]
        },
        "CaseNote": {
          "description": "Something the caller should know that did not stop the capture.\n\nTyped rather than a prose list, because capture gaps, provenance\nobservations and write problems are three different things and a caller\nacting on them should not have to tell them apart by reading English.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "`capture_gap`, `provenance`, `truncated` or `write`.",
              "type": "string"
            },
            "detail": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "detail"
          ]
        },
        "DomainDataOutcome": {
          "description": "What happened to one entry.\n\n`outcome` is one of `created`, `updated`, `validated`, `unknown`,\n`rejected`. The last two carry a discriminant rather than prose, so a caller\ncan branch instead of string-matching a sentence.",
          "type": "object",
          "properties": {
            "path": {
              "description": "The key as the caller spelled it, sigil included.",
              "type": "string"
            },
            "outcome": {
              "type": "string"
            },
            "cause": {
              "description": "`never_set` | `no_registry` | `undetermined` — present on `unknown`.\n\nThree, not two: `/logmon/first_seen` is written identically for a\nbrand-new domain and for one whose file was lost, so from the registry\nalone those are not distinguishable. `undetermined` is the honest\ndefault and `no_registry` is claimed only with a quarantine artifact.",
              "type": [
                "string",
                "null"
              ]
            },
            "reason": {
              "description": "`malformed_path` | `path_too_long` | `value_too_long` |\n`reserved_prefix` | `sigil_not_allowed_here` | `malformed_ttl` |\n`registry_full` — present on `rejected`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "path",
            "outcome"
          ]
        }
      }
    },
    "CaseFile": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CaseFile",
      "description": "One written evidence file.",
      "type": "object",
      "properties": {
        "records": {
          "description": "Data records, excluding the format header line.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "bytes": {
          "description": "File size including the header.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "records",
        "bytes"
      ]
    },
    "CaseNote": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CaseNote",
      "description": "Something the caller should know that did not stop the capture.\n\nTyped rather than a prose list, because capture gaps, provenance\nobservations and write problems are three different things and a caller\nacting on them should not have to tell them apart by reading English.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "`capture_gap`, `provenance`, `truncated` or `write`.",
          "type": "string"
        },
        "detail": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "detail"
      ]
    },
    "BookmarksAdd": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksAdd",
      "type": "object",
      "properties": {
        "name": {
          "description": "Bookmark name (alphanumerics, '-', '_'; max 64 chars). Will be qualified\nwith the calling session's name automatically.",
          "type": "string"
        },
        "start_seq": {
          "description": "Caller-chosen anchor seq. Defaults to the daemon's current seq counter\n(i.e., \"right now\") when omitted.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "description": {
          "description": "Optional caller-supplied note retained for human display.",
          "type": [
            "string",
            "null"
          ]
        },
        "replace": {
          "description": "If true, overwrite an existing bookmark with the same qualified name.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "BookmarksAddResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksAddResult",
      "type": "object",
      "properties": {
        "qualified_name": {
          "type": "string"
        },
        "seq": {
          "description": "Anchor seq the bookmark was created at.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "replaced": {
          "type": "boolean"
        }
      },
      "required": [
        "qualified_name",
        "seq",
        "replaced"
      ]
    },
    "BookmarksList": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksList",
      "type": "object",
      "properties": {
        "session": {
          "description": "Optional: filter to bookmarks created by this session name.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "BookmarksListResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksListResult",
      "type": "object",
      "properties": {
        "bookmarks": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/BookmarkInfo"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "bookmarks",
        "count"
      ],
      "$defs": {
        "BookmarkInfo": {
          "description": "One entry in a `bookmarks.list` response.\n\nAs of the cursor design (2026-05-01), bookmark position is a `u64` seq\nrather than a wall-clock timestamp. `created_at` is retained for human\ndisplay only; never use it for filter semantics.",
          "type": "object",
          "properties": {
            "qualified_name": {
              "description": "Qualified form (`\"session/bookmark\"`). The bare name is recoverable\nby splitting on the last `/`.",
              "type": "string"
            },
            "seq": {
              "description": "Position the bookmark anchors. Filter DSL `b>=name` matches records\nwith `entry.seq > seq`; `b<=name` matches `entry.seq < seq`.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "created_at": {
              "description": "Wall-clock creation time, for display only.",
              "type": "string",
              "format": "date-time"
            },
            "description": {
              "description": "Optional caller-supplied note.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "qualified_name",
            "seq",
            "created_at"
          ]
        }
      }
    },
    "BookmarksRemove": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksRemove",
      "type": "object",
      "properties": {
        "name": {
          "description": "Bare name (resolved against current session) or qualified\n\"session/name\".",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "BookmarksRemoveResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksRemoveResult",
      "type": "object",
      "properties": {
        "removed": {
          "type": "string"
        }
      },
      "required": [
        "removed"
      ]
    },
    "BookmarksClear": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksClear",
      "type": "object",
      "properties": {
        "session": {
          "description": "Defaults to the calling session when omitted.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "BookmarksClearResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarksClearResult",
      "type": "object",
      "properties": {
        "removed_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "session": {
          "type": "string"
        }
      },
      "required": [
        "removed_count",
        "session"
      ]
    },
    "SessionsList": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionsList",
      "type": "object",
      "additionalProperties": false
    },
    "SessionsListResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionsListResult",
      "type": "object",
      "properties": {
        "sessions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SessionInfo"
          }
        }
      },
      "required": [
        "sessions"
      ],
      "$defs": {
        "SessionInfo": {
          "description": "One entry in a `sessions.list` or `status.get` response.",
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "connected": {
              "type": "boolean"
            },
            "trigger_count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "filter_count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "queue_size": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "last_seen_secs_ago": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "client_info": {
              "description": "Caller-supplied identity blob from the most recent `session.start`.\nOmitted from the wire when never set."
            }
          },
          "required": [
            "id",
            "connected",
            "trigger_count",
            "filter_count",
            "queue_size",
            "last_seen_secs_ago"
          ]
        }
      }
    },
    "SessionsDrop": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionsDrop",
      "type": "object",
      "properties": {
        "name": {
          "description": "Name of the session to drop",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "SessionsDropResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionsDropResult",
      "type": "object",
      "properties": {
        "dropped": {
          "type": "string"
        }
      },
      "required": [
        "dropped"
      ]
    },
    "StatusGet": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "StatusGet",
      "type": "object",
      "additionalProperties": false
    },
    "StatusGetResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "StatusGetResult",
      "type": "object",
      "properties": {
        "session": {
          "anyOf": [
            {
              "$ref": "#/$defs/SessionInfo"
            },
            {
              "type": "null"
            }
          ]
        },
        "daemon_uptime_secs": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "broker_version": {
          "description": "This broker's version. Additive; an older daemon that omits it\ndeserializes as `\"\"`.",
          "type": "string",
          "default": ""
        },
        "broker_tools": {
          "description": "The MCP tools a shim built at this broker's version exposes\n([`crate::mcp_tools::TOOLS`]). A client holding fewer is out of date.\n\nMirrored here — not left to the untyped passthrough — because this type\nis deserialized and re-serialized on the typed SDK path, so a field the\ndaemon emits but this struct lacks is **silently dropped for every typed\ncaller**. That has happened before in this crate; see the note on\n[`TriggersEditResult::post_remaining`]. `verify-schema` cannot catch it:\nit checks schema-against-Rust, not daemon-JSON-against-Rust.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "receivers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "store": {
          "$ref": "#/$defs/StoreStats"
        },
        "receiver_drops": {
          "$ref": "#/$defs/ReceiverDropCounts",
          "default": {
            "gelf_udp": 0,
            "gelf_tcp": 0,
            "otlp_http_logs": 0,
            "otlp_http_traces": 0,
            "otlp_grpc_logs": 0,
            "otlp_grpc_traces": 0
          }
        },
        "trace_ingest": {
          "description": "Spans lost on the OTLP trace transports before any collector saw\nthem — a sibling of `receiver_drops`, not a member (§ doc on\n[`TraceIngestCounts`]). Non-zero means every span-derived number\nelsewhere in this payload (and in `collectors.*`) is a lower bound.",
          "$ref": "#/$defs/TraceIngestCounts",
          "default": {
            "dropped": 0,
            "shed_batches": 0,
            "malformed_dropped": 0
          }
        },
        "current_domain": {
          "description": "The domain the calling session is currently bound to (§7). Additive:\nan older daemon that omits it deserializes as `\"\"`.",
          "type": "string",
          "default": ""
        },
        "active_filters": {
          "description": "Echo of the session's active buffer-filter strings (§7) so a single\nstatus call answers \"what is narrowing me\". Additive.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "receiver_liveness": {
          "description": "Per-listener last-received wall-clock for the caller's bound domain — the\ndrill-down for \"which port is (not) receiving.\" (Consumer #2.)",
          "$ref": "#/$defs/ReceiverLiveness",
          "default": {}
        }
      },
      "required": [
        "daemon_uptime_secs",
        "receivers",
        "store"
      ],
      "$defs": {
        "SessionInfo": {
          "description": "One entry in a `sessions.list` or `status.get` response.",
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "connected": {
              "type": "boolean"
            },
            "trigger_count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "filter_count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "queue_size": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "last_seen_secs_ago": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "client_info": {
              "description": "Caller-supplied identity blob from the most recent `session.start`.\nOmitted from the wire when never set."
            }
          },
          "required": [
            "id",
            "connected",
            "trigger_count",
            "filter_count",
            "queue_size",
            "last_seen_secs_ago"
          ]
        },
        "StoreStats": {
          "description": "Pipeline buffer / receive statistics. Embedded in `status.get`.",
          "type": "object",
          "properties": {
            "total_received": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "total_stored": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "current_size": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "required": [
            "total_received",
            "total_stored",
            "malformed_count",
            "current_size"
          ]
        },
        "ReceiverDropCounts": {
          "description": "Per-source receiver drop counts. Each field is the cumulative count of\nentries dropped at that receiver call site since broker start, due to\nthe upstream channel being full (i.e. the consumer couldn't keep up).\nHealthy operation keeps all fields at 0.",
          "type": "object",
          "properties": {
            "gelf_udp": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "gelf_tcp": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "otlp_http_logs": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "otlp_http_traces": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "otlp_grpc_logs": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "otlp_grpc_traces": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            }
          }
        },
        "TraceIngestCounts": {
          "description": "Span loss on the two OTLP trace transports — surfaced as a SIBLING of\n[`ReceiverDropCounts`] on purpose, not folded into it.\n\n`receiver_drops` documents itself as counting entries the broker lost\n*silently* — the channel was full and the sender was never told. None of\nthese three counters describe that. A shed batch is a 429 / UNAVAILABLE\nthe caller saw and can retry; a malformed span was refused for cause, not\nbackpressure. Merging any of them into `receiver_drops` would change what\nthat field means. Keep them apart — do not \"fix\" this into a merge.\n\n**`dropped` is not new information: it is exactly\n`receiver_drops.otlp_http_traces + receiver_drops.otlp_grpc_traces`**, read\nfrom the same two atomics. It is repeated here so the three trace-loss\nfigures can be read as one block, and it is stated here because **adding it\nto those fields double-counts.** `shed_batches` and `malformed_dropped` are\nthe genuinely new numbers — nothing else in this payload reports them.\n\nScope: the calling session's bound domain, monotonic since that domain's\ncounters were created (daemon start for `default`, domain creation for\nany other domain).",
          "type": "object",
          "properties": {
            "dropped": {
              "description": "Per-span channel-full drops on the two OTLP trace transports.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shed_batches": {
              "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_dropped": {
              "description": "Spans discarded at parse time for unusable identity. Named\n`malformed_dropped` rather than `malformed`: `store.malformed_count`\n(log-side) already sits in this same payload, and the two must not\nread as one number.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "dropped",
            "shed_batches",
            "malformed_dropped"
          ]
        },
        "ReceiverLiveness": {
          "description": "Per-listener last-received wall-clock timestamps for the caller's bound\ndomain — the drill-down for \"which port is (not) receiving.\" `None` = that\nlistener has received nothing. Mirrors [`ReceiverDropCounts`]. (Consumer #2.)",
          "type": "object",
          "properties": {
            "gelf_udp": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "gelf_tcp": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "otlp_http_logs": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "otlp_http_traces": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "otlp_grpc_logs": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "otlp_grpc_traces": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            }
          }
        }
      }
    },
    "TraceIngestCounts": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TraceIngestCounts",
      "description": "Span loss on the two OTLP trace transports — surfaced as a SIBLING of\n[`ReceiverDropCounts`] on purpose, not folded into it.\n\n`receiver_drops` documents itself as counting entries the broker lost\n*silently* — the channel was full and the sender was never told. None of\nthese three counters describe that. A shed batch is a 429 / UNAVAILABLE\nthe caller saw and can retry; a malformed span was refused for cause, not\nbackpressure. Merging any of them into `receiver_drops` would change what\nthat field means. Keep them apart — do not \"fix\" this into a merge.\n\n**`dropped` is not new information: it is exactly\n`receiver_drops.otlp_http_traces + receiver_drops.otlp_grpc_traces`**, read\nfrom the same two atomics. It is repeated here so the three trace-loss\nfigures can be read as one block, and it is stated here because **adding it\nto those fields double-counts.** `shed_batches` and `malformed_dropped` are\nthe genuinely new numbers — nothing else in this payload reports them.\n\nScope: the calling session's bound domain, monotonic since that domain's\ncounters were created (daemon start for `default`, domain creation for\nany other domain).",
      "type": "object",
      "properties": {
        "dropped": {
          "description": "Per-span channel-full drops on the two OTLP trace transports.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "shed_batches": {
          "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "malformed_dropped": {
          "description": "Spans discarded at parse time for unusable identity. Named\n`malformed_dropped` rather than `malformed`: `store.malformed_count`\n(log-side) already sits in this same payload, and the two must not\nread as one number.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "dropped",
        "shed_batches",
        "malformed_dropped"
      ]
    },
    "SessionStartParams": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionStartParams",
      "description": "session.start parameters",
      "type": "object",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "protocol_version": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "client_info": {
          "description": "Caller-supplied identity/metadata. The daemon currently records and ignores\nthis field; size validation lands in Task 11."
        },
        "domain": {
          "description": "Optional connect-time domain bind: an atomic equivalent of calling\n`domains.use` immediately after connecting. Errors the handshake if the\nnamed domain does not exist. Omitted → the `default` domain.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "protocol_version"
      ]
    },
    "SessionStartResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionStartResult",
      "description": "session.start response",
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string"
        },
        "is_new": {
          "type": "boolean"
        },
        "queued_notifications": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "trigger_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "filter_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "daemon_uptime_secs": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "buffer_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "receivers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "capabilities": {
          "description": "Capability strings advertised by the daemon. Lets SDKs feature-gate\nwithout sniffing protocol versions. Always serialized (no\n`skip_serializing_if`) so callers can rely on the field being present.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      },
      "required": [
        "session_id",
        "is_new",
        "queued_notifications",
        "trigger_count",
        "filter_count",
        "daemon_uptime_secs",
        "buffer_size",
        "receivers"
      ]
    },
    "DomainInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainInfo",
      "description": "A single domain as reported by `domains.list` and `domains.create`.\n\nA port value of `0` means that receiver is disabled for the domain. The\ncount/seq fields reflect the domain's live buffers; the `last_*_received_at` /\n`idle_secs` / `stale` liveness fields answer \"is this domain actually\nreceiving?\" (`status.get.receiver_liveness` gives the per-listener drill-down).",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "gelf_port": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "otlp_grpc_port": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "otlp_http_port": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "source": {
          "description": "`\"config\"` | `\"persistent\"` | `\"ephemeral\"`.",
          "type": "string"
        },
        "log_buffer_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "span_buffer_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "log_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "span_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "oldest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "bound_sessions": {
          "description": "Sessions currently bound to this domain (connected first, disconnected\nsuffixed). DERIVED from the session registry — never caller-supplied —\nso with meaningful session names, `domains.list` answers \"which\nconversation is using which domain\" authoritatively. Populated by\n`domains.list`; empty elsewhere.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "last_log_received_at": {
          "description": "Wall-clock of the last log ingested into this domain (across any listener),\nor `None` if none ever arrived. A `None` here is the \"nothing is shipping\nto this domain — did I misconfigure the port?\" signal. (Consumer #2.)",
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "last_span_received_at": {
          "description": "Wall-clock of the last span ingested into this domain, or `None`.",
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "idle_secs": {
          "description": "Seconds since the most recent log OR span ingest; `None` if neither ever\narrived.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "stale": {
          "description": "`true` when the domain HAS received before but has been idle longer than\nthe broker's `stale_after_secs` (config, default 60s). A never-received\ndomain is `false` — distinguish it via `last_*_received_at == None`.",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "name",
        "gelf_port",
        "otlp_grpc_port",
        "otlp_http_port",
        "source",
        "log_buffer_size",
        "span_buffer_size",
        "log_count",
        "span_count"
      ]
    },
    "DomainsCreate": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsCreate",
      "type": "object",
      "properties": {
        "name": {
          "description": "Domain name — the isolation key.",
          "type": "string"
        },
        "gelf_port": {
          "description": "GELF UDP+TCP port. Omitted → auto-allocate; `0` → disable GELF for this\ndomain; otherwise bind exactly this port (error if held by another domain).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "otlp_grpc_port": {
          "description": "OTLP gRPC port. Omitted → auto-allocate; 0 → disable.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "otlp_http_port": {
          "description": "OTLP HTTP port. Omitted → auto-allocate; 0 → disable.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "log_buffer_size": {
          "description": "Log ring capacity (default: the daemon's configured size).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "span_buffer_size": {
          "description": "Span ring capacity (default: the daemon's configured size).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "persist": {
          "description": "`false` (default) → ephemeral (gone on restart). `true` → durable\n(re-created at boot). Durable domains are a later stage; Wave 2 accepts\nonly ephemeral creation.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "DomainsDelete": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsDelete",
      "type": "object",
      "properties": {
        "name": {
          "description": "Name of the domain to delete. Refuses config-declared domains (incl.\n'default').",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "DomainsDeleteResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsDeleteResult",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "deleted": {
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "deleted"
      ]
    },
    "DomainsList": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsList",
      "type": "object",
      "additionalProperties": false
    },
    "DomainsListResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsListResult",
      "type": "object",
      "properties": {
        "domains": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainInfo"
          }
        }
      },
      "required": [
        "domains"
      ],
      "$defs": {
        "DomainInfo": {
          "description": "A single domain as reported by `domains.list` and `domains.create`.\n\nA port value of `0` means that receiver is disabled for the domain. The\ncount/seq fields reflect the domain's live buffers; the `last_*_received_at` /\n`idle_secs` / `stale` liveness fields answer \"is this domain actually\nreceiving?\" (`status.get.receiver_liveness` gives the per-listener drill-down).",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "gelf_port": {
              "type": "integer",
              "format": "uint16",
              "minimum": 0,
              "maximum": 65535
            },
            "otlp_grpc_port": {
              "type": "integer",
              "format": "uint16",
              "minimum": 0,
              "maximum": 65535
            },
            "otlp_http_port": {
              "type": "integer",
              "format": "uint16",
              "minimum": 0,
              "maximum": 65535
            },
            "source": {
              "description": "`\"config\"` | `\"persistent\"` | `\"ephemeral\"`.",
              "type": "string"
            },
            "log_buffer_size": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "span_buffer_size": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "log_count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "span_count": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "oldest_seq": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "newest_seq": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "bound_sessions": {
              "description": "Sessions currently bound to this domain (connected first, disconnected\nsuffixed). DERIVED from the session registry — never caller-supplied —\nso with meaningful session names, `domains.list` answers \"which\nconversation is using which domain\" authoritatively. Populated by\n`domains.list`; empty elsewhere.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "last_log_received_at": {
              "description": "Wall-clock of the last log ingested into this domain (across any listener),\nor `None` if none ever arrived. A `None` here is the \"nothing is shipping\nto this domain — did I misconfigure the port?\" signal. (Consumer #2.)",
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "last_span_received_at": {
              "description": "Wall-clock of the last span ingested into this domain, or `None`.",
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "idle_secs": {
              "description": "Seconds since the most recent log OR span ingest; `None` if neither ever\narrived.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "stale": {
              "description": "`true` when the domain HAS received before but has been idle longer than\nthe broker's `stale_after_secs` (config, default 60s). A never-received\ndomain is `false` — distinguish it via `last_*_received_at == None`.",
              "type": "boolean",
              "default": false
            }
          },
          "required": [
            "name",
            "gelf_port",
            "otlp_grpc_port",
            "otlp_http_port",
            "source",
            "log_buffer_size",
            "span_buffer_size",
            "log_count",
            "span_count"
          ]
        }
      }
    },
    "DomainsUse": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsUse",
      "type": "object",
      "properties": {
        "name": {
          "description": "Domain to bind this session to for subsequent queries and notifications.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "DomainsClear": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsClear",
      "type": "object",
      "additionalProperties": false
    },
    "DomainsClearResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsClearResult",
      "type": "object",
      "properties": {
        "logs_cleared": {
          "description": "Number of log records disposed from the bound domain.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "spans_cleared": {
          "description": "Number of spans disposed from the bound domain.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "logs_cleared",
        "spans_cleared"
      ]
    },
    "CollectorsReset": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsReset",
      "description": "`collectors.reset` — zero a collector and **discard** the window.\n\nA one-field request type still earns its place: without it the method has no\nschema, and a tool whose parameters nothing describes cannot be checked\nagainst the shim, generated from, or published in a manifest.",
      "type": "object",
      "properties": {
        "name": {
          "description": "The collector to zero.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "CollectorsRemove": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsRemove",
      "description": "`collectors.remove` — disarm and delete, taking its recorded runs with it.",
      "type": "object",
      "properties": {
        "name": {
          "description": "The collector to remove.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "SessionsRename": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionsRename",
      "description": "`sessions.rename` — re-key the calling session to a meaningful name\n(`<Project>-Main-<short8>` / `<Project>-tN-<branch>`, spec 2026-07-17),\npreserving all state. Errors with \"already connected\" when the target name\nis held by a LIVE session — the one-conversation-per-lane invariant.",
      "type": "object",
      "properties": {
        "name": {
          "description": "New session name: <Project>-Main-<short8> or <Project>-tN-<branch> ('/'\nsanitized to '-'; alphanumerics, '-' and '_' only).",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "SessionsRenameResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionsRenameResult",
      "type": "object",
      "properties": {
        "name": {
          "description": "The session's new name (echoed; the connection re-keys itself on it).",
          "type": "string"
        },
        "displaced_stale_holder": {
          "description": "Set when a stale (disconnected) holder of this name was displaced.",
          "type": "boolean"
        }
      },
      "required": [
        "name"
      ]
    },
    "DomainsUseResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainsUseResult",
      "description": "`domains.use` returns the now-bound domain (same shape as a `domains.list`\nentry), plus a warning when the bind REPLACED a different non-default\nbinding.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "gelf_port": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "otlp_grpc_port": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "otlp_http_port": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "source": {
          "description": "`\"config\"` | `\"persistent\"` | `\"ephemeral\"`.",
          "type": "string"
        },
        "log_buffer_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "span_buffer_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "log_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "span_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "oldest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "newest_seq": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "bound_sessions": {
          "description": "Sessions currently bound to this domain (connected first, disconnected\nsuffixed). DERIVED from the session registry — never caller-supplied —\nso with meaningful session names, `domains.list` answers \"which\nconversation is using which domain\" authoritatively. Populated by\n`domains.list`; empty elsewhere.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "last_log_received_at": {
          "description": "Wall-clock of the last log ingested into this domain (across any listener),\nor `None` if none ever arrived. A `None` here is the \"nothing is shipping\nto this domain — did I misconfigure the port?\" signal. (Consumer #2.)",
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "last_span_received_at": {
          "description": "Wall-clock of the last span ingested into this domain, or `None`.",
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "idle_secs": {
          "description": "Seconds since the most recent log OR span ingest; `None` if neither ever\narrived.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "stale": {
          "description": "`true` when the domain HAS received before but has been idle longer than\nthe broker's `stale_after_secs` (config, default 60s). A never-received\ndomain is `false` — distinguish it via `last_*_received_at == None`.",
          "type": "boolean",
          "default": false
        },
        "rebind_warning": {
          "description": "Set when this bind switched the session from one NON-default domain to\na different one. A session's normal lifecycle is `default → its domain`,\nthen idempotent re-binds; a non-default→non-default switch is the\nsignature of two clients sharing one session (e.g. two agent\nconversations behind one shared MCP server process), each rebinding the\nother's reads out from under it. The bind still succeeds — the warning\nrides the response so BOTH consumers of the shared stream see it.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "gelf_port",
        "otlp_grpc_port",
        "otlp_http_port",
        "source",
        "log_buffer_size",
        "span_buffer_size",
        "log_count",
        "span_count"
      ]
    },
    "ToolsManifest": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ToolsManifest",
      "description": "`tools.manifest` takes no parameters: the manifest describes the protocol,\nnot the connection, so nothing about the caller could change the answer.",
      "type": "object"
    },
    "ToolsManifestResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ToolsManifestResult",
      "description": "What `tools.manifest` answers with.\n\nLives here rather than in `methods` because it describes the MCP front-end's\nsurface, which is the boundary this module exists to keep separate from the\nwire protocol's own types.",
      "type": "object",
      "properties": {
        "protocol_version": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "broker_version": {
          "description": "Which daemon build answered. A shim reports skew against this rather\nthan guessing from the tool list.",
          "type": "string"
        },
        "tools": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ManifestEntry"
          }
        },
        "skill": {
          "description": "The agent-facing guide (`SKILL`), for the client to surface as MCP\n`instructions`.\n\nOptional on the wire, and its absence is **not** fatal to a shim: a\nmissing document degrades guidance, while a missing tool list would make\nthe shim misrepresent the surface. Those warrant different answers, and\nthe shim gives them different answers.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "protocol_version",
        "broker_version",
        "tools"
      ],
      "$defs": {
        "ManifestEntry": {
          "description": "One tool, as the daemon serves it: the name an agent calls, the text it reads\nto decide whether to, and the schema its arguments must satisfy.\n\nThis is the whole point of the exercise. A shim holding these three things\ncan register a tool it has never heard of, which is what lets the daemon gain\na tool without the shim being reinstalled.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "method": {
              "type": "string"
            },
            "description": {
              "type": "string"
            },
            "input_schema": {
              "description": "The JSON Schema for this tool's parameters.\n\n`None` means the schema has no definition under this tool's derived name\n— which is a **generator gap, not a tool without parameters**. The two\nare indistinguishable from the outside, so they must not be conflated:\nserving `{}` for a missing definition would advertise \"takes nothing\" for\na tool that takes several, and every call would then be rejected for\nunknown fields.\n\n**`$ref`s are resolved before it is served.** A client would otherwise\nhave to chase `#/$defs/ThresholdSpec` to discover that `threshold` is an\nobject with five fields — and a client that cannot see inside a nested\nparameter cannot offer it at all."
            },
            "cli": {
              "description": "What a front-end needs beyond the schema to build a command line.",
              "$ref": "#/$defs/CliHints",
              "default": {}
            }
          },
          "required": [
            "name",
            "method",
            "description"
          ]
        },
        "CliHints": {
          "description": "[`Cli`] as it crosses the wire. `Cli` itself is `&'static` so it can live in\na `const`; this is the owned shape a client deserializes into.",
          "type": "object",
          "properties": {
            "positional": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "variadic": {
              "type": "boolean"
            },
            "file_output": {
              "anyOf": [
                {
                  "$ref": "#/$defs/FileOutputHints"
                },
                {
                  "type": "null"
                }
              ]
            }
          }
        },
        "FileOutputHints": {
          "description": "[`FileOutput`] as it crosses the wire.",
          "type": "object",
          "properties": {
            "path_param": {
              "type": "string"
            },
            "content_field": {
              "type": [
                "string",
                "null"
              ]
            },
            "sidecar": {
              "anyOf": [
                {
                  "$ref": "#/$defs/SidecarHints"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "path_param"
          ]
        },
        "SidecarHints": {
          "description": "[`Sidecar`] as it crosses the wire.",
          "type": "object",
          "properties": {
            "name_field": {
              "type": "string"
            },
            "content_field": {
              "type": "string"
            }
          },
          "required": [
            "name_field",
            "content_field"
          ]
        }
      }
    },
    "ManifestEntry": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ManifestEntry",
      "description": "One tool, as the daemon serves it: the name an agent calls, the text it reads\nto decide whether to, and the schema its arguments must satisfy.\n\nThis is the whole point of the exercise. A shim holding these three things\ncan register a tool it has never heard of, which is what lets the daemon gain\na tool without the shim being reinstalled.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "method": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "input_schema": {
          "description": "The JSON Schema for this tool's parameters.\n\n`None` means the schema has no definition under this tool's derived name\n— which is a **generator gap, not a tool without parameters**. The two\nare indistinguishable from the outside, so they must not be conflated:\nserving `{}` for a missing definition would advertise \"takes nothing\" for\na tool that takes several, and every call would then be rejected for\nunknown fields.\n\n**`$ref`s are resolved before it is served.** A client would otherwise\nhave to chase `#/$defs/ThresholdSpec` to discover that `threshold` is an\nobject with five fields — and a client that cannot see inside a nested\nparameter cannot offer it at all."
        },
        "cli": {
          "description": "What a front-end needs beyond the schema to build a command line.",
          "$ref": "#/$defs/CliHints",
          "default": {}
        }
      },
      "required": [
        "name",
        "method",
        "description"
      ],
      "$defs": {
        "CliHints": {
          "description": "[`Cli`] as it crosses the wire. `Cli` itself is `&'static` so it can live in\na `const`; this is the owned shape a client deserializes into.",
          "type": "object",
          "properties": {
            "positional": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "variadic": {
              "type": "boolean"
            },
            "file_output": {
              "anyOf": [
                {
                  "$ref": "#/$defs/FileOutputHints"
                },
                {
                  "type": "null"
                }
              ]
            }
          }
        },
        "FileOutputHints": {
          "description": "[`FileOutput`] as it crosses the wire.",
          "type": "object",
          "properties": {
            "path_param": {
              "type": "string"
            },
            "content_field": {
              "type": [
                "string",
                "null"
              ]
            },
            "sidecar": {
              "anyOf": [
                {
                  "$ref": "#/$defs/SidecarHints"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "path_param"
          ]
        },
        "SidecarHints": {
          "description": "[`Sidecar`] as it crosses the wire.",
          "type": "object",
          "properties": {
            "name_field": {
              "type": "string"
            },
            "content_field": {
              "type": "string"
            }
          },
          "required": [
            "name_field",
            "content_field"
          ]
        }
      }
    },
    "CliHints": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CliHints",
      "description": "[`Cli`] as it crosses the wire. `Cli` itself is `&'static` so it can live in\na `const`; this is the owned shape a client deserializes into.",
      "type": "object",
      "properties": {
        "positional": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "variadic": {
          "type": "boolean"
        },
        "file_output": {
          "anyOf": [
            {
              "$ref": "#/$defs/FileOutputHints"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "$defs": {
        "FileOutputHints": {
          "description": "[`FileOutput`] as it crosses the wire.",
          "type": "object",
          "properties": {
            "path_param": {
              "type": "string"
            },
            "content_field": {
              "type": [
                "string",
                "null"
              ]
            },
            "sidecar": {
              "anyOf": [
                {
                  "$ref": "#/$defs/SidecarHints"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "path_param"
          ]
        },
        "SidecarHints": {
          "description": "[`Sidecar`] as it crosses the wire.",
          "type": "object",
          "properties": {
            "name_field": {
              "type": "string"
            },
            "content_field": {
              "type": "string"
            }
          },
          "required": [
            "name_field",
            "content_field"
          ]
        }
      }
    },
    "DomainDataEntry": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataEntry",
      "description": "One requested change to the registry.\n\n**Absent ≡ `null` ≡ unchanged, for both `value` and `ttl`**, and the two\nshare one rule deliberately: two optional fields on one object with opposite\nabsent semantics is the footgun this surface exists to avoid, and a client\nserialising a missing field as `null` would otherwise mean the opposite of\nwhat it sent. Erasure is `domain_data.remove`, never `value: null`.",
      "type": "object",
      "properties": {
        "path": {
          "description": "A registry path — `/Build/commit`. A leading `@` scopes the fact to one\ncase document instead of the domain, and is accepted only where a\ndocument exists to receive it.",
          "type": "string"
        },
        "value": {
          "description": "Present → set. Absent → validate only, which never creates.",
          "type": [
            "string",
            "null"
          ]
        },
        "ttl": {
          "description": "A duration literal (`30s`, `5m`, `2h`, `7d`, `4w`), or `false` to clear\none in place. Absent leaves any existing lifetime untouched.\n\n`false` rather than `null` carries the clear because `null` is already\nspoken for above. This is the opposite convention to\n`CollectorsEdit.threshold`, where `null` removes — noted because the two\nlive in one daemon and the difference is deliberate rather than drift."
        }
      },
      "additionalProperties": false,
      "required": [
        "path"
      ]
    },
    "DomainDataUpdate": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataUpdate",
      "type": "object",
      "properties": {
        "entries": {
          "description": "Entries to record. Each is `{path, value?, ttl?}`. **Value present** →\nset it. **Value absent** → *validate*: confirm what is already there,\nmoving only its confirmation time. A key-only entry never creates —\nrecording a key with no value would be a guess. `ttl` is `30s` / `5m` /\n`2h` / `7d` / `4w`, or `false` to clear one. Absent leaves any existing\nlifetime alone, exactly as an absent value does, so restating a value\ncannot silently drop its lifetime.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainDataEntry"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "entries"
      ],
      "$defs": {
        "DomainDataEntry": {
          "description": "One requested change to the registry.\n\n**Absent ≡ `null` ≡ unchanged, for both `value` and `ttl`**, and the two\nshare one rule deliberately: two optional fields on one object with opposite\nabsent semantics is the footgun this surface exists to avoid, and a client\nserialising a missing field as `null` would otherwise mean the opposite of\nwhat it sent. Erasure is `domain_data.remove`, never `value: null`.",
          "type": "object",
          "properties": {
            "path": {
              "description": "A registry path — `/Build/commit`. A leading `@` scopes the fact to one\ncase document instead of the domain, and is accepted only where a\ndocument exists to receive it.",
              "type": "string"
            },
            "value": {
              "description": "Present → set. Absent → validate only, which never creates.",
              "type": [
                "string",
                "null"
              ]
            },
            "ttl": {
              "description": "A duration literal (`30s`, `5m`, `2h`, `7d`, `4w`), or `false` to clear\none in place. Absent leaves any existing lifetime untouched.\n\n`false` rather than `null` carries the clear because `null` is already\nspoken for above. This is the opposite convention to\n`CollectorsEdit.threshold`, where `null` removes — noted because the two\nlive in one daemon and the difference is deliberate rather than drift."
            }
          },
          "additionalProperties": false,
          "required": [
            "path"
          ]
        }
      }
    },
    "DomainDataOutcome": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataOutcome",
      "description": "What happened to one entry.\n\n`outcome` is one of `created`, `updated`, `validated`, `unknown`,\n`rejected`. The last two carry a discriminant rather than prose, so a caller\ncan branch instead of string-matching a sentence.",
      "type": "object",
      "properties": {
        "path": {
          "description": "The key as the caller spelled it, sigil included.",
          "type": "string"
        },
        "outcome": {
          "type": "string"
        },
        "cause": {
          "description": "`never_set` | `no_registry` | `undetermined` — present on `unknown`.\n\nThree, not two: `/logmon/first_seen` is written identically for a\nbrand-new domain and for one whose file was lost, so from the registry\nalone those are not distinguishable. `undetermined` is the honest\ndefault and `no_registry` is claimed only with a quarantine artifact.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "`malformed_path` | `path_too_long` | `value_too_long` |\n`reserved_prefix` | `sigil_not_allowed_here` | `malformed_ttl` |\n`registry_full` — present on `rejected`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "path",
        "outcome"
      ]
    },
    "DomainDataUpdateResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataUpdateResult",
      "type": "object",
      "properties": {
        "outcomes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainDataOutcome"
          }
        },
        "persist_error": {
          "description": "Set when the change is in memory but did not reach disk. Named rather\nthan swallowed: the caller's provenance will not survive a restart, and\nsilence there is exactly the failure this registry exists to prevent.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "outcomes"
      ],
      "$defs": {
        "DomainDataOutcome": {
          "description": "What happened to one entry.\n\n`outcome` is one of `created`, `updated`, `validated`, `unknown`,\n`rejected`. The last two carry a discriminant rather than prose, so a caller\ncan branch instead of string-matching a sentence.",
          "type": "object",
          "properties": {
            "path": {
              "description": "The key as the caller spelled it, sigil included.",
              "type": "string"
            },
            "outcome": {
              "type": "string"
            },
            "cause": {
              "description": "`never_set` | `no_registry` | `undetermined` — present on `unknown`.\n\nThree, not two: `/logmon/first_seen` is written identically for a\nbrand-new domain and for one whose file was lost, so from the registry\nalone those are not distinguishable. `undetermined` is the honest\ndefault and `no_registry` is claimed only with a quarantine artifact.",
              "type": [
                "string",
                "null"
              ]
            },
            "reason": {
              "description": "`malformed_path` | `path_too_long` | `value_too_long` |\n`reserved_prefix` | `sigil_not_allowed_here` | `malformed_ttl` |\n`registry_full` — present on `rejected`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "path",
            "outcome"
          ]
        }
      }
    },
    "DomainDataRemove": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataRemove",
      "type": "object",
      "properties": {
        "patterns": {
          "description": "Prefix patterns, matched on **segment boundaries** — `/Versions` matches\n`/Versions/ht_server` but `/Ver` matches neither. There is no undo.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "patterns"
      ]
    },
    "DomainDataRemoveOutcome": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataRemoveOutcome",
      "type": "object",
      "properties": {
        "pattern": {
          "type": "string"
        },
        "removed": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "reason": {
          "description": "Present when the pattern was refused — notably `reserved_prefix`, which\ncovers the bare `/logmon` as well as `/logmon/version`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "pattern",
        "removed"
      ]
    },
    "DomainDataRemoveResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataRemoveResult",
      "type": "object",
      "properties": {
        "outcomes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainDataRemoveOutcome"
          }
        },
        "persist_error": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "outcomes"
      ],
      "$defs": {
        "DomainDataRemoveOutcome": {
          "type": "object",
          "properties": {
            "pattern": {
              "type": "string"
            },
            "removed": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "reason": {
              "description": "Present when the pattern was refused — notably `reserved_prefix`, which\ncovers the bare `/logmon` as well as `/logmon/version`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "pattern",
            "removed"
          ]
        }
      }
    },
    "DomainDataGet": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataGet",
      "type": "object",
      "properties": {
        "prefix": {
          "description": "Narrow to a subtree, on segment boundaries.",
          "type": [
            "string",
            "null"
          ]
        },
        "validated_before_secs": {
          "description": "Return only keys last confirmed before this many seconds ago — the\n\"what is stale\" question, as a filter over `get` rather than a tool of\nits own, so there are not two surfaces to keep in step.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "DomainDataKey": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataKey",
      "description": "One key as read back.",
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "created_at": {
          "description": "When THIS VALUE came into force. Moves only when the value changes, so a\nvalue confirmed daily for a month still reports the month.",
          "type": "string"
        },
        "validated_at": {
          "description": "When someone last confirmed it. Invariant: `>= created_at`.",
          "type": "string"
        },
        "age_secs": {
          "type": "integer",
          "format": "int64"
        },
        "ttl": {
          "type": [
            "string",
            "null"
          ]
        },
        "expired": {
          "description": "`true`/`false` only when a `ttl` was stated. **Absent means unknown, not\nfresh** — a key without a lifetime reports its age and no verdict, ever.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "path",
        "value",
        "created_at",
        "validated_at",
        "age_secs"
      ]
    },
    "DomainDataGetResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DomainDataGetResult",
      "type": "object",
      "properties": {
        "domain": {
          "type": "string"
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainDataKey"
          }
        },
        "total": {
          "description": "Total keys in the registry before `prefix`/`validated_before_secs`\nnarrowed it, so a filtered read cannot be mistaken for an empty one.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "missing_core": {
          "description": "Coverage against the recommended core set, named rather than counted:\nwhich of `/Build/commit`, `/Build/profile`, `/Action` are missing.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "domain",
        "keys",
        "total",
        "missing_core"
      ],
      "$defs": {
        "DomainDataKey": {
          "description": "One key as read back.",
          "type": "object",
          "properties": {
            "path": {
              "type": "string"
            },
            "value": {
              "type": "string"
            },
            "created_at": {
              "description": "When THIS VALUE came into force. Moves only when the value changes, so a\nvalue confirmed daily for a month still reports the month.",
              "type": "string"
            },
            "validated_at": {
              "description": "When someone last confirmed it. Invariant: `>= created_at`.",
              "type": "string"
            },
            "age_secs": {
              "type": "integer",
              "format": "int64"
            },
            "ttl": {
              "type": [
                "string",
                "null"
              ]
            },
            "expired": {
              "description": "`true`/`false` only when a `ttl` was stated. **Absent means unknown, not\nfresh** — a key without a lifetime reports its age and no verdict, ever.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "required": [
            "path",
            "value",
            "created_at",
            "validated_at",
            "age_secs"
          ]
        }
      }
    },
    "TriggerFiredPayload": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggerFiredPayload",
      "description": "Payload of `notifications/trigger_fired`.",
      "type": "object",
      "properties": {
        "trigger_id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "filter_string": {
          "type": "string"
        },
        "pre_window": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "post_window": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "notify_context": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "oneshot": {
          "description": "New in v1; mirrors the `oneshot` param on `triggers.add`. Tells the\nclient this is the only fire it will see for this trigger ID.",
          "type": "boolean",
          "default": false
        },
        "matched_entry": {
          "$ref": "#/$defs/LogEntry"
        },
        "context_before": {
          "description": "Context entries that arrived before the matched_entry. Size at most\n`notify_context`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/LogEntry"
          }
        }
      },
      "required": [
        "trigger_id",
        "filter_string",
        "pre_window",
        "post_window",
        "notify_context",
        "matched_entry"
      ],
      "$defs": {
        "LogEntry": {
          "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
          "type": "object",
          "properties": {
            "seq": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "level": {
              "$ref": "#/$defs/Level"
            },
            "message": {
              "type": "string"
            },
            "full_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "host": {
              "type": "string"
            },
            "facility": {
              "type": [
                "string",
                "null"
              ]
            },
            "file": {
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "additional_fields": {
              "type": "object",
              "additionalProperties": true
            },
            "trace_id": {
              "description": "Trace id rendered as 32-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "span_id": {
              "description": "Span id rendered as 16-char lowercase hex.",
              "type": [
                "string",
                "null"
              ]
            },
            "matched_filters": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "source": {
              "$ref": "#/$defs/LogSource",
              "default": "Filter"
            }
          },
          "required": [
            "seq",
            "timestamp",
            "level",
            "message",
            "host"
          ]
        },
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        }
      }
    },
    "LogEntry": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogEntry",
      "description": "A single log line as the daemon emits it. Mirrors `core::gelf::message::LogEntry`.",
      "type": "object",
      "properties": {
        "seq": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "level": {
          "$ref": "#/$defs/Level"
        },
        "message": {
          "type": "string"
        },
        "full_message": {
          "type": [
            "string",
            "null"
          ]
        },
        "host": {
          "type": "string"
        },
        "facility": {
          "type": [
            "string",
            "null"
          ]
        },
        "file": {
          "type": [
            "string",
            "null"
          ]
        },
        "line": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "additional_fields": {
          "type": "object",
          "additionalProperties": true
        },
        "trace_id": {
          "description": "Trace id rendered as 32-char lowercase hex.",
          "type": [
            "string",
            "null"
          ]
        },
        "span_id": {
          "description": "Span id rendered as 16-char lowercase hex.",
          "type": [
            "string",
            "null"
          ]
        },
        "matched_filters": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "source": {
          "$ref": "#/$defs/LogSource",
          "default": "Filter"
        }
      },
      "required": [
        "seq",
        "timestamp",
        "level",
        "message",
        "host"
      ],
      "$defs": {
        "Level": {
          "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
          "type": "string",
          "enum": [
            "Trace",
            "Debug",
            "Info",
            "Warn",
            "Error"
          ]
        },
        "LogSource": {
          "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
          "type": "string",
          "enum": [
            "Filter",
            "PreTrigger",
            "PostTrigger"
          ]
        }
      }
    },
    "Level": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Level",
      "description": "Log severity level. Wire format is the variant name as written\n(e.g. `\"Info\"`, `\"Error\"`), matching `core::gelf::message::Level`.",
      "type": "string",
      "enum": [
        "Trace",
        "Debug",
        "Info",
        "Warn",
        "Error"
      ]
    },
    "LogSource": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "LogSource",
      "description": "Origin of a log entry within the pipeline. Mirrors `core::gelf::message::LogSource`.",
      "type": "string",
      "enum": [
        "Filter",
        "PreTrigger",
        "PostTrigger"
      ]
    },
    "SpanEntry": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpanEntry",
      "description": "A single span. Mirrors `core::span::types::SpanEntry`.",
      "type": "object",
      "properties": {
        "seq": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "trace_id": {
          "description": "Trace id rendered as 32-char lowercase hex.",
          "type": "string"
        },
        "span_id": {
          "description": "Span id rendered as 16-char lowercase hex.",
          "type": "string"
        },
        "parent_span_id": {
          "description": "Parent span id rendered as 16-char lowercase hex (none for root spans).",
          "type": [
            "string",
            "null"
          ]
        },
        "start_time": {
          "type": "string",
          "format": "date-time"
        },
        "end_time": {
          "type": "string",
          "format": "date-time"
        },
        "duration_ms": {
          "type": "number",
          "format": "double"
        },
        "name": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/SpanKind"
        },
        "service_name": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/SpanStatus"
        },
        "attributes": {
          "type": "object",
          "additionalProperties": true
        },
        "events": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SpanEvent"
          },
          "default": []
        }
      },
      "required": [
        "seq",
        "trace_id",
        "span_id",
        "start_time",
        "end_time",
        "duration_ms",
        "name",
        "kind",
        "service_name",
        "status"
      ],
      "$defs": {
        "SpanKind": {
          "description": "Span kind. Wire format is snake_case, matching `core::span::types::SpanKind`.",
          "type": "string",
          "enum": [
            "unspecified",
            "internal",
            "server",
            "client",
            "producer",
            "consumer"
          ]
        },
        "SpanStatus": {
          "description": "Span status. Wire format mirrors `core::span::types::SpanStatus`:\n`{\"type\":\"unset\"}`, `{\"type\":\"ok\"}`, `{\"type\":\"error\",\"message\":\"...\"}`.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "unset"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "ok"
                }
              },
              "required": [
                "type"
              ]
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "const": "error"
                },
                "message": {
                  "type": "string"
                }
              },
              "required": [
                "type",
                "message"
              ]
            }
          ]
        },
        "SpanEvent": {
          "description": "A timestamped event attached to a span. Mirrors `core::span::types::SpanEvent`.",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "timestamp": {
              "type": "string",
              "format": "date-time"
            },
            "attributes": {
              "type": "object",
              "additionalProperties": true
            }
          },
          "required": [
            "name",
            "timestamp"
          ]
        }
      }
    },
    "SpanKind": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpanKind",
      "description": "Span kind. Wire format is snake_case, matching `core::span::types::SpanKind`.",
      "type": "string",
      "enum": [
        "unspecified",
        "internal",
        "server",
        "client",
        "producer",
        "consumer"
      ]
    },
    "SpanStatus": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpanStatus",
      "description": "Span status. Wire format mirrors `core::span::types::SpanStatus`:\n`{\"type\":\"unset\"}`, `{\"type\":\"ok\"}`, `{\"type\":\"error\",\"message\":\"...\"}`.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "unset"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "ok"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "error"
            },
            "message": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "message"
          ]
        }
      ]
    },
    "SpanEvent": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SpanEvent",
      "description": "A timestamped event attached to a span. Mirrors `core::span::types::SpanEvent`.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "attributes": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "required": [
        "name",
        "timestamp"
      ]
    },
    "TraceSummary": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TraceSummary",
      "description": "A high-level summary of a single trace. Mirrors `core::span::types::TraceSummary`.",
      "type": "object",
      "properties": {
        "trace_id": {
          "description": "Trace id rendered as 32-char lowercase hex.",
          "type": "string"
        },
        "root_span_name": {
          "type": "string"
        },
        "service_name": {
          "type": "string"
        },
        "start_time": {
          "type": "string",
          "format": "date-time"
        },
        "total_duration_ms": {
          "type": "number",
          "format": "double"
        },
        "span_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "has_errors": {
          "type": "boolean"
        },
        "linked_log_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "trace_id",
        "root_span_name",
        "service_name",
        "start_time",
        "total_duration_ms",
        "span_count",
        "has_errors",
        "linked_log_count"
      ]
    },
    "TraceSummaryBreakdownEntry": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TraceSummaryBreakdownEntry",
      "description": "One row in `TracesSummaryResult.breakdown`: the contribution of one direct\nchild of the root span to the trace's wall-clock time.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "self_time_ms": {
          "type": "number",
          "format": "double"
        },
        "total_time_ms": {
          "type": "number",
          "format": "double"
        },
        "percentage": {
          "type": "number",
          "format": "double"
        },
        "is_error": {
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "self_time_ms",
        "total_time_ms",
        "percentage",
        "is_error"
      ]
    },
    "TracesSlowGroup": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesSlowGroup",
      "description": "One row in a grouped `TracesSlowResult` (when `group_by == \"name\"`).\n\nThese statistics describe **every** stored span of this name that matched\nthe filter, not only the ones above `min_duration_ms`. The floor decides\nwhich names appear, not which spans are counted — so a name can qualify on\nits `max_ms` while its `avg_ms` sits far below the floor, and that gap is\nthe useful signal.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "avg_ms": {
          "type": "number",
          "format": "double"
        },
        "p50_ms": {
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "p95_ms": {
          "type": "number",
          "format": "double"
        },
        "max_ms": {
          "description": "The slowest span of this name. This is what the display floor tests.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "name",
        "avg_ms",
        "p95_ms",
        "count"
      ]
    },
    "CollectorsAdd": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsAdd",
      "type": "object",
      "properties": {
        "name": {
          "description": "Collector name. Letters, digits, '_' and '-' only.",
          "type": "string"
        },
        "filter": {
          "description": "Span filter DSL. Matched against every span in the domain this session\nis bound to *now*; the collector stays pinned to that domain afterwards.",
          "type": "string"
        },
        "level": {
          "description": "`scalar`, `timing`, or `tree` (default).",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "scalar",
            "timing",
            "tree",
            null
          ]
        },
        "group_keys": {
          "description": "Span attributes to split the numbers by. Values are read directly, so\nbooleans and numbers work — unlike the filter path, which only sees\nstrings.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "description": {
          "description": "Why this collector exists. Returned with every read, so a result found\nlater still carries its context.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_sample_bytes": {
          "description": "Per-collector retained-sample budget in bytes (default 64 MiB).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "threshold": {
          "description": "A rolling guard over this collector's matched spans (§8).",
          "anyOf": [
            {
              "$ref": "#/$defs/ThresholdSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "data": {
          "description": "Provenance to record while arming: `domain_data.update` entries, applied\nto this call's domain. Same validation, same per-entry outcomes, same\n`/logmon/` guard, same caps — one implementation, not a second store.\n\nA shorthand and nothing more. It is **not** persisted with the\ncollector, does not appear in its definition, and `collectors.edit` does\nnot touch it, because the registry already holds it. It exists because\nrecording what the project was *while you are already describing what\nyou are measuring* costs one parameter, where a separate call costs a\ndecision — and the decision is the part that does not get made.\n\nA `@`-sigilled key is **rejected**: the sigil scopes a fact to one\ndocument, and arming a collector writes none.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/DomainDataEntry"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "filter"
      ],
      "$defs": {
        "ThresholdSpec": {
          "description": "A rolling threshold as declared (§8).\n\nEvaluated over a window **advanced by span arrival, never by a clock**, so an\nidle collector costs nothing — and so a breached threshold on a finished run\nneither fires again nor clears. That is the right behaviour for a load-time\nguard and the wrong one for a liveness check, which is why every report\ncarries the note saying so.",
          "type": "object",
          "properties": {
            "metric": {
              "description": "`count`, `total_ms`, `avg_ms`, `error_count` or `error_rate_pct`.\n\n**Percentiles are deliberately absent.** A rolling percentile needs a\nduration sketch per bucket, and this design's per-collector memory is\nbounded precisely so a collector cannot grow without limit. Use `avg_ms`\nfor the guard and read the real percentiles with `collectors.get`.",
              "type": "string",
              "enum": [
                "count",
                "total_ms",
                "avg_ms",
                "error_count",
                "error_rate_pct"
              ]
            },
            "group": {
              "description": "Restrict to one group value. Requires the collector to declare\n`group_keys`.",
              "type": [
                "string",
                "null"
              ]
            },
            "op": {
              "description": "`gt`, `gte`, `lt` or `lte` — and the symbolic spellings `>`, `>=`, `<`\nand `<=`, which the parser has always accepted (`threshold.rs:120`) and\nthis doc comment used to omit. The word forms are canonical; the symbols\nare listed because a schema narrower than the daemon rejects calls the\ndaemon would have served.",
              "type": "string",
              "enum": [
                "gt",
                ">",
                "gte",
                ">=",
                "lt",
                "<",
                "lte",
                "<="
              ]
            },
            "value": {
              "type": "number",
              "format": "double"
            },
            "window_ms": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": [
            "metric",
            "op",
            "value",
            "window_ms"
          ]
        },
        "DomainDataEntry": {
          "description": "One requested change to the registry.\n\n**Absent ≡ `null` ≡ unchanged, for both `value` and `ttl`**, and the two\nshare one rule deliberately: two optional fields on one object with opposite\nabsent semantics is the footgun this surface exists to avoid, and a client\nserialising a missing field as `null` would otherwise mean the opposite of\nwhat it sent. Erasure is `domain_data.remove`, never `value: null`.",
          "type": "object",
          "properties": {
            "path": {
              "description": "A registry path — `/Build/commit`. A leading `@` scopes the fact to one\ncase document instead of the domain, and is accepted only where a\ndocument exists to receive it.",
              "type": "string"
            },
            "value": {
              "description": "Present → set. Absent → validate only, which never creates.",
              "type": [
                "string",
                "null"
              ]
            },
            "ttl": {
              "description": "A duration literal (`30s`, `5m`, `2h`, `7d`, `4w`), or `false` to clear\none in place. Absent leaves any existing lifetime untouched.\n\n`false` rather than `null` carries the clear because `null` is already\nspoken for above. This is the opposite convention to\n`CollectorsEdit.threshold`, where `null` removes — noted because the two\nlive in one daemon and the difference is deliberate rather than drift."
            }
          },
          "additionalProperties": false,
          "required": [
            "path"
          ]
        }
      }
    },
    "CollectorsAddResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsAddResult",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "filter": {
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "group_keys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "domain": {
          "description": "The domain the collector is pinned to. Rebinding the session later does\nnot move it.",
          "type": "string"
        },
        "max_sample_bytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "warnings": {
          "description": "Filters that are legal but probably not what was meant — a mistyped\nselector, a status match that also admits errors, a no-op threshold.\nNever a refusal: each of these collects *something*.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "data_outcomes": {
          "description": "One outcome per `data` entry, in the order sent — exactly what\n`domain_data.update` returns, so an agent learns one vocabulary rather\nthan two. Absent when no `data` was sent.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/DomainDataOutcome"
          }
        },
        "data_persist_error": {
          "description": "Set when `data` reached memory but not disk. Named rather than\nswallowed, for the reason `domain_data.update` names it: provenance that\nwill not survive a restart, reported as success, is the exact failure\nthe registry exists to prevent.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "filter",
        "level",
        "domain",
        "max_sample_bytes"
      ],
      "$defs": {
        "DomainDataOutcome": {
          "description": "What happened to one entry.\n\n`outcome` is one of `created`, `updated`, `validated`, `unknown`,\n`rejected`. The last two carry a discriminant rather than prose, so a caller\ncan branch instead of string-matching a sentence.",
          "type": "object",
          "properties": {
            "path": {
              "description": "The key as the caller spelled it, sigil included.",
              "type": "string"
            },
            "outcome": {
              "type": "string"
            },
            "cause": {
              "description": "`never_set` | `no_registry` | `undetermined` — present on `unknown`.\n\nThree, not two: `/logmon/first_seen` is written identically for a\nbrand-new domain and for one whose file was lost, so from the registry\nalone those are not distinguishable. `undetermined` is the honest\ndefault and `no_registry` is claimed only with a quarantine artifact.",
              "type": [
                "string",
                "null"
              ]
            },
            "reason": {
              "description": "`malformed_path` | `path_too_long` | `value_too_long` |\n`reserved_prefix` | `sigil_not_allowed_here` | `malformed_ttl` |\n`registry_full` — present on `rejected`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "path",
            "outcome"
          ]
        }
      }
    },
    "CollectorsList": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsList",
      "type": "object",
      "additionalProperties": false
    },
    "CollectorsListResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsListResult",
      "type": "object",
      "properties": {
        "collectors": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CollectorInfo"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "reserved_bytes": {
          "description": "Sample bytes reserved across every armed collector, daemon-wide.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "collectors",
        "count",
        "reserved_bytes"
      ],
      "$defs": {
        "CollectorInfo": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "filter": {
              "type": "string"
            },
            "level": {
              "type": "string"
            },
            "group_keys": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "domain": {
              "type": "string"
            },
            "matched": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "armed_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "zeroed_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "zeroed_by": {
              "description": "Why the live window is empty, when there is a reason: `daemon_restart`,\n`reset`, or `edit`. Absent means nothing has zeroed it — so a caller\nseeing `matched: 0` can tell \"no traffic yet\" from \"the run you were\nmeasuring did not survive\".",
              "type": [
                "string",
                "null"
              ]
            },
            "snapshots": {
              "description": "Recorded runs held for this collector.",
              "type": "integer",
              "format": "uint",
              "minimum": 0,
              "default": 0
            },
            "sample_complete": {
              "description": "`false` once the sample budget stopped retention.",
              "type": "boolean"
            },
            "orphaned": {
              "description": "The pinned domain no longer exists, so this collector can never match\nanother span. The normal outcome of a restart for one armed on an\nephemeral domain, since those are not re-created.",
              "type": "boolean"
            },
            "orphan_note": {
              "type": [
                "string",
                "null"
              ]
            },
            "threshold": {
              "description": "The armed threshold and whether it is currently breached (§8).",
              "anyOf": [
                {
                  "$ref": "#/$defs/ThresholdInfo"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "name",
            "filter",
            "level",
            "domain",
            "matched",
            "sample_complete"
          ]
        },
        "ThresholdInfo": {
          "description": "A threshold and its current verdict, as `collectors.list` and\n`collectors.get` report it.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "group": {
              "type": [
                "string",
                "null"
              ]
            },
            "op": {
              "type": "string"
            },
            "value": {
              "type": "number",
              "format": "double"
            },
            "window_ms": {
              "description": "The window as declared.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "effective_window_ms": {
              "description": "The window as **evaluated**: the declared width rounded up to a whole\nnumber of ring buckets, so it is never narrower than what was asked for.\nEqual to `window_ms` when that is a multiple of the bucket count.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "breached": {
              "type": "boolean"
            },
            "fires": {
              "description": "Clear-to-breached **transitions**, not evaluations: a threshold breached\nfor a whole run has fired once, which is what \"did this happen\" means.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "last_value": {
              "description": "The metric's value at the last evaluation. **Absent until one has\nhappened — unknown, not zero**, because zero is a value `count` can\nlegitimately hold and an `lt` threshold would read it as a breach.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "evaluated": {
              "description": "Whether any span has yet been evaluated against this threshold.",
              "type": "boolean"
            },
            "note": {
              "description": "Carried with every report, because it is the one thing about this\nmechanism that surprises people.",
              "type": "string"
            }
          },
          "required": [
            "metric",
            "op",
            "value",
            "window_ms",
            "breached",
            "fires",
            "evaluated",
            "note"
          ]
        }
      }
    },
    "CollectorInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorInfo",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "filter": {
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "group_keys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "domain": {
          "type": "string"
        },
        "matched": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "armed_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "zeroed_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "zeroed_by": {
          "description": "Why the live window is empty, when there is a reason: `daemon_restart`,\n`reset`, or `edit`. Absent means nothing has zeroed it — so a caller\nseeing `matched: 0` can tell \"no traffic yet\" from \"the run you were\nmeasuring did not survive\".",
          "type": [
            "string",
            "null"
          ]
        },
        "snapshots": {
          "description": "Recorded runs held for this collector.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "sample_complete": {
          "description": "`false` once the sample budget stopped retention.",
          "type": "boolean"
        },
        "orphaned": {
          "description": "The pinned domain no longer exists, so this collector can never match\nanother span. The normal outcome of a restart for one armed on an\nephemeral domain, since those are not re-created.",
          "type": "boolean"
        },
        "orphan_note": {
          "type": [
            "string",
            "null"
          ]
        },
        "threshold": {
          "description": "The armed threshold and whether it is currently breached (§8).",
          "anyOf": [
            {
              "$ref": "#/$defs/ThresholdInfo"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "name",
        "filter",
        "level",
        "domain",
        "matched",
        "sample_complete"
      ],
      "$defs": {
        "ThresholdInfo": {
          "description": "A threshold and its current verdict, as `collectors.list` and\n`collectors.get` report it.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "group": {
              "type": [
                "string",
                "null"
              ]
            },
            "op": {
              "type": "string"
            },
            "value": {
              "type": "number",
              "format": "double"
            },
            "window_ms": {
              "description": "The window as declared.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "effective_window_ms": {
              "description": "The window as **evaluated**: the declared width rounded up to a whole\nnumber of ring buckets, so it is never narrower than what was asked for.\nEqual to `window_ms` when that is a multiple of the bucket count.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "breached": {
              "type": "boolean"
            },
            "fires": {
              "description": "Clear-to-breached **transitions**, not evaluations: a threshold breached\nfor a whole run has fired once, which is what \"did this happen\" means.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "last_value": {
              "description": "The metric's value at the last evaluation. **Absent until one has\nhappened — unknown, not zero**, because zero is a value `count` can\nlegitimately hold and an `lt` threshold would read it as a breach.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "evaluated": {
              "description": "Whether any span has yet been evaluated against this threshold.",
              "type": "boolean"
            },
            "note": {
              "description": "Carried with every report, because it is the one thing about this\nmechanism that surprises people.",
              "type": "string"
            }
          },
          "required": [
            "metric",
            "op",
            "value",
            "window_ms",
            "breached",
            "fires",
            "evaluated",
            "note"
          ]
        }
      }
    },
    "CollectorsGet": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsGet",
      "type": "object",
      "properties": {
        "name": {
          "description": "The collector to read.",
          "type": "string"
        },
        "snapshot": {
          "description": "Read a recorded snapshot by label instead of the live window.",
          "type": [
            "string",
            "null"
          ]
        },
        "group_by": {
          "description": "`name`, `group`, `trace`, or `path`. `none` and `\"\"` are also accepted\nand mean the ungrouped overall figures — the same as omitting it\n(`project.rs:74`).",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "name",
            "group",
            "trace",
            "path",
            "none",
            "",
            null
          ]
        },
        "skip_warmup_ms": {
          "description": "Exclude spans starting within this many ms of the first matched span.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "top_n": {
          "description": "Rows returned in the breakdown (default 20).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "CollectorsName": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsName",
      "description": "`collectors.reset` / `collectors.remove`.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ]
    },
    "CollectorsResetResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsResetResult",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "discarded": {
          "$ref": "#/$defs/CollectorsDiscarded"
        }
      },
      "required": [
        "name",
        "discarded"
      ],
      "$defs": {
        "CollectorsDiscarded": {
          "description": "What a reset threw away. Returned so a reset issued one call too early is\nnot silently unrecoverable.",
          "type": "object",
          "properties": {
            "matched": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "total_ms": {
              "type": "number",
              "format": "double"
            },
            "error_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "window_start": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            }
          },
          "required": [
            "matched",
            "total_ms",
            "error_count"
          ]
        }
      }
    },
    "CollectorsDiscarded": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsDiscarded",
      "description": "What a reset threw away. Returned so a reset issued one call too early is\nnot silently unrecoverable.",
      "type": "object",
      "properties": {
        "matched": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "total_ms": {
          "type": "number",
          "format": "double"
        },
        "error_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "window_start": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        }
      },
      "required": [
        "matched",
        "total_ms",
        "error_count"
      ]
    },
    "CollectorsRemoveResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsRemoveResult",
      "type": "object",
      "properties": {
        "removed": {
          "type": "string"
        },
        "reserved_bytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "removed",
        "reserved_bytes"
      ]
    },
    "TracesProfile": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TracesProfile",
      "type": "object",
      "properties": {
        "filter": {
          "description": "Span filter DSL. Default \"ALL\".",
          "type": [
            "string",
            "null"
          ]
        },
        "group_by": {
          "description": "`name`, `group`, `trace`, or `path`; `none`, `\"\"` or omitted for the\nungrouped figures. The same axes `collectors.get` takes — both read\nthrough `profile_options` (`rpc_handler.rs:2314`), so they cannot drift.",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "name",
            "group",
            "trace",
            "path",
            "none",
            "",
            null
          ]
        },
        "group_keys": {
          "description": "Span attributes to split by. Required for group_by \"group\".",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "skip_warmup_ms": {
          "description": "Exclude spans starting within this many ms of the first matched span.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "top_n": {
          "description": "Rows returned in the breakdown (default 20).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "CollectorsEdit": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsEdit",
      "description": "An edit (§7.1). `description` is free; **any other field discards the live\nwindow** and re-runs every gate `collectors.add` runs. Recorded snapshots\nare untouched and each keeps the definition it was taken under.",
      "type": "object",
      "properties": {
        "name": {
          "description": "The collector to change.",
          "type": "string"
        },
        "description": {
          "description": "Free — changes nothing about what is collected.",
          "type": [
            "string",
            "null"
          ]
        },
        "filter": {
          "description": "Any of the fields below DISCARDS the live window. Recorded snapshots are\nuntouched and each keeps the definition it was taken under.",
          "type": [
            "string",
            "null"
          ]
        },
        "level": {
          "description": "`scalar`, `timing` or `tree`. Permitted in **both** directions: a `tree`\ncollector heading for the sample cap mid-run can drop to `timing` for\n2.5× the records, which is the only remedy left once the daemon-wide\nreservation is exhausted.",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "scalar",
            "timing",
            "tree",
            null
          ]
        },
        "group_keys": {
          "description": "Replace the declared group keys. A structural change: it zeroes the live\nwindow.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "max_sample_bytes": {
          "description": "Replace the per-collector sample budget. A structural change: it zeroes\nthe live window.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "domain": {
          "description": "Re-pin to another domain. Only while the collector is zeroed, so a\nrecorded window and the domain it was measured on cannot disagree. This\nis the remedy for a collector orphaned by a restart.",
          "type": [
            "string",
            "null"
          ]
        },
        "threshold": {
          "description": "Set or replace the rolling guard. Pass JSON `null` to remove one — which\nis why this is doubly optional: \"leave it alone\" and \"take it away\" are\ndifferent requests and a single `Option` cannot express both.\n\nLike every other structural field, changing it **zeroes the live\nwindow**: the rolling ring is measurement state, and evaluating a new\nlimit over a window accumulated under the old one would breach for\nreasons unrelated to load.",
          "anyOf": [
            {
              "$ref": "#/$defs/ThresholdSpec"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "$defs": {
        "ThresholdSpec": {
          "description": "A rolling threshold as declared (§8).\n\nEvaluated over a window **advanced by span arrival, never by a clock**, so an\nidle collector costs nothing — and so a breached threshold on a finished run\nneither fires again nor clears. That is the right behaviour for a load-time\nguard and the wrong one for a liveness check, which is why every report\ncarries the note saying so.",
          "type": "object",
          "properties": {
            "metric": {
              "description": "`count`, `total_ms`, `avg_ms`, `error_count` or `error_rate_pct`.\n\n**Percentiles are deliberately absent.** A rolling percentile needs a\nduration sketch per bucket, and this design's per-collector memory is\nbounded precisely so a collector cannot grow without limit. Use `avg_ms`\nfor the guard and read the real percentiles with `collectors.get`.",
              "type": "string",
              "enum": [
                "count",
                "total_ms",
                "avg_ms",
                "error_count",
                "error_rate_pct"
              ]
            },
            "group": {
              "description": "Restrict to one group value. Requires the collector to declare\n`group_keys`.",
              "type": [
                "string",
                "null"
              ]
            },
            "op": {
              "description": "`gt`, `gte`, `lt` or `lte` — and the symbolic spellings `>`, `>=`, `<`\nand `<=`, which the parser has always accepted (`threshold.rs:120`) and\nthis doc comment used to omit. The word forms are canonical; the symbols\nare listed because a schema narrower than the daemon rejects calls the\ndaemon would have served.",
              "type": "string",
              "enum": [
                "gt",
                ">",
                "gte",
                ">=",
                "lt",
                "<",
                "lte",
                "<="
              ]
            },
            "value": {
              "type": "number",
              "format": "double"
            },
            "window_ms": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": [
            "metric",
            "op",
            "value",
            "window_ms"
          ]
        }
      }
    },
    "CollectorsEditResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsEditResult",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "filter": {
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "group_keys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "domain": {
          "type": "string"
        },
        "max_sample_bytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "zeroed": {
          "description": "Whether the live window was discarded. History never is.",
          "type": "boolean"
        },
        "note": {
          "type": "string"
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "name",
        "filter",
        "level",
        "domain",
        "max_sample_bytes",
        "zeroed",
        "note"
      ]
    },
    "CollectorsSnapshot": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsSnapshot",
      "type": "object",
      "properties": {
        "name": {
          "description": "The collector to record.",
          "type": "string"
        },
        "label": {
          "description": "Unique per collector, `[A-Za-z0-9._-]`. Omitted → `snapshot-<n>`, and\n`n` never repeats over a collector's life even after eviction.",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "What this run was. Recorded with the data, so a number found weeks\nlater still says what it measured.",
          "type": [
            "string",
            "null"
          ]
        },
        "meta": {
          "description": "Provenance logmon cannot infer — commit, build profile, config. Free\nform, and per-snapshot because two arms of a comparison differ in it."
        },
        "reset": {
          "description": "Zero the collector and start a fresh window. Default `true` — the\npoint of a snapshot is usually to end one run and begin the next.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "per_name": {
          "description": "Keep the per-span-name breakdown in the recorded run. Default true. Per-\naxis breakdowns are what a later comparison splits by, and they are\ndropped on the way to disk when this is false — so the choice is made\nhere, once, and cannot be revisited from a recorded run.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "per_group": {
          "description": "Keep the per-group-key breakdown in the recorded run. Default true.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "projections": {
          "description": "Store the sample-derived figures. Computed now or never — the samples\nthemselves are not retained.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "CollectorsHistory": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsHistory",
      "type": "object",
      "properties": {
        "name": {
          "description": "The collector whose recorded runs to list.",
          "type": "string"
        },
        "limit": {
          "description": "Most recent N. Omitted → all retained.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "merge": {
          "description": "Also return the snapshots combined into one exact tier. Sample-derived\nfigures do not merge and are omitted with a reason.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "CollectorsHistoryResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsHistoryResult",
      "type": "object",
      "properties": {
        "collector": {
          "type": "string"
        },
        "snapshots": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SnapshotSummary"
          }
        },
        "count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "evicted": {
          "description": "Snapshots dropped to the retention cap. Reported rather than inferred:\na caller comparing \"the first run\" needs to know it is gone.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "merged": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileExact"
            },
            {
              "type": "null"
            }
          ]
        },
        "merged_estimated": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileEstimated"
            },
            {
              "type": "null"
            }
          ]
        },
        "floor": {
          "anyOf": [
            {
              "$ref": "#/$defs/RunToRunFloor"
            },
            {
              "type": "null"
            }
          ]
        },
        "suppressed": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Suppressed"
          }
        }
      },
      "required": [
        "collector",
        "snapshots",
        "count",
        "evicted"
      ],
      "$defs": {
        "SnapshotSummary": {
          "description": "One recorded run, as `collectors.history` lists it.",
          "type": "object",
          "properties": {
            "label": {
              "type": "string"
            },
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "meta": true,
            "taken_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "window_start": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "wall_ms": {
              "type": "number",
              "format": "double"
            },
            "filter": {
              "description": "The definition **as of this snapshot**, never the live collector's.",
              "type": "string"
            },
            "level": {
              "type": "string"
            },
            "group_keys": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "exact": {
              "$ref": "#/$defs/ProfileExact"
            },
            "estimated": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileEstimated"
                },
                {
                  "type": "null"
                }
              ]
            },
            "sampled": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileSampled"
                },
                {
                  "type": "null"
                }
              ]
            },
            "ingest": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileIngest"
                },
                {
                  "type": "null"
                }
              ]
            },
            "sample_complete": {
              "description": "`false` if the sample budget truncated during this window. Travels with\nthe record so a snapshot cannot launder a truncated run.",
              "type": "boolean"
            },
            "cardinality_capped": {
              "type": "boolean"
            },
            "durable": {
              "description": "Absent means durable. `false` means the run is in this response and in\nthe daemon's memory but was not written, so it will not survive a\nrestart — which matters to anyone collecting runs to compare later.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "durability_warning": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "label",
            "wall_ms",
            "filter",
            "level",
            "exact",
            "sample_complete"
          ]
        },
        "ProfileExact": {
          "description": "The always-exact tier. Never capped by the sample budget.",
          "type": "object",
          "properties": {
            "count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "total_ms": {
              "type": "number",
              "format": "double"
            },
            "avg_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "min_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "max_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "error_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "out_of_range_spans": {
              "description": "Well-formed spans longer than the sketch's declared range: counted in\nfull toward `total_ms`, clamped in the distribution.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "count",
            "total_ms",
            "error_count",
            "out_of_range_spans"
          ]
        },
        "ProfileEstimated": {
          "description": "Percentiles from the sketch — bounded relative error, unbounded population.",
          "type": "object",
          "properties": {
            "axis": {
              "description": "Which population this sketch covers: `collector`, `name`, or `group`.",
              "type": "string"
            },
            "alpha_pct": {
              "description": "The sketch's relative-accuracy guarantee, as a percentage.",
              "type": "number",
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            }
          },
          "required": [
            "axis",
            "alpha_pct"
          ]
        },
        "ProfileSampled": {
          "description": "Everything derived from retained per-span records. Exact over the records it\nholds — which is the whole population only while `complete` is true.",
          "type": "object",
          "properties": {
            "complete": {
              "description": "`false` once the sample budget stopped retention. The retained set is\nthen a contiguous prefix, not a sample of the whole run.",
              "type": "boolean"
            },
            "sample_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "self_ms": {
              "description": "Duration minus the union of matched children clipped to this span —\ntime spent in this span and nothing matched below it.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "nested_matches": {
              "description": "Matched spans whose parent is also matched. Zero means self time equals\ntotal time by construction, and `self_ms` is suppressed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "overlapping_child_ms": {
              "description": "Child time that lay outside its parent's interval and was clipped away.\nNon-zero means clock skew or instrumentation error, not concurrency.",
              "type": "number",
              "format": "double"
            },
            "overlapping_child_spans": {
              "description": "How many children needed clipping. Paired with the millisecond figure\nbecause one child off by a second and a thousand off by a millisecond\nwant opposite remedies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "wall_union_ms": {
              "description": "Elapsed time during which at least one matched span was in flight.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "achieved_concurrency": {
              "description": "Total matched work divided by the wall time it occupied. 1.0 is fully\nserial; 4.0 means four matched spans in flight on average.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "stddev_ms": {
              "description": "Sample standard deviation (Bessel-corrected, `n-1`) over exactly the\ndurations the percentiles beside it were computed from — the same\npopulation, after any warm-up cut.\n\nAbsent below two records, where it is undefined rather than zero. Also\nabsent on `group_by: path` rows, which hold a count and a self-time sum\nrather than the per-span durations either figure needs; there, absence\nsays nothing about how many records the row covers.\n\nThe `n-1` form because at the sizes this serves the population form is\nabout 18% smaller at three records (it is smaller by `sqrt((n-1)/n)`).\nNote that is the gap between the two *formulas*: both still estimate the\ntrue spread from very few samples, and at three records the estimate is\nitself uncertain to roughly ±45%. This is a description of the observed\nspread, not a significance test — comparing two three-run means properly\nneeds a difference of roughly 2.3 standard deviations, not one.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "durations_ms": {
              "description": "Every retained duration, in **arrival order**, when the sample is\ncomplete and holds at most 50 records.\n\nArrival order rather than sorted: a reader can sort these but could not\nunsort them, and the order carries drift across a run — first-call\neffects, a cache filling — which every other figure here discards.\n`durations_ms[0]` is the FIRST duration, not the smallest.\n\nAbsent when the sample was truncated (a prefix of a run presented as its\ndurations would invite exactly the per-value reasoning a biased subset\ncannot support), and absent above 50 records. On a live read `suppressed`\nnames which of the two applies.\n\nTwo further absences carry no `suppressed` entry at all, because their\nresponses have no such channel. A recorded run read by label serves the\nprojection as it stood when the snapshot was taken; and any response that\nLISTS runs — `collectors.history`, and the `collectors.snapshot` reply —\ndrops the durations whatever their size, because a bound written for one\nblock would otherwise multiply by the number of runs returned. Read a\nsingle run by label to get them.\n\nOnly the top-level `sampled` block populates this. Group rows leave it\nabsent regardless of their size: it is the population the headline\nfigures describe, and per-row lists would multiply it by `top_n`.",
              "type": [
                "array",
                "null"
              ],
              "items": {
                "type": "number",
                "format": "double"
              }
            }
          },
          "required": [
            "complete",
            "sample_count",
            "nested_matches",
            "overlapping_child_ms",
            "overlapping_child_spans"
          ]
        },
        "ProfileIngest": {
          "description": "Span loss during the window, and the collector's own view of unusable input.\n\n`attribution` is `\"domain\"`, always: the transport counters are per-domain\nand unfiltered, so most of what they count may be spans this collector's\nfilter would never have matched. That makes this sound as a trigger for\nmarking a result untrustworthy, and wrong as a statement about how many\nmatches went missing.",
          "type": "object",
          "properties": {
            "drops_in_window": {
              "description": "Per-span channel-full drops on the two OTLP trace transports.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shed_batches": {
              "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_dropped": {
              "description": "Spans discarded at parse time for unusable identity.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_timestamps": {
              "description": "Matched spans whose timestamps this collector could not use.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "negative_duration_spans": {
              "description": "Matched spans that ended before they started.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "attribution": {
              "type": "string"
            }
          },
          "required": [
            "drops_in_window",
            "shed_batches",
            "malformed_dropped",
            "malformed_timestamps",
            "negative_duration_spans",
            "attribution"
          ]
        },
        "RunToRunFloor": {
          "description": "Spread across repeats of one configuration.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "runs": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "min": {
              "type": "number",
              "format": "double"
            },
            "max": {
              "type": "number",
              "format": "double"
            },
            "mean": {
              "type": "number",
              "format": "double"
            },
            "cv_pct": {
              "description": "Coefficient of variation, as a percentage. **Absent for a single run —\nunknown, not zero.** A floor of zero would license calling any\ndifference significant.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "caveat": {
              "description": "What this figure does *not* cover. Carried with it because a bare\nspread invites being read as a total error bar.",
              "type": "string"
            }
          },
          "required": [
            "metric",
            "runs",
            "min",
            "max",
            "mean",
            "caveat"
          ]
        },
        "Suppressed": {
          "description": "Why a field that normally carries a number is `null`.\n\nA profile has several ways to be legitimately unable to answer — the\nretention level is too low, a warm-up cut makes an unwindowed tier\nmeaningless, nothing nested so self time carries no information. Returning\n`null` alone would leave the reader to guess which, and the guesses differ\nin what they imply about the run. Every suppression names its field and its\nreason so a consumer can distinguish \"not measured\" from \"measured as zero\".",
          "type": "object",
          "properties": {
            "field": {
              "description": "Dotted path of the suppressed field, e.g. `sampled.self_ms`.",
              "type": "string"
            },
            "reason": {
              "type": "string"
            },
            "remedy": {
              "description": "What the caller could change to get a number instead. Absent when\nnothing would help.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "field",
            "reason"
          ]
        }
      }
    },
    "SnapshotSummary": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SnapshotSummary",
      "description": "One recorded run, as `collectors.history` lists it.",
      "type": "object",
      "properties": {
        "label": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "meta": true,
        "taken_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "window_start": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "wall_ms": {
          "type": "number",
          "format": "double"
        },
        "filter": {
          "description": "The definition **as of this snapshot**, never the live collector's.",
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "group_keys": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "exact": {
          "$ref": "#/$defs/ProfileExact"
        },
        "estimated": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileEstimated"
            },
            {
              "type": "null"
            }
          ]
        },
        "sampled": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileSampled"
            },
            {
              "type": "null"
            }
          ]
        },
        "ingest": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileIngest"
            },
            {
              "type": "null"
            }
          ]
        },
        "sample_complete": {
          "description": "`false` if the sample budget truncated during this window. Travels with\nthe record so a snapshot cannot launder a truncated run.",
          "type": "boolean"
        },
        "cardinality_capped": {
          "type": "boolean"
        },
        "durable": {
          "description": "Absent means durable. `false` means the run is in this response and in\nthe daemon's memory but was not written, so it will not survive a\nrestart — which matters to anyone collecting runs to compare later.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "durability_warning": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "label",
        "wall_ms",
        "filter",
        "level",
        "exact",
        "sample_complete"
      ],
      "$defs": {
        "ProfileExact": {
          "description": "The always-exact tier. Never capped by the sample budget.",
          "type": "object",
          "properties": {
            "count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "total_ms": {
              "type": "number",
              "format": "double"
            },
            "avg_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "min_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "max_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "error_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "out_of_range_spans": {
              "description": "Well-formed spans longer than the sketch's declared range: counted in\nfull toward `total_ms`, clamped in the distribution.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "count",
            "total_ms",
            "error_count",
            "out_of_range_spans"
          ]
        },
        "ProfileEstimated": {
          "description": "Percentiles from the sketch — bounded relative error, unbounded population.",
          "type": "object",
          "properties": {
            "axis": {
              "description": "Which population this sketch covers: `collector`, `name`, or `group`.",
              "type": "string"
            },
            "alpha_pct": {
              "description": "The sketch's relative-accuracy guarantee, as a percentage.",
              "type": "number",
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            }
          },
          "required": [
            "axis",
            "alpha_pct"
          ]
        },
        "ProfileSampled": {
          "description": "Everything derived from retained per-span records. Exact over the records it\nholds — which is the whole population only while `complete` is true.",
          "type": "object",
          "properties": {
            "complete": {
              "description": "`false` once the sample budget stopped retention. The retained set is\nthen a contiguous prefix, not a sample of the whole run.",
              "type": "boolean"
            },
            "sample_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "self_ms": {
              "description": "Duration minus the union of matched children clipped to this span —\ntime spent in this span and nothing matched below it.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "nested_matches": {
              "description": "Matched spans whose parent is also matched. Zero means self time equals\ntotal time by construction, and `self_ms` is suppressed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "overlapping_child_ms": {
              "description": "Child time that lay outside its parent's interval and was clipped away.\nNon-zero means clock skew or instrumentation error, not concurrency.",
              "type": "number",
              "format": "double"
            },
            "overlapping_child_spans": {
              "description": "How many children needed clipping. Paired with the millisecond figure\nbecause one child off by a second and a thousand off by a millisecond\nwant opposite remedies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "wall_union_ms": {
              "description": "Elapsed time during which at least one matched span was in flight.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "achieved_concurrency": {
              "description": "Total matched work divided by the wall time it occupied. 1.0 is fully\nserial; 4.0 means four matched spans in flight on average.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "stddev_ms": {
              "description": "Sample standard deviation (Bessel-corrected, `n-1`) over exactly the\ndurations the percentiles beside it were computed from — the same\npopulation, after any warm-up cut.\n\nAbsent below two records, where it is undefined rather than zero. Also\nabsent on `group_by: path` rows, which hold a count and a self-time sum\nrather than the per-span durations either figure needs; there, absence\nsays nothing about how many records the row covers.\n\nThe `n-1` form because at the sizes this serves the population form is\nabout 18% smaller at three records (it is smaller by `sqrt((n-1)/n)`).\nNote that is the gap between the two *formulas*: both still estimate the\ntrue spread from very few samples, and at three records the estimate is\nitself uncertain to roughly ±45%. This is a description of the observed\nspread, not a significance test — comparing two three-run means properly\nneeds a difference of roughly 2.3 standard deviations, not one.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "durations_ms": {
              "description": "Every retained duration, in **arrival order**, when the sample is\ncomplete and holds at most 50 records.\n\nArrival order rather than sorted: a reader can sort these but could not\nunsort them, and the order carries drift across a run — first-call\neffects, a cache filling — which every other figure here discards.\n`durations_ms[0]` is the FIRST duration, not the smallest.\n\nAbsent when the sample was truncated (a prefix of a run presented as its\ndurations would invite exactly the per-value reasoning a biased subset\ncannot support), and absent above 50 records. On a live read `suppressed`\nnames which of the two applies.\n\nTwo further absences carry no `suppressed` entry at all, because their\nresponses have no such channel. A recorded run read by label serves the\nprojection as it stood when the snapshot was taken; and any response that\nLISTS runs — `collectors.history`, and the `collectors.snapshot` reply —\ndrops the durations whatever their size, because a bound written for one\nblock would otherwise multiply by the number of runs returned. Read a\nsingle run by label to get them.\n\nOnly the top-level `sampled` block populates this. Group rows leave it\nabsent regardless of their size: it is the population the headline\nfigures describe, and per-row lists would multiply it by `top_n`.",
              "type": [
                "array",
                "null"
              ],
              "items": {
                "type": "number",
                "format": "double"
              }
            }
          },
          "required": [
            "complete",
            "sample_count",
            "nested_matches",
            "overlapping_child_ms",
            "overlapping_child_spans"
          ]
        },
        "ProfileIngest": {
          "description": "Span loss during the window, and the collector's own view of unusable input.\n\n`attribution` is `\"domain\"`, always: the transport counters are per-domain\nand unfiltered, so most of what they count may be spans this collector's\nfilter would never have matched. That makes this sound as a trigger for\nmarking a result untrustworthy, and wrong as a statement about how many\nmatches went missing.",
          "type": "object",
          "properties": {
            "drops_in_window": {
              "description": "Per-span channel-full drops on the two OTLP trace transports.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shed_batches": {
              "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_dropped": {
              "description": "Spans discarded at parse time for unusable identity.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_timestamps": {
              "description": "Matched spans whose timestamps this collector could not use.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "negative_duration_spans": {
              "description": "Matched spans that ended before they started.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "attribution": {
              "type": "string"
            }
          },
          "required": [
            "drops_in_window",
            "shed_batches",
            "malformed_dropped",
            "malformed_timestamps",
            "negative_duration_spans",
            "attribution"
          ]
        }
      }
    },
    "RunToRunFloor": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "RunToRunFloor",
      "description": "Spread across repeats of one configuration.",
      "type": "object",
      "properties": {
        "metric": {
          "type": "string"
        },
        "runs": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "min": {
          "type": "number",
          "format": "double"
        },
        "max": {
          "type": "number",
          "format": "double"
        },
        "mean": {
          "type": "number",
          "format": "double"
        },
        "cv_pct": {
          "description": "Coefficient of variation, as a percentage. **Absent for a single run —\nunknown, not zero.** A floor of zero would license calling any\ndifference significant.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "caveat": {
          "description": "What this figure does *not* cover. Carried with it because a bare\nspread invites being read as a total error bar.",
          "type": "string"
        }
      },
      "required": [
        "metric",
        "runs",
        "min",
        "max",
        "mean",
        "caveat"
      ]
    },
    "CollectorsDiff": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsDiff",
      "description": "`collectors.diff` — compare two arms (§5.6).\n\nAn arm is `<collector>` for the live window, `<collector>@<label>` for one\nrecorded run, or `<collector>@*` for every recorded run merged. The merged\nform is the only one that yields a run-to-run floor, which is what decides\nwhether a delta is a result or scheduling noise.",
      "type": "object",
      "properties": {
        "a": {
          "description": "Baseline arm: \"<collector>\" for the live window, \"<collector>@<label>\"\nfor one recorded run, or \"<collector>@*\" for every recorded run merged.",
          "type": "string"
        },
        "b": {
          "description": "The arm to compare against the baseline, same syntax.",
          "type": "string"
        },
        "group_by": {
          "description": "`name` or `group`. Not `trace` or `path`: both are projected from\nper-span records, and a recorded run does not retain those. `none` and\n`\"\"` are accepted for the ungrouped comparison, which is also the\ndefault when this is omitted (`diff.rs:77`).",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "name",
            "group",
            "none",
            "",
            null
          ]
        },
        "top_n": {
          "description": "Rows returned in the breakdown (default 20).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "allow_mismatch": {
          "description": "Compare arms whose filters matched different populations.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allow_lossy": {
          "description": "Compare when one arm lost spans and the other did not.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allow_truncated": {
          "description": "Compare when both arms hit the sample budget.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "a",
        "b"
      ]
    },
    "CollectorsDiffResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsDiffResult",
      "type": "object",
      "properties": {
        "a": {
          "$ref": "#/$defs/DiffArm"
        },
        "b": {
          "$ref": "#/$defs/DiffArm"
        },
        "level": {
          "description": "The level both arms can answer at — `min(level)`.",
          "type": "string"
        },
        "trustworthy": {
          "description": "False when any mark makes a number unsafe to act on, **or** when either\narm is a single run. A reader who reads nothing else should still not be\nmisled.",
          "type": "boolean"
        },
        "rows": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DiffRow"
          }
        },
        "grouped_by": {
          "type": [
            "string",
            "null"
          ]
        },
        "groups": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DiffGroup"
          }
        },
        "groups_total": {
          "description": "Comparable keys **before** `top_n` truncation, so a reader can tell \"the\ntop 15 of 15\" from \"the top 15 of 200\".",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 0
        },
        "marks": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DiffMark"
          }
        },
        "overflow_rows_suppressed": {
          "description": "How many group rows were dropped because a cardinality cap folded them\ninto `__overflow__` on at least one arm. A count rather than a flag: one\nfolded row and two hundred of them mean different things about whether\nthe breakdown below is worth reading.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 0
        },
        "suppressed": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Suppressed"
          }
        }
      },
      "required": [
        "a",
        "b",
        "level",
        "trustworthy",
        "rows"
      ],
      "$defs": {
        "DiffArm": {
          "description": "One side of a comparison, as the result describes it.",
          "type": "object",
          "properties": {
            "spec": {
              "description": "How the caller named it.",
              "type": "string"
            },
            "collector": {
              "type": "string"
            },
            "aggregation": {
              "description": "Which runs this figure came from, and how they were combined. **Always\npresent**: a reader looking at a single number cannot otherwise tell one\nrun from the mean of five, and the floor depends on which it is.",
              "type": "string"
            },
            "filter": {
              "description": "The definition **as of the measurement**, never the live collector's.",
              "type": "string"
            },
            "level": {
              "type": "string"
            },
            "group_keys": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "meta": {
              "description": "Provenance logmon cannot infer — commit, build profile, configuration."
            },
            "matched": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "wall_ms": {
              "type": "number",
              "format": "double"
            },
            "window_start": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "taken_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "nesting": {
              "description": "`detected`, `undetected`, or `unknown`.",
              "type": "string"
            },
            "sample_complete": {
              "type": "boolean"
            },
            "cardinality_capped": {
              "type": "boolean"
            },
            "ingest": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileIngest"
                },
                {
                  "type": "null"
                }
              ]
            },
            "floor": {
              "anyOf": [
                {
                  "$ref": "#/$defs/RunToRunFloor"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "spec",
            "collector",
            "aggregation",
            "filter",
            "level",
            "matched",
            "wall_ms",
            "nesting",
            "sample_complete"
          ]
        },
        "ProfileIngest": {
          "description": "Span loss during the window, and the collector's own view of unusable input.\n\n`attribution` is `\"domain\"`, always: the transport counters are per-domain\nand unfiltered, so most of what they count may be spans this collector's\nfilter would never have matched. That makes this sound as a trigger for\nmarking a result untrustworthy, and wrong as a statement about how many\nmatches went missing.",
          "type": "object",
          "properties": {
            "drops_in_window": {
              "description": "Per-span channel-full drops on the two OTLP trace transports.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shed_batches": {
              "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_dropped": {
              "description": "Spans discarded at parse time for unusable identity.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_timestamps": {
              "description": "Matched spans whose timestamps this collector could not use.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "negative_duration_spans": {
              "description": "Matched spans that ended before they started.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "attribution": {
              "type": "string"
            }
          },
          "required": [
            "drops_in_window",
            "shed_batches",
            "malformed_dropped",
            "malformed_timestamps",
            "negative_duration_spans",
            "attribution"
          ]
        },
        "RunToRunFloor": {
          "description": "Spread across repeats of one configuration.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "runs": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "min": {
              "type": "number",
              "format": "double"
            },
            "max": {
              "type": "number",
              "format": "double"
            },
            "mean": {
              "type": "number",
              "format": "double"
            },
            "cv_pct": {
              "description": "Coefficient of variation, as a percentage. **Absent for a single run —\nunknown, not zero.** A floor of zero would license calling any\ndifference significant.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "caveat": {
              "description": "What this figure does *not* cover. Carried with it because a bare\nspread invites being read as a total error bar.",
              "type": "string"
            }
          },
          "required": [
            "metric",
            "runs",
            "min",
            "max",
            "mean",
            "caveat"
          ]
        },
        "DiffRow": {
          "description": "One metric's delta, with everything needed to decide whether to believe it.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "tier": {
              "description": "`exact`, `estimated`, or `sampled` — never mixed within a row. The two\ncan differ by 40 % under truncation, so a row taking one value from each\nwould be uninterpretable.",
              "type": "string"
            },
            "a": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "b": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "delta": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "delta_pct": {
              "description": "Change from `a` to `b`, as a percentage of `a`.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "threshold_abs": {
              "description": "**The threshold, in the metric's own units.** A delta at or below this is\nsuppressed — and this is the number displayed beside it, because striking\na delta through using a bound other than the one shown is how a reader\nends up unable to check the tool's own reasoning.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "threshold_pct": {
              "description": "The same threshold as a percentage of the two values' mean.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "threshold_basis": {
              "description": "`run-to-run`, `measurement-resolution`, both, or `unknown`.",
              "type": "string"
            },
            "error_bound_pct": {
              "description": "§5.6's `α(a+b)/|a−b|`: the worst-case error on this delta as a\npercentage **of the delta**, on `estimated` rows only. At or above 100 %\nthe error bar is wider than the delta it qualifies.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "suppressed": {
              "type": "boolean"
            },
            "trustworthy": {
              "type": "boolean"
            },
            "notes": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "metric",
            "tier",
            "threshold_basis",
            "trustworthy"
          ]
        },
        "DiffGroup": {
          "description": "One breakdown key's rows.",
          "type": "object",
          "properties": {
            "key": {
              "type": "string"
            },
            "presence": {
              "description": "`both`, `a_only`, or `b_only`. A key on one arm only gets no delta: it\nmay be missing because nothing matched it, or because a cardinality cap\nfolded it into `__overflow__`, and those want opposite readings.",
              "type": "string"
            },
            "rows": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DiffRow"
              }
            }
          },
          "required": [
            "key",
            "presence",
            "rows"
          ]
        },
        "DiffMark": {
          "description": "Something that differs between the arms, or that makes a number unsafe to\nact on. Never silently absorbed.",
          "type": "object",
          "properties": {
            "kind": {
              "description": "`level`, `filter`, `group_keys`, `ingest_loss`, `truncated`, `nesting`,\n`cardinality`, or `collector`.",
              "type": "string"
            },
            "detail": {
              "type": "string"
            },
            "overridden_by": {
              "description": "The flag the caller passed to permit this. Absent for conditions that\nonly ever mark.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "detail"
          ]
        },
        "Suppressed": {
          "description": "Why a field that normally carries a number is `null`.\n\nA profile has several ways to be legitimately unable to answer — the\nretention level is too low, a warm-up cut makes an unwindowed tier\nmeaningless, nothing nested so self time carries no information. Returning\n`null` alone would leave the reader to guess which, and the guesses differ\nin what they imply about the run. Every suppression names its field and its\nreason so a consumer can distinguish \"not measured\" from \"measured as zero\".",
          "type": "object",
          "properties": {
            "field": {
              "description": "Dotted path of the suppressed field, e.g. `sampled.self_ms`.",
              "type": "string"
            },
            "reason": {
              "type": "string"
            },
            "remedy": {
              "description": "What the caller could change to get a number instead. Absent when\nnothing would help.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "field",
            "reason"
          ]
        }
      }
    },
    "DiffArm": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DiffArm",
      "description": "One side of a comparison, as the result describes it.",
      "type": "object",
      "properties": {
        "spec": {
          "description": "How the caller named it.",
          "type": "string"
        },
        "collector": {
          "type": "string"
        },
        "aggregation": {
          "description": "Which runs this figure came from, and how they were combined. **Always\npresent**: a reader looking at a single number cannot otherwise tell one\nrun from the mean of five, and the floor depends on which it is.",
          "type": "string"
        },
        "filter": {
          "description": "The definition **as of the measurement**, never the live collector's.",
          "type": "string"
        },
        "level": {
          "type": "string"
        },
        "group_keys": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "meta": {
          "description": "Provenance logmon cannot infer — commit, build profile, configuration."
        },
        "matched": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "wall_ms": {
          "type": "number",
          "format": "double"
        },
        "window_start": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "taken_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "nesting": {
          "description": "`detected`, `undetected`, or `unknown`.",
          "type": "string"
        },
        "sample_complete": {
          "type": "boolean"
        },
        "cardinality_capped": {
          "type": "boolean"
        },
        "ingest": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileIngest"
            },
            {
              "type": "null"
            }
          ]
        },
        "floor": {
          "anyOf": [
            {
              "$ref": "#/$defs/RunToRunFloor"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "spec",
        "collector",
        "aggregation",
        "filter",
        "level",
        "matched",
        "wall_ms",
        "nesting",
        "sample_complete"
      ],
      "$defs": {
        "ProfileIngest": {
          "description": "Span loss during the window, and the collector's own view of unusable input.\n\n`attribution` is `\"domain\"`, always: the transport counters are per-domain\nand unfiltered, so most of what they count may be spans this collector's\nfilter would never have matched. That makes this sound as a trigger for\nmarking a result untrustworthy, and wrong as a statement about how many\nmatches went missing.",
          "type": "object",
          "properties": {
            "drops_in_window": {
              "description": "Per-span channel-full drops on the two OTLP trace transports.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shed_batches": {
              "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_dropped": {
              "description": "Spans discarded at parse time for unusable identity.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_timestamps": {
              "description": "Matched spans whose timestamps this collector could not use.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "negative_duration_spans": {
              "description": "Matched spans that ended before they started.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "attribution": {
              "type": "string"
            }
          },
          "required": [
            "drops_in_window",
            "shed_batches",
            "malformed_dropped",
            "malformed_timestamps",
            "negative_duration_spans",
            "attribution"
          ]
        },
        "RunToRunFloor": {
          "description": "Spread across repeats of one configuration.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "runs": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            },
            "min": {
              "type": "number",
              "format": "double"
            },
            "max": {
              "type": "number",
              "format": "double"
            },
            "mean": {
              "type": "number",
              "format": "double"
            },
            "cv_pct": {
              "description": "Coefficient of variation, as a percentage. **Absent for a single run —\nunknown, not zero.** A floor of zero would license calling any\ndifference significant.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "caveat": {
              "description": "What this figure does *not* cover. Carried with it because a bare\nspread invites being read as a total error bar.",
              "type": "string"
            }
          },
          "required": [
            "metric",
            "runs",
            "min",
            "max",
            "mean",
            "caveat"
          ]
        }
      }
    },
    "DiffRow": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DiffRow",
      "description": "One metric's delta, with everything needed to decide whether to believe it.",
      "type": "object",
      "properties": {
        "metric": {
          "type": "string"
        },
        "tier": {
          "description": "`exact`, `estimated`, or `sampled` — never mixed within a row. The two\ncan differ by 40 % under truncation, so a row taking one value from each\nwould be uninterpretable.",
          "type": "string"
        },
        "a": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "b": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "delta": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "delta_pct": {
          "description": "Change from `a` to `b`, as a percentage of `a`.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "threshold_abs": {
          "description": "**The threshold, in the metric's own units.** A delta at or below this is\nsuppressed — and this is the number displayed beside it, because striking\na delta through using a bound other than the one shown is how a reader\nends up unable to check the tool's own reasoning.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "threshold_pct": {
          "description": "The same threshold as a percentage of the two values' mean.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "threshold_basis": {
          "description": "`run-to-run`, `measurement-resolution`, both, or `unknown`.",
          "type": "string"
        },
        "error_bound_pct": {
          "description": "§5.6's `α(a+b)/|a−b|`: the worst-case error on this delta as a\npercentage **of the delta**, on `estimated` rows only. At or above 100 %\nthe error bar is wider than the delta it qualifies.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "suppressed": {
          "type": "boolean"
        },
        "trustworthy": {
          "type": "boolean"
        },
        "notes": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "metric",
        "tier",
        "threshold_basis",
        "trustworthy"
      ]
    },
    "DiffGroup": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DiffGroup",
      "description": "One breakdown key's rows.",
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "presence": {
          "description": "`both`, `a_only`, or `b_only`. A key on one arm only gets no delta: it\nmay be missing because nothing matched it, or because a cardinality cap\nfolded it into `__overflow__`, and those want opposite readings.",
          "type": "string"
        },
        "rows": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DiffRow"
          }
        }
      },
      "required": [
        "key",
        "presence",
        "rows"
      ],
      "$defs": {
        "DiffRow": {
          "description": "One metric's delta, with everything needed to decide whether to believe it.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "tier": {
              "description": "`exact`, `estimated`, or `sampled` — never mixed within a row. The two\ncan differ by 40 % under truncation, so a row taking one value from each\nwould be uninterpretable.",
              "type": "string"
            },
            "a": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "b": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "delta": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "delta_pct": {
              "description": "Change from `a` to `b`, as a percentage of `a`.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "threshold_abs": {
              "description": "**The threshold, in the metric's own units.** A delta at or below this is\nsuppressed — and this is the number displayed beside it, because striking\na delta through using a bound other than the one shown is how a reader\nends up unable to check the tool's own reasoning.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "threshold_pct": {
              "description": "The same threshold as a percentage of the two values' mean.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "threshold_basis": {
              "description": "`run-to-run`, `measurement-resolution`, both, or `unknown`.",
              "type": "string"
            },
            "error_bound_pct": {
              "description": "§5.6's `α(a+b)/|a−b|`: the worst-case error on this delta as a\npercentage **of the delta**, on `estimated` rows only. At or above 100 %\nthe error bar is wider than the delta it qualifies.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "suppressed": {
              "type": "boolean"
            },
            "trustworthy": {
              "type": "boolean"
            },
            "notes": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "metric",
            "tier",
            "threshold_basis",
            "trustworthy"
          ]
        }
      }
    },
    "DiffMark": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "DiffMark",
      "description": "Something that differs between the arms, or that makes a number unsafe to\nact on. Never silently absorbed.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "`level`, `filter`, `group_keys`, `ingest_loss`, `truncated`, `nesting`,\n`cardinality`, or `collector`.",
          "type": "string"
        },
        "detail": {
          "type": "string"
        },
        "overridden_by": {
          "description": "The flag the caller passed to permit this. Absent for conditions that\nonly ever mark.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind",
        "detail"
      ]
    },
    "PathRow": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "PathRow",
      "description": "One call path's self time, as a snapshot retains it (§9.8).\n\nRecorded at snapshot time because the per-span records it is walked from are\nnot retained — a flame graph not taken then cannot be taken later. The list\nis capped, and a snapshot says when it was cut short: a flame graph rendered\nfrom a partial list is missing mass, and nothing about looking at one reveals\nthat.",
      "type": "object",
      "properties": {
        "frames": {
          "description": "Matched ancestors, root first, **one frame per element**.\n\nA list rather than a joined string, because the joined form is\nambiguous: a span named `parse > eval` is indistinguishable from two\nframes once ` > ` is the separator, and the flame-graph renderer that\nsplits it back apart would silently produce a stack that never ran. The\ndisplay form is derived; this is the form that survives.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "self_ms": {
          "description": "Time in this path and nothing matched below it.",
          "type": "number",
          "format": "double"
        },
        "count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "incomplete": {
          "description": "The walk stopped at a parent that was not retained, so this is a suffix\nrather than a root-to-here chain. Rendered with a leading `[?]` frame.",
          "type": "boolean"
        }
      },
      "required": [
        "frames",
        "self_ms",
        "count"
      ]
    },
    "ThresholdSpec": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ThresholdSpec",
      "description": "A rolling threshold as declared (§8).\n\nEvaluated over a window **advanced by span arrival, never by a clock**, so an\nidle collector costs nothing — and so a breached threshold on a finished run\nneither fires again nor clears. That is the right behaviour for a load-time\nguard and the wrong one for a liveness check, which is why every report\ncarries the note saying so.",
      "type": "object",
      "properties": {
        "metric": {
          "description": "`count`, `total_ms`, `avg_ms`, `error_count` or `error_rate_pct`.\n\n**Percentiles are deliberately absent.** A rolling percentile needs a\nduration sketch per bucket, and this design's per-collector memory is\nbounded precisely so a collector cannot grow without limit. Use `avg_ms`\nfor the guard and read the real percentiles with `collectors.get`.",
          "type": "string",
          "enum": [
            "count",
            "total_ms",
            "avg_ms",
            "error_count",
            "error_rate_pct"
          ]
        },
        "group": {
          "description": "Restrict to one group value. Requires the collector to declare\n`group_keys`.",
          "type": [
            "string",
            "null"
          ]
        },
        "op": {
          "description": "`gt`, `gte`, `lt` or `lte` — and the symbolic spellings `>`, `>=`, `<`\nand `<=`, which the parser has always accepted (`threshold.rs:120`) and\nthis doc comment used to omit. The word forms are canonical; the symbols\nare listed because a schema narrower than the daemon rejects calls the\ndaemon would have served.",
          "type": "string",
          "enum": [
            "gt",
            ">",
            "gte",
            ">=",
            "lt",
            "<",
            "lte",
            "<="
          ]
        },
        "value": {
          "type": "number",
          "format": "double"
        },
        "window_ms": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "metric",
        "op",
        "value",
        "window_ms"
      ]
    },
    "ThresholdInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ThresholdInfo",
      "description": "A threshold and its current verdict, as `collectors.list` and\n`collectors.get` report it.",
      "type": "object",
      "properties": {
        "metric": {
          "type": "string"
        },
        "group": {
          "type": [
            "string",
            "null"
          ]
        },
        "op": {
          "type": "string"
        },
        "value": {
          "type": "number",
          "format": "double"
        },
        "window_ms": {
          "description": "The window as declared.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "effective_window_ms": {
          "description": "The window as **evaluated**: the declared width rounded up to a whole\nnumber of ring buckets, so it is never narrower than what was asked for.\nEqual to `window_ms` when that is a multiple of the bucket count.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 0
        },
        "breached": {
          "type": "boolean"
        },
        "fires": {
          "description": "Clear-to-breached **transitions**, not evaluations: a threshold breached\nfor a whole run has fired once, which is what \"did this happen\" means.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "last_value": {
          "description": "The metric's value at the last evaluation. **Absent until one has\nhappened — unknown, not zero**, because zero is a value `count` can\nlegitimately hold and an `lt` threshold would read it as a breach.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "evaluated": {
          "description": "Whether any span has yet been evaluated against this threshold.",
          "type": "boolean"
        },
        "note": {
          "description": "Carried with every report, because it is the one thing about this\nmechanism that surprises people.",
          "type": "string"
        }
      },
      "required": [
        "metric",
        "op",
        "value",
        "window_ms",
        "breached",
        "fires",
        "evaluated",
        "note"
      ]
    },
    "CollectorsDocument": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsDocument",
      "description": "`collectors.document` — render a measurement for a reader (§9).\n\n**The daemon returns bytes; the client writes them** (§9.9). The broker runs\nas a service, so a relative path would resolve against its working directory\nrather than the caller's — and `logs.export` already puts the write in the\nclient for the same reason. Regeneration is free and lossless, so nothing is\nstored: the normal shape is to read the document, learn something, and\nregenerate it with `finding` filled in.",
      "type": "object",
      "properties": {
        "names": {
          "description": "The arms to document. The **first is the baseline**; every other one is\ncompared against it. Same syntax as `collectors.diff`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "format": {
          "description": "`md` (default), `json`, or `folded`. `markdown` and `\"\"` are accepted\nfor `md`, and `collapsed` for `folded` (`document.rs:57`) — aliases the\nerror message does not name but the parser has always taken.",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "md",
            "markdown",
            "",
            "json",
            "folded",
            "collapsed",
            null
          ]
        },
        "question": {
          "description": "What you were trying to find out. Carries most of the triage weight when\nthis is read months later, which is why it is asked for.",
          "type": [
            "string",
            "null"
          ]
        },
        "finding": {
          "description": "What you concluded. Normally supplied on a **second** call, after the\nfirst read — which is why regeneration is free.",
          "type": [
            "string",
            "null"
          ]
        },
        "filter_intent": {
          "description": "What the filter was meant to capture. Defaults to the collector's\ndescription.",
          "type": [
            "string",
            "null"
          ]
        },
        "correctness_evidence": {
          "description": "Evidence that the faster arm is still correct. logmon cannot know this,\nand \"we asked and nobody supplied it\" is a different statement from\nsilence — so the field is always present and defaults to `unknown`.",
          "type": [
            "string",
            "null"
          ]
        },
        "group_by": {
          "description": "`name` (default) or `group`. `none` and `\"\"` are accepted for the\nungrouped document (`diff.rs:77`). Note the default differs from\n`collectors.diff`: omitting it here means `name`, not ungrouped.",
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "name",
            "group",
            "none",
            "",
            null
          ]
        },
        "top_n": {
          "description": "Rows in the ranked table (default 15).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "allow_mismatch": {
          "description": "Document arms whose filters matched different populations. Off by\ndefault: the comparison is then between different things.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allow_lossy": {
          "description": "Document arms whose ingest dropped spans. Off by default: the missing\nspans are not random.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allow_truncated": {
          "description": "Document arms whose sample tier hit its cap. Off by default: the tail is\nthe part that was cut.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "names"
      ]
    },
    "CollectorsDocumentResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "CollectorsDocumentResult",
      "type": "object",
      "properties": {
        "content": {
          "type": "string"
        },
        "format": {
          "type": "string"
        },
        "bytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "sidecar_name": {
          "description": "The bulk companion (§9.7) — a percentile table plus layout identity,\nwhich is what decides whether two documents are comparable. Named in the\ndocument's own front-matter so the two can be reunited after being moved.",
          "type": [
            "string",
            "null"
          ]
        },
        "sidecar_content": {
          "type": [
            "string",
            "null"
          ]
        },
        "warnings": {
          "description": "Things to know about the *document*, as distinct from the measurement it\ndescribes — a path list that was capped, detail moved to the sidecar.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "content",
        "format",
        "bytes"
      ]
    },
    "ProfileResult": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileResult",
      "description": "The result of profiling a collector or an ad-hoc span query.",
      "type": "object",
      "properties": {
        "collector": {
          "description": "Collector name, or absent for an ad-hoc `traces.profile` query.",
          "type": [
            "string",
            "null"
          ]
        },
        "snapshot": {
          "description": "Set when this is a recorded run rather than the live window. The label\nis how the run is referred to everywhere else, so a result without it\nwould be unquotable.",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "filter": {
          "type": "string"
        },
        "level": {
          "description": "`scalar`, `timing`, or `tree`.",
          "type": "string"
        },
        "matched": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "nesting": {
          "description": "`detected`, `undetected`, or `unknown` — the last when the retention\nlevel cannot answer the question at all.",
          "type": "string"
        },
        "window": {
          "$ref": "#/$defs/ProfileWindow"
        },
        "ingest": {
          "description": "Absent when the pinned domain is gone, or was recreated under the\ncollector, which makes a counter delta meaningless.",
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileIngest"
            },
            {
              "type": "null"
            }
          ]
        },
        "exact": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileExact"
            },
            {
              "type": "null"
            }
          ]
        },
        "estimated": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileEstimated"
            },
            {
              "type": "null"
            }
          ]
        },
        "sampled": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileSampled"
            },
            {
              "type": "null"
            }
          ]
        },
        "grouped_by": {
          "type": [
            "string",
            "null"
          ]
        },
        "groups": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProfileGroup"
          }
        },
        "groups_total": {
          "description": "Group keys **before** `top_n` truncation, so a reader can tell \"the top\n20 of 20\" from \"the top 20 of 900\".\n\nAbsent when no grouping was performed — either none was asked for, or\nthe one asked for was refused (see `suppressed`). A refused grouping has\nno denominator, and `0` there would read as \"this run touched nothing\"\nrather than \"we did not look\".\n\nCounts every key this one arm's axis holds, `__overflow__` included when\na cardinality cap folded values into it. `CollectorsDiffResult` carries a\nfield of the same name that counts something else — the *comparable* keys\nacross the union of two arms, `__overflow__` excluded — so the two are\nnot expected to reconcile and can differ by any amount.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "excluded_by_warmup": {
          "description": "How many retained spans `skip_warmup_ms` excluded from the sample tier.\n\n**Absent means the filter never ran** — not that it ran and excluded\nnothing. A reader that cannot tell those apart cannot tell \"warm-up was\nnegligible\" from \"warm-up was never cut\", which are opposite facts about\nthe same number. `sampled.sample_count` is the population that survived,\nso the two together give the whole picture.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "cardinality_capped": {
          "description": "True when a cardinality cap folded values into `__overflow__`, so at\nleast one row aggregates an arbitrary, arrival-ordered member set.",
          "type": "boolean"
        },
        "suppressed": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Suppressed"
          }
        },
        "warnings": {
          "description": "`traces.profile` only — the same admission warnings `collectors.add`\nreturns, since an ad-hoc filter has no arm-time moment to report them.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "threshold": {
          "description": "The armed threshold and its verdict, so one read answers both \"what are\nthe numbers\" and \"did it go over\" (§8). Absent on a recorded run: a\nrolling window is live state and a snapshot does not preserve it.",
          "anyOf": [
            {
              "$ref": "#/$defs/ThresholdInfo"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "filter",
        "level",
        "matched",
        "nesting",
        "window"
      ],
      "$defs": {
        "ProfileWindow": {
          "description": "The window a profile covers. `wall_ms` runs from the later of arming and the\nlast zeroing, so a collector that was reset mid-run does not report the wall\ntime of data it no longer holds.",
          "type": "object",
          "properties": {
            "armed_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "zeroed_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "read_at": {
              "type": [
                "string",
                "null"
              ],
              "format": "date-time"
            },
            "wall_ms": {
              "type": "number",
              "format": "double"
            }
          },
          "required": [
            "wall_ms"
          ]
        },
        "ProfileIngest": {
          "description": "Span loss during the window, and the collector's own view of unusable input.\n\n`attribution` is `\"domain\"`, always: the transport counters are per-domain\nand unfiltered, so most of what they count may be spans this collector's\nfilter would never have matched. That makes this sound as a trigger for\nmarking a result untrustworthy, and wrong as a statement about how many\nmatches went missing.",
          "type": "object",
          "properties": {
            "drops_in_window": {
              "description": "Per-span channel-full drops on the two OTLP trace transports.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "shed_batches": {
              "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_dropped": {
              "description": "Spans discarded at parse time for unusable identity.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "malformed_timestamps": {
              "description": "Matched spans whose timestamps this collector could not use.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "negative_duration_spans": {
              "description": "Matched spans that ended before they started.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "attribution": {
              "type": "string"
            }
          },
          "required": [
            "drops_in_window",
            "shed_batches",
            "malformed_dropped",
            "malformed_timestamps",
            "negative_duration_spans",
            "attribution"
          ]
        },
        "ProfileExact": {
          "description": "The always-exact tier. Never capped by the sample budget.",
          "type": "object",
          "properties": {
            "count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "total_ms": {
              "type": "number",
              "format": "double"
            },
            "avg_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "min_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "max_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "error_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "out_of_range_spans": {
              "description": "Well-formed spans longer than the sketch's declared range: counted in\nfull toward `total_ms`, clamped in the distribution.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "count",
            "total_ms",
            "error_count",
            "out_of_range_spans"
          ]
        },
        "ProfileEstimated": {
          "description": "Percentiles from the sketch — bounded relative error, unbounded population.",
          "type": "object",
          "properties": {
            "axis": {
              "description": "Which population this sketch covers: `collector`, `name`, or `group`.",
              "type": "string"
            },
            "alpha_pct": {
              "description": "The sketch's relative-accuracy guarantee, as a percentage.",
              "type": "number",
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            }
          },
          "required": [
            "axis",
            "alpha_pct"
          ]
        },
        "ProfileSampled": {
          "description": "Everything derived from retained per-span records. Exact over the records it\nholds — which is the whole population only while `complete` is true.",
          "type": "object",
          "properties": {
            "complete": {
              "description": "`false` once the sample budget stopped retention. The retained set is\nthen a contiguous prefix, not a sample of the whole run.",
              "type": "boolean"
            },
            "sample_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "self_ms": {
              "description": "Duration minus the union of matched children clipped to this span —\ntime spent in this span and nothing matched below it.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "nested_matches": {
              "description": "Matched spans whose parent is also matched. Zero means self time equals\ntotal time by construction, and `self_ms` is suppressed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "overlapping_child_ms": {
              "description": "Child time that lay outside its parent's interval and was clipped away.\nNon-zero means clock skew or instrumentation error, not concurrency.",
              "type": "number",
              "format": "double"
            },
            "overlapping_child_spans": {
              "description": "How many children needed clipping. Paired with the millisecond figure\nbecause one child off by a second and a thousand off by a millisecond\nwant opposite remedies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "wall_union_ms": {
              "description": "Elapsed time during which at least one matched span was in flight.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "achieved_concurrency": {
              "description": "Total matched work divided by the wall time it occupied. 1.0 is fully\nserial; 4.0 means four matched spans in flight on average.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "stddev_ms": {
              "description": "Sample standard deviation (Bessel-corrected, `n-1`) over exactly the\ndurations the percentiles beside it were computed from — the same\npopulation, after any warm-up cut.\n\nAbsent below two records, where it is undefined rather than zero. Also\nabsent on `group_by: path` rows, which hold a count and a self-time sum\nrather than the per-span durations either figure needs; there, absence\nsays nothing about how many records the row covers.\n\nThe `n-1` form because at the sizes this serves the population form is\nabout 18% smaller at three records (it is smaller by `sqrt((n-1)/n)`).\nNote that is the gap between the two *formulas*: both still estimate the\ntrue spread from very few samples, and at three records the estimate is\nitself uncertain to roughly ±45%. This is a description of the observed\nspread, not a significance test — comparing two three-run means properly\nneeds a difference of roughly 2.3 standard deviations, not one.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "durations_ms": {
              "description": "Every retained duration, in **arrival order**, when the sample is\ncomplete and holds at most 50 records.\n\nArrival order rather than sorted: a reader can sort these but could not\nunsort them, and the order carries drift across a run — first-call\neffects, a cache filling — which every other figure here discards.\n`durations_ms[0]` is the FIRST duration, not the smallest.\n\nAbsent when the sample was truncated (a prefix of a run presented as its\ndurations would invite exactly the per-value reasoning a biased subset\ncannot support), and absent above 50 records. On a live read `suppressed`\nnames which of the two applies.\n\nTwo further absences carry no `suppressed` entry at all, because their\nresponses have no such channel. A recorded run read by label serves the\nprojection as it stood when the snapshot was taken; and any response that\nLISTS runs — `collectors.history`, and the `collectors.snapshot` reply —\ndrops the durations whatever their size, because a bound written for one\nblock would otherwise multiply by the number of runs returned. Read a\nsingle run by label to get them.\n\nOnly the top-level `sampled` block populates this. Group rows leave it\nabsent regardless of their size: it is the population the headline\nfigures describe, and per-row lists would multiply it by `top_n`.",
              "type": [
                "array",
                "null"
              ],
              "items": {
                "type": "number",
                "format": "double"
              }
            }
          },
          "required": [
            "complete",
            "sample_count",
            "nested_matches",
            "overlapping_child_ms",
            "overlapping_child_spans"
          ]
        },
        "ProfileGroup": {
          "description": "One row of a breakdown. `key` is the span name, the group tuple, the trace\nid, or the call path, depending on `grouped_by`.",
          "type": "object",
          "properties": {
            "key": {
              "type": "string"
            },
            "exact": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileExact"
                },
                {
                  "type": "null"
                }
              ]
            },
            "estimated": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileEstimated"
                },
                {
                  "type": "null"
                }
              ]
            },
            "sampled": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ProfileSampled"
                },
                {
                  "type": "null"
                }
              ]
            },
            "path_incomplete": {
              "description": "Set on a call path whose walk stopped at a parent that is not retained,\nso the path is a suffix rather than a root-to-here chain.",
              "type": "boolean"
            }
          },
          "required": [
            "key"
          ]
        },
        "Suppressed": {
          "description": "Why a field that normally carries a number is `null`.\n\nA profile has several ways to be legitimately unable to answer — the\nretention level is too low, a warm-up cut makes an unwindowed tier\nmeaningless, nothing nested so self time carries no information. Returning\n`null` alone would leave the reader to guess which, and the guesses differ\nin what they imply about the run. Every suppression names its field and its\nreason so a consumer can distinguish \"not measured\" from \"measured as zero\".",
          "type": "object",
          "properties": {
            "field": {
              "description": "Dotted path of the suppressed field, e.g. `sampled.self_ms`.",
              "type": "string"
            },
            "reason": {
              "type": "string"
            },
            "remedy": {
              "description": "What the caller could change to get a number instead. Absent when\nnothing would help.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "field",
            "reason"
          ]
        },
        "ThresholdInfo": {
          "description": "A threshold and its current verdict, as `collectors.list` and\n`collectors.get` report it.",
          "type": "object",
          "properties": {
            "metric": {
              "type": "string"
            },
            "group": {
              "type": [
                "string",
                "null"
              ]
            },
            "op": {
              "type": "string"
            },
            "value": {
              "type": "number",
              "format": "double"
            },
            "window_ms": {
              "description": "The window as declared.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "effective_window_ms": {
              "description": "The window as **evaluated**: the declared width rounded up to a whole\nnumber of ring buckets, so it is never narrower than what was asked for.\nEqual to `window_ms` when that is a multiple of the bucket count.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0,
              "default": 0
            },
            "breached": {
              "type": "boolean"
            },
            "fires": {
              "description": "Clear-to-breached **transitions**, not evaluations: a threshold breached\nfor a whole run has fired once, which is what \"did this happen\" means.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "last_value": {
              "description": "The metric's value at the last evaluation. **Absent until one has\nhappened — unknown, not zero**, because zero is a value `count` can\nlegitimately hold and an `lt` threshold would read it as a breach.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "evaluated": {
              "description": "Whether any span has yet been evaluated against this threshold.",
              "type": "boolean"
            },
            "note": {
              "description": "Carried with every report, because it is the one thing about this\nmechanism that surprises people.",
              "type": "string"
            }
          },
          "required": [
            "metric",
            "op",
            "value",
            "window_ms",
            "breached",
            "fires",
            "evaluated",
            "note"
          ]
        }
      }
    },
    "ProfileWindow": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileWindow",
      "description": "The window a profile covers. `wall_ms` runs from the later of arming and the\nlast zeroing, so a collector that was reset mid-run does not report the wall\ntime of data it no longer holds.",
      "type": "object",
      "properties": {
        "armed_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "zeroed_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "read_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "wall_ms": {
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "wall_ms"
      ]
    },
    "ProfileIngest": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileIngest",
      "description": "Span loss during the window, and the collector's own view of unusable input.\n\n`attribution` is `\"domain\"`, always: the transport counters are per-domain\nand unfiltered, so most of what they count may be spans this collector's\nfilter would never have matched. That makes this sound as a trigger for\nmarking a result untrustworthy, and wrong as a statement about how many\nmatches went missing.",
      "type": "object",
      "properties": {
        "drops_in_window": {
          "description": "Per-span channel-full drops on the two OTLP trace transports.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "shed_batches": {
          "description": "Whole request bodies refused with 429 / UNAVAILABLE. The span count\nbehind these is unknowable — the bodies were never parsed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "malformed_dropped": {
          "description": "Spans discarded at parse time for unusable identity.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "malformed_timestamps": {
          "description": "Matched spans whose timestamps this collector could not use.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "negative_duration_spans": {
          "description": "Matched spans that ended before they started.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "attribution": {
          "type": "string"
        }
      },
      "required": [
        "drops_in_window",
        "shed_batches",
        "malformed_dropped",
        "malformed_timestamps",
        "negative_duration_spans",
        "attribution"
      ]
    },
    "ProfileExact": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileExact",
      "description": "The always-exact tier. Never capped by the sample budget.",
      "type": "object",
      "properties": {
        "count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "total_ms": {
          "type": "number",
          "format": "double"
        },
        "avg_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "min_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "max_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "error_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "out_of_range_spans": {
          "description": "Well-formed spans longer than the sketch's declared range: counted in\nfull toward `total_ms`, clamped in the distribution.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "count",
        "total_ms",
        "error_count",
        "out_of_range_spans"
      ]
    },
    "ProfileEstimated": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileEstimated",
      "description": "Percentiles from the sketch — bounded relative error, unbounded population.",
      "type": "object",
      "properties": {
        "axis": {
          "description": "Which population this sketch covers: `collector`, `name`, or `group`.",
          "type": "string"
        },
        "alpha_pct": {
          "description": "The sketch's relative-accuracy guarantee, as a percentage.",
          "type": "number",
          "format": "double"
        },
        "p50_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p80_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p95_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p99_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      },
      "required": [
        "axis",
        "alpha_pct"
      ]
    },
    "ProfileSampled": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileSampled",
      "description": "Everything derived from retained per-span records. Exact over the records it\nholds — which is the whole population only while `complete` is true.",
      "type": "object",
      "properties": {
        "complete": {
          "description": "`false` once the sample budget stopped retention. The retained set is\nthen a contiguous prefix, not a sample of the whole run.",
          "type": "boolean"
        },
        "sample_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "self_ms": {
          "description": "Duration minus the union of matched children clipped to this span —\ntime spent in this span and nothing matched below it.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "nested_matches": {
          "description": "Matched spans whose parent is also matched. Zero means self time equals\ntotal time by construction, and `self_ms` is suppressed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "overlapping_child_ms": {
          "description": "Child time that lay outside its parent's interval and was clipped away.\nNon-zero means clock skew or instrumentation error, not concurrency.",
          "type": "number",
          "format": "double"
        },
        "overlapping_child_spans": {
          "description": "How many children needed clipping. Paired with the millisecond figure\nbecause one child off by a second and a thousand off by a millisecond\nwant opposite remedies.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "wall_union_ms": {
          "description": "Elapsed time during which at least one matched span was in flight.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "achieved_concurrency": {
          "description": "Total matched work divided by the wall time it occupied. 1.0 is fully\nserial; 4.0 means four matched spans in flight on average.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p50_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p80_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p95_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "p99_ms": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "stddev_ms": {
          "description": "Sample standard deviation (Bessel-corrected, `n-1`) over exactly the\ndurations the percentiles beside it were computed from — the same\npopulation, after any warm-up cut.\n\nAbsent below two records, where it is undefined rather than zero. Also\nabsent on `group_by: path` rows, which hold a count and a self-time sum\nrather than the per-span durations either figure needs; there, absence\nsays nothing about how many records the row covers.\n\nThe `n-1` form because at the sizes this serves the population form is\nabout 18% smaller at three records (it is smaller by `sqrt((n-1)/n)`).\nNote that is the gap between the two *formulas*: both still estimate the\ntrue spread from very few samples, and at three records the estimate is\nitself uncertain to roughly ±45%. This is a description of the observed\nspread, not a significance test — comparing two three-run means properly\nneeds a difference of roughly 2.3 standard deviations, not one.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "durations_ms": {
          "description": "Every retained duration, in **arrival order**, when the sample is\ncomplete and holds at most 50 records.\n\nArrival order rather than sorted: a reader can sort these but could not\nunsort them, and the order carries drift across a run — first-call\neffects, a cache filling — which every other figure here discards.\n`durations_ms[0]` is the FIRST duration, not the smallest.\n\nAbsent when the sample was truncated (a prefix of a run presented as its\ndurations would invite exactly the per-value reasoning a biased subset\ncannot support), and absent above 50 records. On a live read `suppressed`\nnames which of the two applies.\n\nTwo further absences carry no `suppressed` entry at all, because their\nresponses have no such channel. A recorded run read by label serves the\nprojection as it stood when the snapshot was taken; and any response that\nLISTS runs — `collectors.history`, and the `collectors.snapshot` reply —\ndrops the durations whatever their size, because a bound written for one\nblock would otherwise multiply by the number of runs returned. Read a\nsingle run by label to get them.\n\nOnly the top-level `sampled` block populates this. Group rows leave it\nabsent regardless of their size: it is the population the headline\nfigures describe, and per-row lists would multiply it by `top_n`.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "number",
            "format": "double"
          }
        }
      },
      "required": [
        "complete",
        "sample_count",
        "nested_matches",
        "overlapping_child_ms",
        "overlapping_child_spans"
      ]
    },
    "ProfileGroup": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "ProfileGroup",
      "description": "One row of a breakdown. `key` is the span name, the group tuple, the trace\nid, or the call path, depending on `grouped_by`.",
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "exact": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileExact"
            },
            {
              "type": "null"
            }
          ]
        },
        "estimated": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileEstimated"
            },
            {
              "type": "null"
            }
          ]
        },
        "sampled": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProfileSampled"
            },
            {
              "type": "null"
            }
          ]
        },
        "path_incomplete": {
          "description": "Set on a call path whose walk stopped at a parent that is not retained,\nso the path is a suffix rather than a root-to-here chain.",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "$defs": {
        "ProfileExact": {
          "description": "The always-exact tier. Never capped by the sample budget.",
          "type": "object",
          "properties": {
            "count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "total_ms": {
              "type": "number",
              "format": "double"
            },
            "avg_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "min_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "max_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "error_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "out_of_range_spans": {
              "description": "Well-formed spans longer than the sketch's declared range: counted in\nfull toward `total_ms`, clamped in the distribution.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "count",
            "total_ms",
            "error_count",
            "out_of_range_spans"
          ]
        },
        "ProfileEstimated": {
          "description": "Percentiles from the sketch — bounded relative error, unbounded population.",
          "type": "object",
          "properties": {
            "axis": {
              "description": "Which population this sketch covers: `collector`, `name`, or `group`.",
              "type": "string"
            },
            "alpha_pct": {
              "description": "The sketch's relative-accuracy guarantee, as a percentage.",
              "type": "number",
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            }
          },
          "required": [
            "axis",
            "alpha_pct"
          ]
        },
        "ProfileSampled": {
          "description": "Everything derived from retained per-span records. Exact over the records it\nholds — which is the whole population only while `complete` is true.",
          "type": "object",
          "properties": {
            "complete": {
              "description": "`false` once the sample budget stopped retention. The retained set is\nthen a contiguous prefix, not a sample of the whole run.",
              "type": "boolean"
            },
            "sample_count": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "self_ms": {
              "description": "Duration minus the union of matched children clipped to this span —\ntime spent in this span and nothing matched below it.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "nested_matches": {
              "description": "Matched spans whose parent is also matched. Zero means self time equals\ntotal time by construction, and `self_ms` is suppressed.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "overlapping_child_ms": {
              "description": "Child time that lay outside its parent's interval and was clipped away.\nNon-zero means clock skew or instrumentation error, not concurrency.",
              "type": "number",
              "format": "double"
            },
            "overlapping_child_spans": {
              "description": "How many children needed clipping. Paired with the millisecond figure\nbecause one child off by a second and a thousand off by a millisecond\nwant opposite remedies.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "wall_union_ms": {
              "description": "Elapsed time during which at least one matched span was in flight.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "achieved_concurrency": {
              "description": "Total matched work divided by the wall time it occupied. 1.0 is fully\nserial; 4.0 means four matched spans in flight on average.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p50_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p80_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p95_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "p99_ms": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "stddev_ms": {
              "description": "Sample standard deviation (Bessel-corrected, `n-1`) over exactly the\ndurations the percentiles beside it were computed from — the same\npopulation, after any warm-up cut.\n\nAbsent below two records, where it is undefined rather than zero. Also\nabsent on `group_by: path` rows, which hold a count and a self-time sum\nrather than the per-span durations either figure needs; there, absence\nsays nothing about how many records the row covers.\n\nThe `n-1` form because at the sizes this serves the population form is\nabout 18% smaller at three records (it is smaller by `sqrt((n-1)/n)`).\nNote that is the gap between the two *formulas*: both still estimate the\ntrue spread from very few samples, and at three records the estimate is\nitself uncertain to roughly ±45%. This is a description of the observed\nspread, not a significance test — comparing two three-run means properly\nneeds a difference of roughly 2.3 standard deviations, not one.",
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "durations_ms": {
              "description": "Every retained duration, in **arrival order**, when the sample is\ncomplete and holds at most 50 records.\n\nArrival order rather than sorted: a reader can sort these but could not\nunsort them, and the order carries drift across a run — first-call\neffects, a cache filling — which every other figure here discards.\n`durations_ms[0]` is the FIRST duration, not the smallest.\n\nAbsent when the sample was truncated (a prefix of a run presented as its\ndurations would invite exactly the per-value reasoning a biased subset\ncannot support), and absent above 50 records. On a live read `suppressed`\nnames which of the two applies.\n\nTwo further absences carry no `suppressed` entry at all, because their\nresponses have no such channel. A recorded run read by label serves the\nprojection as it stood when the snapshot was taken; and any response that\nLISTS runs — `collectors.history`, and the `collectors.snapshot` reply —\ndrops the durations whatever their size, because a bound written for one\nblock would otherwise multiply by the number of runs returned. Read a\nsingle run by label to get them.\n\nOnly the top-level `sampled` block populates this. Group rows leave it\nabsent regardless of their size: it is the population the headline\nfigures describe, and per-row lists would multiply it by `top_n`.",
              "type": [
                "array",
                "null"
              ],
              "items": {
                "type": "number",
                "format": "double"
              }
            }
          },
          "required": [
            "complete",
            "sample_count",
            "nested_matches",
            "overlapping_child_ms",
            "overlapping_child_spans"
          ]
        }
      }
    },
    "Suppressed": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Suppressed",
      "description": "Why a field that normally carries a number is `null`.\n\nA profile has several ways to be legitimately unable to answer — the\nretention level is too low, a warm-up cut makes an unwindowed tier\nmeaningless, nothing nested so self time carries no information. Returning\n`null` alone would leave the reader to guess which, and the guesses differ\nin what they imply about the run. Every suppression names its field and its\nreason so a consumer can distinguish \"not measured\" from \"measured as zero\".",
      "type": "object",
      "properties": {
        "field": {
          "description": "Dotted path of the suppressed field, e.g. `sampled.self_ms`.",
          "type": "string"
        },
        "reason": {
          "type": "string"
        },
        "remedy": {
          "description": "What the caller could change to get a number instead. Absent when\nnothing would help.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "field",
        "reason"
      ]
    },
    "FilterInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "FilterInfo",
      "description": "One entry in a `filters.list` response.",
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "filter": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "filter"
      ]
    },
    "TriggerInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "TriggerInfo",
      "description": "One entry in a `triggers.list` response.",
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "filter": {
          "type": "string"
        },
        "pre_window": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "post_window": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "notify_context": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "match_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "oneshot": {
          "description": "When `true`, the trigger auto-removes after its first match. Always\nserialized (no `skip_serializing_if`) so clients see the explicit value.",
          "type": "boolean",
          "default": false
        },
        "post_remaining": {
          "description": "Entries still to pass before this trigger can fire again — its own\ndebounce window; `0` means armed and live. A trigger that looks stuck\nwith a non-zero value here is debounced, not broken.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 0
        }
      },
      "required": [
        "id",
        "filter",
        "pre_window",
        "post_window",
        "notify_context",
        "match_count"
      ]
    },
    "SessionInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "SessionInfo",
      "description": "One entry in a `sessions.list` or `status.get` response.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "connected": {
          "type": "boolean"
        },
        "trigger_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "filter_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "queue_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "last_seen_secs_ago": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "client_info": {
          "description": "Caller-supplied identity blob from the most recent `session.start`.\nOmitted from the wire when never set."
        }
      },
      "required": [
        "id",
        "connected",
        "trigger_count",
        "filter_count",
        "queue_size",
        "last_seen_secs_ago"
      ]
    },
    "StoreStats": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "StoreStats",
      "description": "Pipeline buffer / receive statistics. Embedded in `status.get`.",
      "type": "object",
      "properties": {
        "total_received": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "total_stored": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "malformed_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "current_size": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "total_received",
        "total_stored",
        "malformed_count",
        "current_size"
      ]
    },
    "BookmarkInfo": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "BookmarkInfo",
      "description": "One entry in a `bookmarks.list` response.\n\nAs of the cursor design (2026-05-01), bookmark position is a `u64` seq\nrather than a wall-clock timestamp. `created_at` is retained for human\ndisplay only; never use it for filter semantics.",
      "type": "object",
      "properties": {
        "qualified_name": {
          "description": "Qualified form (`\"session/bookmark\"`). The bare name is recoverable\nby splitting on the last `/`.",
          "type": "string"
        },
        "seq": {
          "description": "Position the bookmark anchors. Filter DSL `b>=name` matches records\nwith `entry.seq > seq`; `b<=name` matches `entry.seq < seq`.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "created_at": {
          "description": "Wall-clock creation time, for display only.",
          "type": "string",
          "format": "date-time"
        },
        "description": {
          "description": "Optional caller-supplied note.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "qualified_name",
        "seq",
        "created_at"
      ]
    }
  }
}
