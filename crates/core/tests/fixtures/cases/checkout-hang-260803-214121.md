---
case: checkout-hang-260803-214121
logmon_format: 1
captured_at: "2026-08-03T21:41:21.707641+00:00"
domain: "default"
incarnation: "1"
reason: "payment gateway timeout on checkout A-1001"
anchor: {kind: seq, value: "1005", seq: 1005, at: "2026-08-03T21:39:46.093389034+00:00"}
headline: "checkout failed"
verdict: complete
seq_range: {from: 1001, to: 1006, requested_before_missing: 6, requested_after_missing: 9, clamped: false}
logdata: {file: "checkout-hang-260803-214121.logdata.jsonl", records: 6}
spandata: {file: "checkout-hang-260803-214121.spandata.jsonl", records: 0}
provenance: {core: "3 of 3", missing: []}
asserted: {}
---

# checkout failed

## 1. What this is

A **case**: one window of a running system, frozen to disk by `logmon`, a log and trace broker. It was captured by a person or an agent who thought it worth keeping; the reason they gave is below. Nothing here is a diagnosis.

`checkout-hang-260803-214121` on **domain** `default` — a logmon domain is one isolated stream of logs and traces, normally one per project or test run — at 2026-08-03T21:39:46.093389034+00:00, because: payment gateway timeout on checkout A-1001

A **seq** is a per-domain counter stamped on every record as it arrives, and it is the only ordering this archive has — timestamps can tie. One counter numbers both logs and spans, so consecutive seqs are not consecutive log records. The **incarnation** is which run of the daemon assigned them: seqs are comparable only within one, which is why it is in the front matter.

Captured at 2026-08-03T21:41:21.707641+00:00, 1m after the anchor.

## 2. Evidence — what this capture can and cannot show

The verdict below grades **how much of this window logmon can vouch for**. It is not a finding about the incident. Storage is conditional — a filter held by any session on this domain narrows what gets kept — so it exists to tell *absence of cause* from *absence of recording*.

**`complete`** — seqs 1001–1006 lay wholly inside one unfiltered epoch, nothing was evicted from it. Everything the daemon stored over these seqs is in the logdata.

Fewer records than seqs is normal and not a gap: one counter numbers both logs and spans, so a span consumes a seq no log will ever occupy.

The window is **narrower than requested**: 6 record(s) short before the anchor and 9 after.

Below the window: seq 1001 is the oldest record this store has ever held and it has dropped nothing, so the shortfall before the anchor is an empty past rather than a loss.

After the window: seq 1006 was the newest record stored when the capture was taken, so the shortfall after the anchor is history that had not happened yet. A ring evicts from the bottom, never the top — nothing was lost here.

Spans: 0 captured, and the span ring had dropped nothing below seq 1001. **Session filters never narrow spans** — they are stored unconditionally — so whatever the verdict above says about the logs, the spans over this range are all of them.

Provenance: **3 of 3 core keys**, all present. Present: `/Action` (0s), `/Build/commit` (0s), `/Build/profile` (0s). Contextual: `/Env/region` (0s).

Nothing was asserted for this case alone.

## 3. What to do next

- **No collector was armed on `default`.** Arming one before the next run turns "it felt slow" into a distribution.

## 4. Anchor entry and neighbours

The anchor is marked `>`. Columns are seq, timestamp, level, message. The full stored window is in the logdata file (§7).

```
      1001  2026-08-03T21:39:45.996348857+00:00  INFO   checkout started
      1002  2026-08-03T21:39:46.020623207+00:00  INFO   inventory reserved
      1003  2026-08-03T21:39:46.045816183+00:00  WARN   payment gateway slow
      1004  2026-08-03T21:39:46.069987058+00:00  ERROR  payment gateway timeout
>     1005  2026-08-03T21:39:46.093389034+00:00  ERROR  checkout failed
      1006  2026-08-03T21:39:46.117357015+00:00  INFO   retry scheduled
```

## 5. Provenance

As of 2026-08-03T21:41:21.707641+00:00. Ages are since last validation; a key with no stated lifetime carries an age and no verdict.

**core** marks the three keys a case cannot be acted on without — `/Build/commit`, `/Build/profile`, `/Action` — graded in the front matter's `provenance` line. Everything else is contextual and is named rather than counted, since it is an open set.

| key | kind | value | age | in force since | lifetime |
|---|---|---|---|---|---|
| `/Env/region` | contextual | eu-west | 0s | 0s | — |
| `/Action` | core | checkout smoke run | 0s | 0s | — |
| `/Build/commit` | core | 9f3a11c | 0s | 0s | — |
| `/Build/profile` | core | release | 0s | 0s | — |

## 6. Collector state at capture

**No collectors were armed on domain `default` at capture** — by this session or any other. There are no measurements to go with these logs.

## 7. Evidence files

| file | records | first line |
|---|---|---|
| `checkout-hang-260803-214121.logdata.jsonl` | 6 | `{"logmon_format":1,"kind":"logdata"}` |
| `checkout-hang-260803-214121.spandata.jsonl` | 0 | `{"logmon_format":1,"kind":"spandata"}` |

Each file is JSONL. **The first line is a header, not a record**, and it is the format contract — `logmon_format` is the integer to branch on if the record shape ever changes. Every line after it is one record, and `records` above counts those alone.

Logdata records are log entries: `seq`, `timestamp`, `level`, `message`, `host`, and optional `full_message`, `facility`, `file`, `line`, `trace_id`, `span_id`, plus a `source` saying which rule stored the entry and `matched_filters` naming any filter it matched. `additional_fields` is an object holding every custom field the sender attached — a GELF `_request_id` arrives here as `request_id`, and it is where anything application-specific will be. Spandata records are spans: `seq`, `trace_id`, `span_id`, `parent_span_id`, `start_time`, `end_time`, `duration_ms`, `name`, `kind`, `service_name`, `status`, `attributes`, `events`. Both are seq-ascending, and both number their seqs from the same counter — which is why a span and a log never share one.

A file with no records still exists, with its header: absent cannot be told from "we captured none".
